# Attach menu & chip — трассы и слоу-мо (23.09.2026)

Заказ дизайнера: сравнить дропдаун с бордом 28726:64760 (фрейм `Menu` 30871:57297) и добавить
Liquid Glass-анимации открытия/закрытия меню, появления/удаления домена в поле, ховер и клик.

- `trace.mjs` — реальное время на собранном превью (`BASE`, `CHROME`): бокс меню против бокса «+»
  (ожидание: dx 0, dy 0, 208 × 105), стиль бокса и рядов, ховер 8 %, блум, фильмы входа, смены уровня,
  ухода, прихода чипа, удаления чипа, файловый чип, чип из ссылки без входа.
  `trace-after.txt` — прогон после `onUpdate`-заглушек (до них: op 0 на 182 мс входа, список на 0 на
  356 мс смены уровня, ✕ на 0 на 283 мс, меню на 1 перед размонтированием).
- `film.mjs` — слоу-мо (`K=6`: `performance.now` ×K для пружин motion, `playbackRate` для CSS-блика и
  WAAPI-сжатия чипа) → `film/*.png`; стилы 1:1 при dpr 2 — `still-menu.png`, `still-list.png`,
  `still-chip-and-menu.png`, `still-row-hover.png`, `still-x-hover.png`, `file-chip.png`.
- Контактные листы: `sheet-open.jpg` (вход, шаг 40 мс), `sheet-level.jpg` (смена уровня, 40 мс),
  `sheet-close.jpg` (уход, 25 мс), `sheet-chip-in.jpg` (приход чипа, 40 мс), `sheet-chip-out.jpg`
  (уход чипа, 30 мс).
