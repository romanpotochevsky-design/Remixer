# Remixer — UI/UX проект (DreamHost AI Website Builder)

## Навигация по базе знаний

Ищешь что-то конкретное — начинай с **`docs/README.md`**: там карта всей базы знаний и
правило размещения нового материала. Короткие адреса:

- **факты о продукте** (план, кредиты, цены доменов, тайминги) → `docs/knowledge/product-facts.md`
- **токены, типографика, стекло, моушен** → `docs/knowledge/design-system.md`
- **что уже решено и что уже отклонено** → `docs/knowledge/decisions.md`
- **глаголы и правила копирайта** → `docs/knowledge/glossary.md`
- **конкуренты одним экраном** → `docs/knowledge/competitors.md`
- **фича-базы знаний** → `docs/features/`: `builder-shell/`, `domains/`, `home-page/`
- **кто что делал в прошлых сессиях** → `docs/sessions/index.md`

Правило размещения: если незнание СЛОМАЕТ работу (жёсткое правило, дорого купленный урок,
проверенный факт) — это живёт здесь, в `CLAUDE.md`, потому что файл читается каждой
сессией. Референс-детали (пиксельная спека, таблица замеров, QA-отчёт, сырьё ресёрча)
живут в `docs/`, а здесь остаётся одна строка со ссылкой.

## Кто с тобой работает и что это за проект

Пользователь — **UI/UX дизайнер в DreamHost** (Roman). Он рисует интерфейс Remixer —
AI-билдера сайтов от DreamHost. **Он не программист**: не знает код, git, GitHub —
вся техническая сторона полностью на Claude. Объясняй технические вещи просто,
никогда не проси его выполнять команды или разбираться в коде.

Это **проект на годы**. Работа идёт постоянно, сессиями. Всё ценное — знания,
исследования, решения, код — обязано попадать в этот репозиторий
(`romanpotochevsky-design/Remixer`), потому что контейнер сессии умирает, а репозиторий
— нет. Правило: закончил осмысленный кусок работы → закоммить и запушь. Новое знание
о конкурентах или продукте → допиши в базу знаний в `docs/` и обнови этот файл, если
изменилось что-то фундаментальное.

## Две роли Claude в этом проекте

1. **Хранитель знаний и аналитик конкурентов.** Claude — «гуру» современных AI website
   builders: досконально знает, как устроены Lovable, Bolt, Base44, v0, GoDaddy Airo,
   Hostinger Horizons. Делает аудиты, ресёрчи, разборы. Подсказывает, советует и
   **останавливает дизайнера**, если тот начинает рисовать что-то неправильно или
   неоптимально — прямо говорит «стоп, вот почему». Знания накапливаются в `docs/`
   и не теряются.

2. **Разработчик прототипа.** Вместо прототипа в Figma — живой кликабельный интерфейс
   в `prototype/`: полностью рабочий фронтенд без бэкенда, с захардкоженными данными,
   настоящими анимациями и переходами. Аудитория прототипа: девелоперы (передача),
   продукт-оунеры и CEO (демонстрация). Он тоже развивается годами — архитектуру
   держать расширяемой, состояние централизованным (см. `prototype/src/state/world.ts`).

## Конкуренты

Чистые AI-билдеры (изучать в первую очередь):
- https://lovable.dev — эталон категории, единый цикл «договориться → сделать → проверить → откатить»
- https://bolt.new (StackBlitz) — лучшее раскрытие кода за иконкой `<>`
- https://app.base44.com (принадлежит Wix) — самая зрелая система токенов, 40+ страниц
- https://v0.app (Vercel) — лучшая воронка «докажи до регистрации», самопроверка скриншотами

Хостинги, которые как и мы делают AI-билдер (структурные близнецы):
- https://www.godaddy.com/airo/ai-builder — «наш двойник»: регистратор+хостер с AI-билдером
- https://www.hostinger.com/horizons — предостережение: билдер, закопанный в hPanel

Критика указала на слепые зоны, которые надо закрывать в будущих аудитах:
Cloudflare, Squarespace, Webflow, Canva, Wix Studio, 10Web, Durable,
WordPress-когорта (главная слепая зона — база DreamHost это WordPress).

## База знаний — что где лежит

**Карта и правила — `docs/README.md`.** Структура: `knowledge/` (долгоживущая правда
проекта) · `features/` (по папке на фича-область) · `sessions/` (журнал всех сессий) ·
`audits/`, `research/`, `handoff/` (готовые исследования и пакеты передачи).

**`docs/knowledge/` — долгоживущая правда:**
- `docs/knowledge/product-facts.md` — все проверенные факты о продукте (план, кредиты,
  цены доменов, тайминги DNS/SSL, ограничения регистратора) со статусом и датой у каждого;
  отдельный раздел «расхождения между источниками» — читать перед тем, как ставить число в макет.
- `docs/knowledge/design-system.md` — дизайн-язык: токены и их смысл, типографика, стекло,
  glow, моушен, геометрия шелла, перформанс-контракт, открытые вопросы к палитре.
- `docs/knowledge/competitors.md` — карта конкурентов на один экран + указатели в аудиты.
- `docs/knowledge/decisions.md` — журнал решений: дата · решение · причина · статус
  (принято / отклонено / заменено). **Смотреть ДО того, как что-то предлагать дизайнеру.**
- `docs/knowledge/glossary.md` — словарь: глаголы (Add / Connect / Publish / Update),
  де-жаргон, канонический чеклист успеха, внутренние термины прототипа.

**`docs/features/` — фича-базы знаний (детали и рассуждения, ⚠️-уроки остаются здесь):**
- `docs/features/README.md` — индекс фича-баз и шаблон новой.
- `docs/features/builder-shell/README.md` — шелл билдера: чат, композер, хореография
  отправки по такту, glow, резайзер, превью, панель Publish, правый рейл.
- `docs/features/domains/README.md` — доменный модуль: дашборд, поиск, результаты,
  чек-аут-шит, три сценария подключения, открытые дыры в макетах.
- `docs/features/home-page/README.md` — входная страница приложения: герой с композером,
  промпт-чипы, док с проектами и шаблонами (Figma 28364:40053 / 28375:43006). Рядом с ней:
  `figma-spec.md` (пиксельная спека + 22 вопроса дизайнеру) и `qa-report.md` (независимая
  пиксельная проверка).

**`docs/sessions/` — журнал всех сессий, чтобы работа не терялась и не переделывалась:**
- `docs/sessions/README.md` — как вести журнал (одна сессия = один файл `YYYY-MM-DD--slug.md`).
- `docs/sessions/index.md` — таблица всех известных сессий, новые сверху.
- Запись текущей сессии пишется **до** её конца.

**Аудиты, исследования, хендоффы (готовое; дополнять, а не переписывать задним числом):**
- `docs/audits/synthesis.md` — полный конкурентный аудит Q3 2026 (~97 тыс. знаков, 10 разделов):
  executive summary, разбор Lovable, per-competitor teardowns, table stakes,
  catch-up list (3 тира), overtake list (§8), design-craft правила (§9), риски.
- `docs/audits/critique.md` — адверсариальный разбор аудита: чего не хватает, какие
  утверждения нельзя цитировать, где советы вредны для новичков. **Читать в паре с synthesis** —
  он исправляет перегибы аудита.
- `docs/audits/measured-design-tokens.md` — замеры с живых сайтов: шрифты, OKLCH-палитры,
  тени, моушен Lovable/Bolt/v0/Base44/Airo (13.08.2026).
- `docs/audits/lovable-builder-teardown.md` — живой билдер Lovable изнутри, publish-флоу по 5 состояниям.
- `docs/audits/bolt-in-app.md` — живой Bolt изнутри.
- `docs/audits/lovable-prebuild-questions-flow.md` — **слабый промпт у Lovable: вопросы перед
  генерацией** (06.09.2026). Разобрано покадрово по экранной записи дизайнера: схлопнутое превью и
  чат 600px по центру, панель из четырёх вопросов НАД композером («Skip all» / «Next» / «Submit»),
  сводка-карточка, «Got it — …», раскрытие превью за ~0,4 с, схлопывание драгом разделителя.
  Плюс правила Design guidance из docs, замеры, матрица конкурентов, чеклист живой проверки.
  Опорные кадры: `docs/audits/lovable-prebuild-flow/01…17.jpg`.
  Артефакт-разбор с кадрами: https://claude.ai/code/artifact/3dceb824-ed13-4d5d-ae2e-96a9ba7eee31 Это эталон для флоу Remixer
  «слабый промпт → бриф → билд» — реализован в прототипе (см. ниже).
- `docs/audits/remixer-competitive-audit.html` — итоговый HTML-документ аудита.
  Опубликован как артефакт: https://claude.ai/code/artifact/df670799-1873-4556-a689-584d0f11f9f1
- `docs/research/recon_dossiers.json` — сырьё: 6 досье по конкурентам.
- `docs/research/lenses.json` — 6 UX-линз.
- `docs/research/factcheck_corrections.json` — поправки фактчека.
- `docs/research/domain-search-research.md` — глубокий ресёрч умного поиска доменов
  (5 агентов, 433 запроса): факты DreamHost, UX поиска у 10 конкурентов, AI-подсказки,
  «свой домен», техническая детекция (RDAP, PSL, Domain Connect-проба).
- `docs/handoff/lovable-brief-flow-handoff.md` — хендофф флоу «слабый промпт → вопросы → билд»:
  схема состояний, план интеграции веток, что осталось (P1–P3), уроки окружения. **Раздел 5
  (интеграция в основной прототип) выполнен 07.09.2026 — читать раздел 0.1 первым: там, где
  реальность разошлась с планом, и что осталось.**
- `docs/handoff/domain-connection-design-handoff.md` — хендофф домен-флоу и publish-панели.
  Figma: https://www.figma.com/design/GP4jNXtc37VTFVZDc9JF0a/AI-Website-Builder,
  страница «Domain Connection Flow». Выбранное направление publish-панели — «Launchpad» (⑥-A).
- `docs/handoff/siri-glow-spec.md` — инженерная спека Remixer glow для девелоперов:
  стоимость в кадрах, анатомия слоёв, губернатор качества, порт-ноты.
- `maintenance-page/` — готовая maintenance-страница (актуальная версия: кнопка 56px pill
  со стеклянным hover; .htaccess отдаёт 503 + Retry-After, чтобы Google не выкинул URL).

## Прототип (`prototype/`)

React 18 + Vite + TypeScript + Tailwind + zustand + motion. Запуск: `npm install && npm run dev`.
Сборка одиночного HTML для артефакта: `npm run artifact` (`scripts/build-artifact.mjs`
зашивает CSS, JS и все .woff2 в base64 — на опубликованной странице CSP режет любой
внешний запрос).
⚠️ **Лимит публикации неизвестен; `CEILING` в скрипте — это ПОЛ, а не потолок.**
07.09.2026 опубликовались подряд 707 289, 724 265, 740 372, 754 590, 754 641, 754 751, 756 148, 756 860 и (08.09) 758 511, 759 793, 760 375, 763 640, 765 199, 765 334, 768 817, 767 155, 766 032, 773 849 и **773 852** байта, хотя тот же
скрипт до этого называл 704 239 «2.3 KB под лимитом», а 754 259 — приговором. Последняя пара
и есть весь аргумент: файл на 331 байт МЕНЬШЕ того, что уже опубликовался, скрипт объявил
мёртвым. Поэтому скрипт печатает headroom, а не
приговор: если файл больше — пробовать публикацию, и при успехе поднимать `CEILING`
до нового известного размера. Понижать «на всякий случай» нельзя — число ценно только
как доказательство.
⚠️ **Сначала `pip install fonttools brotli`, иначе файл не влезет в лимит публикации.**
Сабсеттинг шрифтов делает `pyftsubset`, а в свежем контейнере его нет; скрипт в этом случае
не падает, а просто зашивает полные латинские подмножества — и это ровно та разница, из-за
которой 07.09.2026 файл собрался на 754 259 байт («OVER by 46.6 KB») вместо 704 155
(«2.3 KB под лимитом»). Урезание шрифтов до реально используемых ~205 глифов экономит 37 КБ.
Проверять не по памяти: скрипт сам печатает и размер каждого шрифта до/после, и headroom.

**Шрифты.** Бренд — Gilroy (имена и цифры) + Proxima Nova (проза), оба коммерческие.
Лицензионные файлы в репозиторий НЕ коммитятся: репозиторий публичный, а это было бы
распространение. Кладутся локально в `prototype/public/fonts/` под именами
`Gilroy-{Regular,Medium,SemiBold}.woff2` и `ProximaNova-{Regular,Medium,Semibold}.woff2`
— `@font-face` для них объявлены ПЕРВЫМИ, поэтому файл начинает работать сразу, как
появился (см. блок TYPEFACES в начале `src/index.css` и `public/fonts/README.md`).
Пока их нет, рендерятся OFL-подмены, зашитые в `src/fonts/`: **Figtree за Proxima Nova,
Outfit за Gilroy**. Это НЕ брендовые начертания, по ним нельзя принимать дизайн — но
метрики близкие (строка 244px против 271px у системного) и, главное, одинаковые на
любой машине. До этого прототип рисовался тем, что нашлось у хоста: SF Pro на маке,
Segoe на винде, DejaVu в Linux-браузере — три разных лейаута, и только один совпадал
с Figma. Кириллицы у подмен нет, UK-локаль падает на системный шрифт по глифам.
⚠️ Ссылки на `/fonts/...` — абсолютные СПЕЦИАЛЬНО: Vite не трогает абсолютные `url()`,
поэтому отсутствующий файл = тихо не загрузившееся начертание, а не падение сборки.
⚠️ `body` берёт `font-family: theme(fontFamily.sans)`, не литеральный стек. Литерал
там стоял раньше и молча перебивал конфиг Tailwind — из-за этого подмена не
применялась к телу документа, хотя была прописана в `tailwind.config.js`.
⚠️ Чтобы вернуть настоящие шрифты навсегда — **сделать репозиторий приватным**, убрать
`public/fonts/*.woff2` из `.gitignore` и закоммитить. Пакеты на npm, которые обещают
эти шрифты (`@qpokychuk/gilroy`, `@dannymichel/proxima-nova`), — нелицензированные
перепаковки, в репозиторий DreamHost их не ставить.

Архитектура (не ломать, расширять):
- `src/state/world.ts` — **единственный источник правды**. Все экраны рендерятся из одного
  объекта World: account × inventory (что за домены у юзера) × domain state × project × chat.
  Новое измерение состояния сначала добавляется сюда.
- `src/state/flows.ts` — декларативный движок флоу: сценарий проигрывается целиком от начала
  до конца («что происходит, когда я подключаю домен с GoDaddy») — для демо CEO.
  Шаги с реальным решением ставят `awaitUser`; ожидания сжаты, но пропорциональны.
- `src/state/scenarios.ts` + `src/devtools/ScenarioPanel.tsx` — консоль сценариев (мировых состояний).
- `src/i18n.ts` — EN по умолчанию (продукт для рынка США), UK вторым.
- Геометрия шелла — **редизайн 2026** из Figma (узел 25819:143144 «Website Builder»,
  снято 16.08.2026): чат СЛЕВА 432px, топбары 52px, рейл СПРАВА 56px (аватар + Style/
  Extension/Analytics/Cloud 48px + freshchat), фон #09090b, «стеклянные» контролы
  (rgba(24,24,27,.8) + blur 16 + белый 12% бордер), панель Publish 548px радиус 20.
  Старая геометрия живого продукта (57/64/522) устарела — не откатывать.
- `src/ui/icons.tsx` — иконки перерисованы вручную (SVG-экспорт Figma блокирует прокси).
  ⚠️ **Одноимённая иконка из двух веток — не текстовый конфликт, а вопрос ВЕСА** (07.09.2026,
  слияние home-page и брифа). Обе ветки нарисовали свой `IconChevronRight`: у домашней
  страницы это концевик ряда чипов по узлу Figma (штрих 1.8 в боксе 20 — глиф занимает 55 %
  высоты), у футера брифа — половина ЗЕРКАЛЬНОЙ пары из общего `base` (24, штрих 1.7, 42 %).
  Оставить любую одну — и внутри одного контрола ‹ › рядом окажутся глифы разного размера и
  веса. Поэтому пара стала `IconCaretLeft`/`IconCaretRight`, а концевик чипов сохранил вес,
  который даёт борд. Правило: сверять НЕ имя, а бокс и штрих, и переименовывать ту иконку,
  чьё имя не защищено узлом Figma.
- `src/ui/SiriGlow.tsx` + секция «Siri edge glow» в `index.css` — фирменный «Remixer glow»:
  свечение по кромке превью во время процессов (генерация/агент/перезагрузка). Сведён
  с реальным эффектом Siri по стоп-кадрам и фото устройств. **Перформанс-контракт:
  ни одно свойство не инвалидирует paint покадрово** — только transform/opacity
  (наивная версия давала 9 FPS). Встроенный «губернатор качества»: телефоны и слабые
  машины автоматически получают лёгкий срез. Спецификация для девелоперов:
  `docs/handoff/siri-glow-spec.md`.
  ⚠️ Урок: Tailwind вычищает классы, собранные динамически (`siri-layer--${k}`) —
  имена классов в tsx всегда писать полными литералами.
  ⚠️ **Губернатор качества меряет ВО ВРЕМЯ работы свечения и стартует с лёгкого среза.**
  Старая версия замеряла кадры ДО включения свечения — видела спокойную страницу,
  решала «машина быстрая» и навсегда ставила полные 4 слоя: пять загрузок подряд все
  до одной уходили в FULL, то есть защита не срабатывала никогда. Теперь наоборот:
  открываемся на лёгком срезе, меряем 800 мс уже со свечением на экране, и полный
  режим включается, только если страница держала >50 fps.
  ⚠️ **…и FULL — не пожизненный приговор.** Проба доказывает, что машина тянет ДВА
  слоя, а выдаёт четыре: пограничная машина проходила пробу, а все следующие
  отправки заикались («после перезагрузки работает, потом деградирует»). Пока
  полный срез на экране, замер продолжается: два плохих окна подряд (<30 fps по
  800 мс) — понижение до lite до конца сессии. Два, не одно: одно окно может
  съесть посторонний GC.
  ⚠️ **`busy` — союз трёх источников, а союз прячет переходы.** Эффект свечения,
  зависящий только от [busy], не перезапускался, когда отправка случалась ВНУТРИ
  reload-пульса (3.2 c): пружина пузыря шла под уже горящим свечением без всякой
  700 мс-форы; симметрично ответ, пришедший при живом reload, печатался под
  свечением целиком. Эффект теперь зависит и от `working`: новая отправка гасит
  свечение и заново взводит 700 мс, приземлившийся ответ — 1200 мс (печать ≤1.1 с).
  ⚠️ **Снимать свечение быстро — 220 мс, не 700.** Ответ начинает печататься ровно
  в момент, когда свечение гасят, и каждый лишний кадр с размытыми слоями отнимается
  у текста: замер печати 5 fps при длинном затухании против 33 fps при коротком.
  ⚠️ **Панель превью в claude.ai — не то же, что вкладка браузера.** Тот же файл,
  открытый отдельным окном, играет анимации нормально, а во встроенной панели их
  «нет»: там меньше бюджета, и свечение съедает кадры. Проверять анимации нужно
  именно в панели — это то, как прототип смотрят.
  ⚠️ **Свечение стоит почти весь кадровый бюджет.** Замер на опубликованной сборке
  (браузер без GPU): со свечением страница рисует 4 fps, без него 60. Перенос блюра
  внутрь, «лёгкий» срез и отключение «дыхания» не помогают — дорого само размытие
  больших поверхностей. Поэтому свечение **стартует через 700 мс после отправки**
  (`App.tsx`), иначе оно съедает кадры у пружины пузыря и у печати ответа, и обе
  анимации выглядят как «их нет».
  **Glow — единственный индикатор загрузки превью.** Никакого скелетона, затемнения
  и перемонтирования сайта: страница остаётся видимой, весь сигнал «идёт работа»
  несёт вспышка появления + бегущее свечение (как на реальном устройстве, где эффект
  играет поверх любого контента). Скелетон был — убран осознанно, не возвращать.
  Режимы поверхности: `light` (узкий ободок), `dark` (полное свечение),
  `split` (сайт светлый сверху / тёмный снизу — статичная маска разводит слои по границе).
- **Стекло — по macOS, не по iOS.** База из Figma (`Preview buttons` 25819:143747):
  заливка `rgba(24,24,27,.8)`, `backdrop-blur 16`, дивайдеры внутри групп `#ffffff0a`
  (Neutral Alpha/50). Рамка — **косой градиент**, а не ровная линия: настоящее стекло
  ловит свет неравномерно. Но амплитуда мягкая, вокруг фигмовских 12% (Neutral Alpha/200):
  20% → 7% → 16% по диагонали 150°. Что делало версию «айосной» — это была амплитуда
  (32% белого в горячем конце) плюс внутренняя подсветка сверху, тень и saturate; всего
  этого быть не должно. Рамка рисуется маской по border-box (`::before`), не `border` —
  чтобы не сдвигать лейаут и следовать радиусу.
- **С iOS 26 берём ТОЛЬКО анимации, не материал.** Стекло — там, где оно есть в Figma
  (кнопки-пилюли в тулбарах, пузыри чата). Панель Publish в макете — сплошная `gray-850`,
  и такой и остаётся: полупрозрачное стекло на ней было ошибкой, откатано.
  ⚠️ Бегущий блик по кромке панели при открытии (`.glass-sheen`) — тоже откатан
  (17.08.2026, вопрос дизайнера «зачем этот эффект блика?»). Блик — это признак
  СТЕКЛА, свет, скользящий по прозрачной поверхности; на непрозрачной панели он
  украшает материал, которого у неё нет. От iOS-26 у панели остаётся только моушен.
- **Моушен — `src/ui/motion.ts`, один язык на все оверлеи** (взято с iOS 26): пружины
  вместо длительностей; попап растёт из угла своего триггера (`transform-origin`);
  контент приезжает на 60 мс позже контейнера; уход быстрее прихода и без отскока.
  Анимируются только `transform` и `opacity`. Новые меню/дропдауны брать отсюда,
  а не писать свои `duration/ease`.
- В чате две растушёвки, обе sticky и обе **чистый градиент без плоского участка**
  (плоская «голова» читается как обрезанный край): сверху 48px `#09090b`→прозрачный
  (Figma 28016:43309), снизу 32px прозрачный→`#09090b` вплотную над полем (28016:46454).
  ⚠️ У скроллера НЕ должно быть верхнего паддинга: sticky прилипает к padding-краю
  вьюпорта, и любой `pt` оставляет над растушёвкой щель, в которой сообщения проезжают
  открытыми. Растушёвка сама стоит в потоке и заодно служит отступом над первым
  сообщением; снизу колонка резервирует `pb-8`.
  Между чатом и канвасом — `src/ui/ChatResizer.tsx`: в покое hairline `--white-100`
  (Neutral Alpha/100; в макете колонка 432, всё внутри 430–431 — этот пиксель и есть
  линия), за него можно **тянуть** и менять ширину чата (340–760). На драге линия
  становится синей, у курсора — ручка «◁▷», и по линии едет блум, привязанный к
  позиции курсора (у Lovable подсветка ровная — у нас свет там, где рука).
  ⚠️ Свечение резайзера — ОДНОТОННО-СИНЕЕ, и это решение дизайнера. Иридесцентная
  версия в спектре Siri (голубой→фиолетовый→розовый, «для консистентности») была
  собрана и показана 17.08.2026 — отклонена («выглядит стремно, верни как было»).
  Урок: спектр принадлежит AI-моментам (флеш отправки, свечение превью) — резайзер
  это утилитарный инструмент, радуга на нём читается как декорация. Не предлагать
  повторно.
  ⚠️ Драг НЕ идёт через React: ширина пишется прямо в `--chat-w` на `<html>`, блум
  двигается `transform`. Ререндер шелла на каждый pointermove перезапускал бы чат,
  превью и его container queries 60 раз в секунду. В стор значение падает на отпускании.
- **Отправка уводит сообщение НАВЕРХ вьюпорта** (как у Lovable, снято с видео их билдера):
  прочитанный тред уезжает, твой запрос встаёт под шапкой, всё под ним пустое — там
  появится ответ. Чат, липнущий к низу, наоборот выталкивает твоё же сообщение с экрана,
  пока пишется ответ. Для этого в конце списка растёт спейсер (`ChatPanel`), считается
  один раз на отправку, не во время ответа — иначе лейаут прыгает.
  ⚠️ Высоту «сколько контента ниже» мерить по самому списку, НЕ по `vp.scrollHeight`:
  он никогда не меньше вьюпорта, и на коротком треде замер врёт.
  ⚠️ Спейсер при замере НЕ трогать — вычитать его высоту, а не обнулять. Обнуление
  укорачивало диапазон прокрутки, браузер зажимал `scrollTop`, и тред дёргался.
  ⚠️ **В reduced-motion блоке писать `transition: none`, а НЕ `transition-duration: .01ms`.**
  Идиома с .01ms оставляет `transition-property: all`, поэтому у каждого элемента висит
  живой микропереход: запись стиля + синхронное чтение лейаута возвращают СТАРОЕ значение
  (у перехода в t=0 это from-значение). Замерено: спейсер 509px → пишем 0 → читаем 509.
  Ошибка в 509px уезжала в расчёт прокрутки, диапазон схлопывался, и весь тред
  телепортировался на 234px ровно в момент пружины пузыря. `.01ms` нужна только чтобы
  срабатывал `transitionend` — у нас его никто не слушает.
  ⚠️ Прокрутка стартует ЧЕРЕЗ 620 мс после отправки — ровно длина пружины пузыря.
  Одновременно с пружиной анимации отправки просто не видно: глаз следит за уездом
  всего треда. Сначала пузырь прилетает, потом тред уносит его наверх.
  ⚠️ Флаг анимации — по каждому сообщению (`seen` + `fresh` set), не общий: с общим флагом
  каждая отправка перерисовывала старые ответы и весь тред перепечатывался. И «свежесть»
  не должна сбрасываться на следующем рендере, иначе анимированные слова сносит на лету.
  ⚠️ **Id сообщений продолжаются от максимума в транскрипте, не от константы.**
  Счётчик — модульная переменная и умирает со страницей, а `world.sent` живёт в
  localStorage с id 1001+. Голый `++seq` после переоткрытия переиспользовал занятые
  id, «новое» сообщение уже лежало в `seen` — и пузырь с печатью молча не играли,
  пока новые id не перерастали старый транскрипт (поэтому баг выглядел случайным).
  Пути восстановления транскрипта: открытие с чистым URL (так открывается артефакт
  из галереи) и перезагрузка посреди отправки.
  ⚠️ Тред, который есть на первом рендере, считается не только «увиденным», но и
  «запаркованным»: демо-треды кончаются сообщением юзера, и без этого парковка
  принимала его за свежую отправку — тред сам собой скроллился через 0.7 с после
  загрузки. И у парковки в cleanup гасится не только таймер, но и rAF: в скрытой
  вкладке rAF замерзает в очереди и по возвращении стрелял скроллом по мёртвой
  разметке.
  ⚠️ **Reduced-motion должен гасить ОБА движка.** CSS-блок глушит только CSS;
  пружины motion/react игнорируют медиа-запрос, пока в корне не стоит
  `<MotionConfig reducedMotion="user">` (main.tsx). Без него под системным
  «Reduce Motion» пузырь прыгал, а печать была мертва — читалось как «сломано»,
  а не как уважение к настройке.
  ⚠️ **…но `MotionConfig reducedMotion="user"` НЕ отменяет смещение — он ПРЫГАЕТ
  в него.** Флаг отключает transform-анимации, а «отключает» значит «значение
  ставится в цель сразу»: вариант с `exit: { y: -12 }` под reduce перестаёт ехать,
  но делает СКАЧОК на 12 px при opacity .906, и только потом фейдится (замерено
  на полке дока, 26.08.2026). Скачок хуже слайда. Вариант, у которого цель на
  выходе — смещение, обязан под reduce выбрасывать САМО смещение: в `motion.ts`
  для этого лежит `listSwapFade`, выбирается через `useReducedMotion()`. Тот же
  скрытый скачок до сих пор сидит в `listSwap` доменной поверхности.
  ⚠️ Не оборачивать печатающийся ответ в motion-анимацию opacity: вложенность гасила
  CSS-анимацию слов (весь блок стоял на opacity 0, слова — на currentTime 0).
- **Флеш при отправке — «налить и слить», не комета. ПРИНЯТО дизайнером 17.08.2026
  («отлично, идеально») — тайминги и хореографию без его просьбы не трогать.**
  Хореография по его скетчу: свет рождается в нижнем-левом углу, разливается по левому
  борту → верху → правому борту, в середине горит ВСЯ подкова (низ тёмный), пауза-позы
  ~30 мс на клоке 0.7 с (финальные цифры дизайнера; ужималось трижды с 200 мс/1.15 с —
  «быстрее и чтобы не било по глазам»), затем хвост отпускает и свет стекает слева
  направо, умирая оранжевым в нижнем-правом углу. Прошлая версия — короткая дуга, летающая по периметру, —
  читалась как «летающая колбаска» и снята.
  Конический градиент не умеет растить дугу (анимация угла = путь к 9 FPS), поэтому
  подкова собрана из **трёх 90°-дуг** (`cg-arc-left/top/right`), запаркованных вдоль
  нижней кромки (135°–225° в квадратном пространстве) и выезжающих поворотом на
  1×/2×/3× скорости; слив зеркально (3×/2×/1×), суммарно +360° — конец = начало.
  Парковку прячет непрозрачный «пол» под нижней кромкой с graded-концами; углы
  открыты, поэтому рождение и смерть света — угловые события.
  ⚠️ Инварианты бесшовности (сломаешь — появятся дыры): у всех трёх дуг ОДИНАКОВАЯ
  timing-function в каждом сегменте keyframes; тела дуг непрозрачные; порядок
  отрисовки right → top → left (жёсткий хвост каждой дуги всегда накрыт дугой выше);
  в позе стыки попадают на совпадающие цвета (violet-violet, red-red). Alpha-рампа
  только у хвоста левой дуги — это единственная видимая кромка.
  ⚠️ Аспект: градиенты нарисованы в КВАДРАТНОМ пространстве (углы коробки — честные
  45/135/225/315), а `scaleX(var(--flash-sx))` растягивает на реальную коробку.
  Растяжка живёт ВНУТРИ transform самих дуг (после rotate), НЕ на обёртке:
  трансформированная обёртка между фильтрованным `i` и детьми — ровно та вложенность,
  которую ест хромовский баг overflow-внутри-filter.
  Движок тот же, что у Siri-glow; материал и толщина по-прежнему по записи Google
  AI Mode: тонкая полоса света снаружи коробки, внутри чисто.
  ⚠️ Chrome НЕ рендерит маску (и `overflow: hidden`) внутри элемента с `filter` —
  проверено по отдельности: каждое само по себе рисует, вложенные гаснут. Маска и блюр
  должны быть на ОДНОМ элементе; плата — маска обрезает разлёт блюра.
  ⚠️ Две крайности, в которые я уже сваливался, — не повторять: жёсткая обводка 2px
  НА бордере читается как неон, а размытая клякса за полем (inset −42, blur 26–44)
  заливает цветом само поле, потому что оно прозрачное на 80% и всё, что за ним,
  просвечивает.
  ⚠️ **Никакой маски у флеша.** Маска применяется ПОСЛЕ фильтра и срезает растушёвку
  блюра в жёсткие края с обеих сторон — получается «толстый градиентный контур».
  Рабочая конструкция: конус клипается скруглённым боксом (overflow на ТОМ ЖЕ
  элементе, что и blur), блюр размывает форму наружу как настоящий свет, тело полосы —
  3px выступа за край поля, а внутрь свет не пускает непрозрачная пластина `#09090b`
  точно под полем (поле прозрачное — без пластины просвечивает).
- **Композер — по Figma (28016:43526/43528), `.composer-field`:** поле — ТЁМНАЯ
  поверхность (`rgba(9,9,11,.8)` + 8% белого, `backdrop-blur 16`), темнее окружающего
  хрома; кружки «+» и микрофон на нём — наоборот, СТЕКЛО (`liquid-glass` с косым
  градиентным бордером) с заливкой `Black/700`. Рамка поля — `Neutral Alpha/300`
  (белый 24%), радиус 24; кружки «+» и микрофон — `rgba(9,9,11,.64)` + та же рамка 24%
  (белый 24%), ярче общих 12% у хрома. Кнопка отправки в покое контурная, без заливки.
  ⚠️ **Токены Figma MCP читать в ТЁМНОЙ теме.** Экспорт резолвит `Neutral Alpha`
  непоследовательно: на кнопках отдал `rgba(255,255,255,.24)`, на поле — светлый
  `rgba(9,9,11,.24)`, и заливку поля показал как «8% чёрного поверх #09090b».
  Буквально это красит поле в цвет фона чата — поверхность исчезает, и стеклу кружков
  не на чем работать. В тёмной теме та же подмешка — 8% БЕЛОГО, и база у поля стеклянная,
  а не чёрная: в макете поле заметно СВЕТЛЕЕ фона, а «+» и микрофон — тёмные диски на нём.
  Кнопка отправки в покое контурная, без заливки (28016:43545), синей становится только
  когда есть что отправить.
- Индикатор ожидания — «Thinking» с бегущим по буквам бликом (`.thinking`,
  `background-clip: text`). Это единственное место, где repaint покадрово допустим:
  одно короткое слово, живущее только пока пишется ответ.
- Моушен чата: своё сообщение выпрыгивает из композера как в iMessage (`bubbleSend`,
  origin bottom-right, пружина `bounce .45 / duration .9`, старт со scale .72×.48 и
  сдвигом +26/+52 к кнопке отправки). ⚠️ Короткая версия (.5 с, scale .6) РЕНДЕРИЛАСЬ —
  проверено покадрово, — но заканчивалась раньше, чем глаз успевал заметить, и читалась
  как «анимации нет». Здесь читаемость важнее сдержанности: это единственный жест,
  который делает пользователь. Прокрутка треда ждёт 620 мс — ровно длину пружины, ответ Remixer «печатается» —
  все слова свёрстаны сразу, но проявляются со ступенькой (`.stream-word`, шаг сжимается
  на длинных ответах, весь ответ ≤ ~1.1 с), кнопки под ответом ждут конца печати.
  ⚠️ Без blur на словах: blur-in красивее, но перерисовывает текст каждый кадр, а в полёте
  одновременно ~15 слов — это ровно тот per-frame paint, который запрещён после 9 FPS.
  Анимируются только сообщения, пришедшие ПОСЛЕ первого рендера (`settled` ref),
  иначе весь тред «выпрыгивает» при каждой загрузке страницы.
- Чат живой: `modules/chat/send.ts` — одно сообщение двигает весь шелл (пузырь → `chat:
  'working'` → glow на превью → через 2.6 с захардкоженный ответ → −10 кредитов →
  +1 к unpublished). Ответы подбираются по ключевым словам в `modules/chat/thread.ts`
  (фото, палитра, мобилка, цены, меню, форма, публикация…), остальное — ротация из трёх
  общих. Транскрипт живёт в `world.sent`; первое отправленное сообщение «замораживает»
  демо-тред сценария внутрь этого массива, дальше `chat` работает только как статус.
  Смена оси `chat` (переключение сценария) обнуляет `sent`.
- Переключатель девайсов — **одна кнопка**, как у Lovable (проверено по видеозаписи их
  билдера): иконка = текущий режим (монитор → после клика телефон), клик переключает.
  Мобильный режим — **фиксированная рамка 390×844** (iPhone 14/15), центрированная по
  обеим осям: сверху и снизу остаётся подложка канваса, а не колонка во всю высоту.
  Glow и скролл идут по рамке телефона. Сайт внутри перестраивается по-настоящему, потому что отвечает
  на **container queries**, а не на ширину окна (у Lovable это даёт iframe; у нас — контейнер
  `.site-stage`). Респонсив-размеры сайта живут в CSS (`.site-pad`, `.site-grid`,
  `.site-hero-title`…), а не в Tailwind-утилитах: утилиты лежат в более позднем слое и
  перебивают правила `@container`. В мобильном режиме у рамки hairline — иначе тёмная
  половина сайта сливается с фоном шелла.
- **Окно доменов — дашборд по Figma 27085:106382 / 26181:33524** (`DomainsSurface`):
  рамка 16 c бордером `gray-800` и двойной тенью, база темнее контента (24% чёрного
  поверх `gray-900`) — она видна только за топбаром 48px (глобус 32 в `Black/600`,
  «Domains» 13px `#e4e4e7`, закрытие 32 на `white-100`); листы контента перекрашивают
  себя в `gray-900` и отделяются волосками NA/100 (хедер) и NA/50 (страница).
  Хедер 195px: тайтл 40px по центру (pt 29 / pb 30) + пилюля поиска 880×56
  (`gray-700`, рамка NA/50, плейсхолдер 17px 48% белого, белый круглый сабмит 32).
  Колонки ≤1200: «Existing domains» (имя 17px + контурный Connect 36px, ничего
  больше — у своих доменов нет цены) и «AI suggestions» (герой Best match +
  список). Обе списочные карточки: `#ffffff08` + рамка NA/50, радиус 16, ряды 72px
  с ховером NA/50 и волосками-разделителями. Цены из `TLD_PRICES` (verified),
  «Renews at $X» 12px — правило «renewal не прячем» соблюдено; в герое зачёркнут
  renewal, крупно первый год. Глаголы по макету: Connect (своё) / **Buy** (новое).
  ⚠️ Градиентная рамка героя — кольцевой маской (`.bestmatch-rim`), НЕ вторым слоем
  background под полупрозрачной заливкой: заливка 10% альфы, непрозрачный градиент
  под ней просвечивает целиком (был сплошной фиолетовый бар).
  ⚠️ Расхождение с аудитом: словарь аудита говорит «Add = купить», макет рисует
  «Buy» — реализовано по макету, вопрос поднят дизайнеру.
- **Результаты поиска — Figma 27729:14650** (`ResultsScreen`). Порядок блоков —
  смысловой, не косметический: сначала КЛАССИЧЕСКАЯ выдача (точное совпадение
  героем + то же имя в других зонах + футер «Show more endings» / «400+ more
  available»), и только потом AI-блок «Name ideas for your site» с другими
  ИМЕНАМИ и причиной у каждого. Человек, который ввёл имя, просит именно первое;
  закопать это под AI-выдачу — категориальная ошибка.
  ⚠️ **Шапка с поиском монтируется ОДИН раз** (`SearchHeader` живёт в
  `DomainsSurface`, снаружи `AnimatePresence`), и состояние поля тоже лежит там.
  Раньше шапка была внутри каждого экрана — поиск подменял экран целиком, поле
  перемонтировалось, теряло фокус и каретку, и это читалось как перезагрузка
  страницы, а не как «пришёл ответ» (замечание дизайнера 17.08.2026). Сейчас
  меняются только списки под шапкой: `listSwap`/`listSwapItem` в `motion.ts` —
  уходящий контент уезжает ВВЕРХ, приходящий поднимается снизу со ступенькой по
  секциям (конвейер, а не кроссфейд). Проверено: `<input>` после поиска — тот же
  DOM-узел, фокус и текст сохраняются.
  Заголовок у этого экрана 32px и тёмно-стеклянный сабмит 40px, у пустого
  состояния — 40px и белый 32px. Так как шапка теперь постоянная, это расхождение
  макетов стало ПЕРЕХОДОМ (герой сжимается, как домашняя страница поисковика
  оседает в выдачу), а не двумя компонентами; кнопка — одна и та же, морфится.
  Вопрос дизайнеру открыт: если он хочет шапку неподвижной — заморозить `compact`.
  ⚠️ В макете НЕТ состояния «домен занят» — ни одного. Самое важное состояние
  доменного поиска не нарисовано; поднято дизайнеру, не выдумывать самим.
- **Чек-аут-шит — Figma 27058:100133 / 27254:11737 / 27275:33023** (`DomainModal`).
  Три борда рисуют шесть состояний ОДНОГО шита по двум осям, поэтому это один
  компонент: `connect-existing` (без цены) / `buy` (первый год + честное
  продление) × «есть план» (узкий шит) / «нет плана» (шит вырастает на
  плановый выбор Yearly/Monthly, CTA становится «Continue to checkout»).
  Это **app-модалка**: скрим 70% чёрного накрывает и чат, и правый рейл, поэтому
  монтируется в самом верху дерева (`App.tsx`), не внутри `<main>`.
  ⚠️ **Стена плана перед всей доменной поверхностью убрана.** Раньше юзер без
  Remixer Build видел вместо дашборда апгрейд-экран. Все три борда рисуют
  триального юзера, стоящего в ПОЛНОМ дашборде со шитом поверх: платёж
  раскрывается в момент покупки, внутри шита. Это же правило аудита — «цену
  до корзины». Гейт теперь живёт только в `DomainModal`.
  ⚠️ Цена продления в макетах ($11.86) не встречается ни в одной строке
  проверенной таблицы DreamHost — это плейсхолдер. Цены берём из `TLD_PRICES`.
  ⚠️ Узкий шит — 600, а не 560, когда несёт цену: Gilroy и Proxima Nova в
  прототип не зашиты, и в более широком системном фолбэке строка на 560 обрезается.
  600 нарисован на борде 27058, так что это в пределах нарисованного.
  ⚠️ Карточки планов в макете разной высоты (72 выбранная / 80 нет) и с разными
  токенами подписей — сделано как нарисовано, помечено дизайнеру как вероятная
  случайность, а не «поправлено» молча.
- **Home page — СТРАНИЦА, а не поверхность шелла** (Figma 28364:40053 канон / 28375:43006
  полка шаблонов / 28364:39116 устаревший, не реализовывать).
  ⚠️ **Фон героя и хореография загрузки — по ЗАПИСИ ПРОДАКШЕНА, не по борду** (решение
  дизайнера 26.08.2026 после отклонения входа v1: «ни градиент, ни анимация не похожи»);
  борд остаётся правдой лейаута, типографики, композера, чипов и карточек — не «чинить»
  краску назад к борду. `src/Root.tsx` — тончайший
  switch по `ui.page`; `App.tsx` не оборачивать и не перепарентить (его тайминги выверены
  до миллисекунд). Герой — инсет 8px, 1640×812 r20, прозрачный топбар 72 ПОВЕРХ него, док
  1656×376 full-bleed **без фона, волоска и тени** (скруглённые нижние углы героя — весь
  разделитель), композер фиксированные 960. Док в двух формах по оси `projects` в
  `world.ts`: вкладки `My projects | Templates` (есть сайты) / заголовок + фильтр-чипы
  (сайтов нет). Enter из композера = первое сообщение чата в билдере. Вся подложка героя —
  статичная краска: замер готовой страницы **61 fps в покое, ни одного покадрового
  repaint**. База знаний: `docs/features/home-page/README.md` (+ `figma-spec.md`, `qa-report.md`).
  ⚠️ **Краска героя (с 26.08.2026) — два боковых МНК-блума, вписанных по кадрам записи**
  (магента rgb(166,71,227) слева, синий rgb(48,85,199) справа; rms 1.87/255), земля
  `#17161c` — панель на ступень светлее фона страницы, как в видео. Тёмный центр —
  ОТСУТСТВИЕ блумов: вертикальная вуаль и плита `Shadow` v1 удалены, не возвращать.
  Ending shape радиала — по-прежнему жёсткая кромка: она невидима только потому, что
  лестница стопов приходит в ноль с почти нулевым наклоном — стоп 93.75% не трогать
  (комментарий в `.home-hero`).
  ⚠️ **Кольца КОНЦЕНТРИЧНЫ на логотипе** (центр 208.25); радиусы с 26.08.2026 — записи
  продакшена [118.6, 248, 401, 536, 704], ровные 8% белого (три бордовых радиуса в записи
  не существуют). Урок чтения Figma стоит: `get_metadata` отдаёт по каждой эллипсе ОДНУ
  верную координату и мусор во второй — доказательство это bbox родительского фрейма
  (`Circles` 28364:40178); размеры изолированного рендера — НЕ проба клипа: обводка
  уходит почти в прозрачность и PNG обрезан по краске.
  ⚠️ **Решётка героя — НЕ константная белая альфа** (с 26.08.2026): это более яркая копия
  цветового поля сквозь ромбовидную маску, шаг 16px — точки пылают над блумами и сами
  гаснут в тёмной долине, без второй маски. Константная альфа ярче всего над почти-чёрным
  — противоположность записи; бордовая 6%-белая решётка на герое снята
  (maintenance-страница держит свой узор — её не трогать заодно).
  ⚠️ **Экспорт градиента из Figma отдаёт стопы БЕЗ позиций ручек** — так плита `Shadow`
  вышла перевёрнутой (тёмный конец сверху) и рисовала жёсткую линию через панель. Урок
  общий для любого чтения градиентов из Figma; сама плита удалена 26.08.2026 вместе со
  всей бордовой краской героя.
  ⚠️ **Логотип: белая пластина ЦЕЛЬНАЯ, из неё вырезана МАЛЕНЬКАЯ искра.** Видно это только
  при рендере РОДИТЕЛЬСКОГО узла (`Logo` 28364:40192); `contentsOnly` у `Logotype`
  28364:40193 теряет boolean и отдаёт плоский белый квадрат — отсюда и взялось ошибочное
  «pixel-perfect match».
  ⚠️ **CSS `border` добавляет там, где обводка Figma сидит ВНУТРИ геометрии** — стоило по
  пикселю композеру и треку вкладок. Рамку рисовать `inset box-shadow`, как у стекла шелла.
  ⚠️ **Активная кнопка Build — СИНЯЯ `--action` #1587FF с белым лейблом** (решение дизайнера
  26.08.2026: «да, когда она активна, она синяя»). Почти-белая `#fafafa` со скрытого фрейма
  28364:40238 — **отменено**, назад не чинить. Этим же снят обход «краска позади движения»
  (правило 4б): у пары «синий ↔ призрак» контраст через кросс 2.20 → 3.95 → 3.53 : 1,
  середина — самый читаемый кадр, тогда как у инвертированной было 19.06 → 1.17 → 2.20.
  ⚠️ **У кнопок в дизайн-системе ровно три размера: 32 (Small) · 40 (Medium) · 48 (Large)**
  (правило дизайн-системы, названное дизайнером 26.08.2026). **36 — нелегальный размер**,
  и это решает старый спор «✕ пикера 36/r10 или 40/r12» в пользу **40/r12** (инсеты 12/12:
  12 + 40 + 12 = 64 держит осевую линию бара, элемент не двигается между списком и деталью).
  ⚠️ Но правило противоречит бордам ещё в девяти местах (аудит 26.08.2026): на 36 сидят
  `Build` 86×36, «+»/микрофон/пилюля «Add template» в композере, чипы фильтров, `Publish` в
  топбаре шелла, `Connect` и два `Buy` в доменах; на 44 — пять кнопок доменной поверхности;
  на 18 — ✕ прикреплённой плитки. Часть нарисована дизайнером именно такой — **молча не
  поднимать**, спрошено, ответа пока нет.
  ⚠️ **Прикреплён ОДИН шаблон; повторный выбор его ЗАМЕНЯЕТ** (дизайнер, 26.08.2026) —
  с плавной анимацией замены. `Build` при прикреплённом шаблоне **активна**: при пустом или
  неинформативном промпте продукт задаёт уточняющие вопросы перед генерацией (intake-шаг,
  нигде не нарисован — но он в продукте есть).
  ⚠️ **Миниатюра в карточке и в полноэкранном превью — ОДНА картинка в двух масштабах,
  а не два состояния.** Замер (26.08.2026): бокс превью 1616×1510 — это аспект карточки
  (233.333/218), растянутый ровно в 6.936×; по всем 19 рисункам ни один прямоугольник не
  отклоняется от чистого масштабирования больше чем на 0.4 % бокса. Разница только в
  КАДРЕ: карточка показывает все 218 px, сцена — верхние 72.8 % и скроллится (борд
  28637:42088 согласен: у AURA зелёная полоса с логотипами срезана кромкой сцены).
  Следствие, которое ломает работу, если его не знать: **композицию нельзя подкрутить
  «только в карточке» или «только в превью»** — любая правка видна в обоих домах. Поэтому
  «карточка подписана, не менять» и «превью читается плохо» — прямой конфликт, и решать
  его надо по борду, а не по одному из двух домов.
  ⚠️ **Высокий контейнер РАЗДАЁТ свободное место**: всё, что расставлено флексом или
  процентами, расползается вместе с ним. Ряд из четырёх маленьких объектов, читаемый на
  233 px как «24.5k+ отзывов», на 1510 px становится четырьмя одинокими кружками у края.
  Группа обязана держать свой бокс (ограничить зазор или сам бокс), иначе она рассыпается
  в мусор. Так же: ни один объект не должен доминировать — у AURA жбан занимал 41 % высоты
  страницы против нарисованных 31.8 %.
  ⚠️ Идея «кэпить дисплейный тип высотным членом» (`min(10.8cqw, 13cqh)`) **не работает при
  аспекте 1.07** — высотный член выигрывает только на боксе шире 1.204:1. Не возвращать.
  **Панель информации в детали шаблона — борд `28637:42088` ПЕРЕДЕЛАН (26.08.2026, заказ
  продукт-оунера).** Бар опустел и сжался 64 → **52**: только ← (32/r8 на 12/10) и ✕; тайтл и
  синяя `Choose a template` из бара УШЛИ (старый вариант лежит в файле скрытым). Сцена
  **1176×1112** (`stageW = sheetW − 448`), панель **432 в обёртке 436**, зазор 8, r12
  концентричен r16 шита, фон `gray-800` непрозрачный (стекла НЕТ). Внутри: тайтл 32, описание
  в две строки с мерой **362** (правый паддинг 32, не артефакт переноса), карточка кнопок
  394×98 r16 (`Use Template` 380×48 синяя + `+ Add Prompt` 380×32), `Preview` 380×48, затем
  информационная карточка — ряд `Category` 59 с волоском NA/100 и два блока «заголовок +
  абзац». Содержимое — `TEMPLATE_DETAILS` в `data/templates.ts`, **по САЙТУ, не по строке**.
  Спека борда (5 линз + 2 критика) — `scratchpad/panel-spec/`, сверка 24 из 29 в ноль.
  ⚠️ **Информационная карточка — `flex-1`**: нарисованные 762 это следствие высоты окна
  (`infoCardH = innerH − 434`), не размер. Не прибивать.
  ⚠️ **Сцена сузилась — и это сдвинуло всё, что мерит полёт.** Рисунок оставлен на аспекте
  карточки плюс `min-h-full`: заполняет сцену без тёмной полосы. Аспект скриншота с борда
  (1898/1918) НЕ взят: он разложил бы тот же рисунок на 7.5 % выше, чем его раскладывает
  карточка, и дал бы хлопок на стыке, где сейчас ноль. Морф садится 0.0000 из обеих дверей.
  ⚠️ **`Add Promt` на борде — опечатка**, шипим `Add Prompt`. И `Use Template` / `Preview`
  выровнены по 380 в одной вертикали: на борде они разъезжаются на пиксель по обоим краям
  из-за рамки карточки кнопок.
  ⚠️ **Что делают `Use Template` и `+ Add Prompt` — НЕ РЕШЕНО дизайнером**, а `Preview` внутри
  превью честно инертна: живого демо у шаблона нет, рисунок — единственный рендер. Не выдумывать.
  ⚠️ **Клик по карточке в ДОКЕ открывает полноэкранное превью** (не запускает генерацию
  сразу), кнопка там — `Remix this template`; в пикере на том же экране остаётся
  `Choose a template`. Синего «+» в доке НЕТ. Ховер нужен на саму карточку и **одинаковый
  в доке и в пикере**. **РЕАЛИЗОВАНО 26.08.2026 (ночь):**
  ⚠️ **Превью — ТРЕТЬЯ ДВЕРЬ в одну поверхность детали, а не второй хост**
  (`ui.pickerSource`: `pill` · `tile` · `card`). Шит, скрим, ✕, фокус-трап, лестница Esc,
  раскатка ✕-пластины и сцена — одни на всех; у двери своё только слово на CTA и колбэк.
  Дверь дока — не новая форма: `tile` уже означал «нет сетки, фейд, полёт на уровне
  страницы, потому что объект снаружи шита». Полёт дока НЕ отдан внутреннему механизму
  пикера: клон внутри фейдящегося шита летит полупрозрачным.
  ⚠️ **Полёт несёт `id` РИСУНКА, а не индекс библиотеки.** Полка дока это `TEMPLATES`, и
  она расходится с `TEMPLATE_LIBRARY` на третьей карточке (`campaign` против `agency`) —
  с индексом из дока вылетала бы не та картинка. Морф садится 0.0000 px из обоих источников.
  ⚠️ **Ховер карточки — CSS, а не React-состояние** (иначе карточки дока ререндерились бы
  на ховер): зум рисунка 1.03 внутри клипа + градиентный обод канона (среднее те же 12 %,
  что у снятого плоского волоска) + матовая пилюля `Preview` внизу-СЛЕВА (справа-сверху
  «взять» — синий «+», только пикер). `--card-hover-dur` работает И длительностью, И тактом
  пилюли (`0s` валиден для обоих) — одна запись делает аффорданс мгновенным для карточки,
  приехавшей под курсор. `@media (hover: hover)` вместо JS-проверки тача. Reduced motion
  выбрасывает САМИ смещения (зум и подъём пилюли) — четвёртый случай той же ловушки.
  Мерить «сдвинулась ли подпись» надо мета-РЯДОМ карточки: рисунок сделан настоящим
  текстом, и первый `p` внутри карточки — параграф внутри картинки.
  ⚠️ **ОДИН СЛОТ: повторный выбор ЗАМЕНЯЕТ, и рост поля 138 → 184 НЕ повторяется** (хуки
  снапа висят на булевом `hasTile`; замерено — одно значение высоты 184 на всём жесте).
  Уходящий рисунок остаётся в слоте призраком и сворачивается за 140 мс к 300-й мс
  («объект визуально дома», `SEAT_ACK_DELAY`), а НЕ к 70 % пружины: остаток 620-мс пружины
  это 2 % осадки. ⚠️ И на пути `Choose a template` свёрт НЕВИДИМ — клон FLIP-а накрывает
  слот на каждом кадре (263/375 сэмплов строго; неподвижная точка лежит внутри плитки);
  призрак сделан ради двери «+» карточки, где клон доходит до слота только к ~200 мс и без
  него слот моргал бы пустым 600 мс. Тот же шаблон, выбранный снова, — не замена: полёта
  нет, плитка отвечает settle-пульсом (просадка 0.94 + вспышка кромки; под reduce остаётся
  только вспышка). Закон — `design-system.md` §5 правило 5а, константы — `attachment.ts`
  § ONE SLOT, замеры — `scratchpad/wp3/`. Заголовок пикера — `Pick a template. We'll remix it.` **С точкой**
  (герой той же страницы «Describe it. Remixer builds it.» тоже с точкой). Правый край ряда чипов — настоящая маска, а не непрозрачная плашка `#283a71`
  с борда (она читается как подсвеченный синий чип).
  **Прикреплённый шаблон — ПЛИТКА в поле, а не чип (борд 28726:64760, 26.08.2026).**
  Превью 56×56 в новой верхней строке поля (инсет 16, r16 с верхним-правым 8), ✕ 18px
  на её углу (48, −8), **пилюля «Add template» остаётся на месте**; поле растёт
  138 → 184 ВНИЗ, ряд чипов уезжает на 46, всё выше стоит. Наш чип-вместо-пилюли
  отменён (история — спека §7.4). Хореография «один объект на три дома» (полёты
  сцена ⇄ плитка слоем над скримом, свёрт в свой ✕, snap-once рост поля):
  `design-system.md` §5 «Хореография вложенного шаблона», код —
  `modules/home/TemplateFlight.tsx` + `attachment.ts`, замеры `scratchpad/qa13/`.
  ⚠️ **Масштаб в FLIP на больших отношениях интерполируется ГЕОМЕТРИЧЕСКИ**
  (`s = s₀^(1−v)`), не линейно: при 29:1 производная масштаба по прогрессу — всё
  отношение, поэтому пружина, перелетевшая прогресс на 0.77 %, приземляла плитку на
  0.785× (сжималась до 44px и отрастала). Геометрически тот же перелёт = 0.974×.
  Перенос при этом выводится из неподвижной точки (`e = q_f·(1−s)`), поэтому морф
  остаётся жёстким при любой рампе.
  ⚠️ **Контент летящего клона верстается по БОЛЬШОМУ концу.** `Thumb` рисует в
  container-units, так что вёрстка на 60px, увеличенная в 27 раз, пропорционально
  та же — но растр нет: это 60-пиксельный битмап на весь экран. Верстать по сцене в
  обе стороны, тогда несовпадение раствора приходится на кадр, где объект 56px.
  ⚠️ **Рост поля не должен красть высоту у спейсеров героя** (они делят слаку
  160:118, +46 подняли бы логотип на 26px): отрицательный `margin-bottom` на ряду
  чипов держит используемую высоту колонки, и 46px приходят из нижней слаки.
  ⚠️ **Слоу-мо для покадровой съёмки ставится только `addInitScript`:** motion 11
  захватывает часы на импорте модуля, поэтому `performance.now`, пропатченный из
  `page.evaluate`, его НЕ замедляет — первый фильм врал («полёт на 95 % готов в
  208 мс»), пока шкалу видели только CSS-анимации.
  ⚠️ **…и даже так замедляется ТОЛЬКО JS-половина анимации.** motion 11 отдаёт
  простые твины `opacity` композитору, и их не тормозит ни `addInitScript`, ни
  патч `Element.prototype.animate`, ни CDP `Animation.setPlaybackRate`: один жест
  начинает отчитываться по двум часам (замерено — scale 0.900→1.0015 растянут на
  180 «мс», а opacity уже 1 в первом же сэмпле). Плюс **скриншот растрируется
  ПОЗЖЕ запроса** (латентность 30–760 мс на софтверном рендере, и `page.screenshot`,
  и CDP `Page.captureScreenshot`): кадр с меткой 0 мс приходит уже осевшим. CDP
  virtual time при этом ВЕШАЕТ страницу (evaluate не возвращается). Рабочий способ:
  съёмка ПО ФАЗАМ — карточка реально наведена, а три значения, которые двигаются,
  вписываются в стили теми числами, что замерены с живой анимации
  (`scratchpad/qa16/05-sequence.mjs` меряет, `08-phases.mjs` снимает).
  **«Add template» + вход страницы реализованы (25.08.2026).** Стеклянная пилюля в
  композере → полноэкранный пикер (борд 28616:59168: инсет 16, скрим 50 %, заголовок
  дизайнера `Pick a template. We'll remix it` — БЕЗ точки; fluid-сетка: поля 32px, шесть
  1fr-колонок, минимум `max(200px, «шесть-в-ряд»)`, ниже — колонки 6→5→4→3) → детальный
  просмотр (борд 28637:42088: FLIP-морф карточка→сцена с вложенным counter-scale,
  прикрепление на `Choose a template`) → продакшен-хореография загрузки, **пересобранная
  26.08.2026 по кадрам записи**: хлопок цвета+решётки ~150 мс (один кадр видео, не фейд) →
  сборка логотипа SVGator 300–1850 мс → контент 1.05–1.3 с → **ВОЛНА** 1.65–4.10 с
  (кольцевой фронт перегорающих точек от логотипа, 0.385 CSS px/мс, тёмная кайма у
  молодых точек, кольца в порядке радиуса позади фронта) → curtain call на 4.45 с
  (`animationend` волны); театр один раз за сессию, осевшая страница статична — 61 fps,
  сеттл против кадра продакшена 4/255. Маска волны (registered `--wave-r`)
  ре-растеризуется ~2.3 с (~17 fps на софтверном рендере) — сознательное
  one-shot-исключение из «ноль покадрового paint», слой демонтируется на curtain call.
  База: README фичи §7 + дополнение §7.4 + `figma-spec-add-template.md`,
  `qa-report-add-template.md`.
  **Шапка пикера сжимается при скролле (борд 28734:65603, 26.08.2026):** 215 → 146,
  заголовок 48 → 32 (он же стал ДВУХТОНОВЫМ и с точкой — борд покоя тоже переделан,
  спека §14.0/§14.1), под фильтрами зажигается волосок 8 % белого.
  ⚠️ **ВЫСОТА ШАПКИ — ФУНКЦИЯ СКРОЛЛА, а не снап с пружиной** (переписано 26.08.2026
  ночью по баг-репорту дизайнера «когда скролишь, список дергается резко»).
  **Спрятать снап от ШАПКИ — не значит удержать КОНТЕНТ:** сжатие отдаёт скроллеру
  69 px вьюпорта, поэтому при неизменном `scrollTop` каждая карточка уезжает на 69 px
  вверх, а 440-мс пружина лишь растягивала прыжок на пять кадров. Замерено: худший
  кадр **16.6 px хода контента при `ΔscrollTop = 0`**, шесть кадров > 3 px.
  Абсорбировать прыжок нельзя — нужно отдать 69 px скролла, а на срабатывании их 36
  (расходуемых 32), потолок компенсации **46 %**. Поэтому подошва =
  `215 − 69·min(S/69, 1)`, и длина рампы ВЫВЕДЕНА: только при R = 69 подошва совпадает
  с верхом первого ряда (`215 − S`) на всём ходу. Шапка стала ОВЕРЛЕЕМ (её бывшая
  высота — `padding-top` скроллера, видимые 146 — `scaleY` плиты), поэтому скроллер не
  меняет размер и сетка двигается только от скролла: **slip 0.00 px на колесе, флинге,
  программном свипе и клавиатуре**. Ушли порог/гистерезис, чтение зажима `scrollTop`,
  сторож короткого списка, обёртка сетки и пружина `HEADER_COMPACT`. Цена: скроллпорт
  на 215 px выше (браузер красит полосу под плитой) — глубокий скролл 43.5 fps против
  48.6, в покое 61.4 у обеих; в полосе перехода наоборот дешевле в разы (7 layout /
  59 recalc против 55 / 185). Плита промоутится НАВСЕГДА (`will-change`) — замер, а не
  привычка: 38.7 → 43.5 fps. Закон и все числа — `design-system.md` §5 «Шапка, которая
  сжимается при скролле».
  ⚠️ Бокс шапки-оверлея ГЛУХОЙ к указателю, а `pointer-events: auto` стоит на самих
  ЧИПАХ, не на их ряду: у ряда 32 px нижнего паддинга, и при сдвиге на −53 он достаёт
  на 16 px НИЖЕ плиты — полоска карточки, которую видно и нельзя нажать.
  ⚠️ **Заголовок, меняющий кегль, — ДВА настоящих текста, а не один масштабируемый:**
  наложенные так, что эффективный размер совпадает на каждом кадре (`48·s` и `32·1.5s`),
  оба резкие на своём конце, в покое без трансформов. У чистого scale-морфа 9.83 %
  размытых пикселей против 3.10 % (dsf 3, БЕЗ заморозки часов — заморозка даёт Chromium
  перерастеризовать и врёт). Вторая копия ВСЕГДА `aria-hidden`, фиксированно.
  ⚠️ **Непрозрачная плита такой шапки — отдельный слой со `scaleY`, а не её фон:**
  щёлкнув сразу в конечную высоту, она оставляет заголовок и чипы снаружи себя, и между
  ними видны карточки.
  ⚠️ **У «снап лейаута один раз» есть НАПРАВЛЕНИЕ** (26.08.2026, баг-репорт дизайнера
  «стрёмно прыгают все элементы интейк формы»): бокс щёлкает первым, только когда он
  РАСТЁТ. На закрытии тот же порядок оставляет отброшенные трансформом строки СНАРУЖИ
  уже сжавшегося бокса — у нас кнопочный ряд ~3 кадра висел на голом герое, и все четыре
  стеклянных контрола разом меняли тон (≈ 3× по светимости), потому что у них живой
  `backdrop-filter`. Закрытие обязано держать бокс временным паддингом и закрывать
  нарисованную кромку `clip-path: inset(… round R)` на той же пружине (замерено: клип
  бесплатен, 38.2 fps против 35.1 без него — платит блюр, а не маска).
  ⚠️ **Инвертированная пара цветов не имеет читаемой середины** — не кроссить её, пока
  что-то движется: у Build (#fafafa+чёрные буквы ⇄ 8 % белого+буквы 24 %) контраст
  «плита ↔ лейбл» падает 19.06 → **1.17** → 2.20 : 1, лейбл исчезает на 3 кадра, и на
  движущемся ряду это читается как «бледная размытая клякса». Таймингами пара не
  спасается — переход ПЕРЕНОСИТСЯ за движение (`disabled` сразу, краска после). Оба
  правила с числами и разделом «как это ловить» (штампы DOM + MutationObserver +
  `getAnimations()`, и «если ничего не нашлось — смотри на подложку, а не на элемент») —
  `design-system.md` §5, правила 4а/4б/4в; замеры `scratchpad/qa15/`.
  ⚠️ **Обводки контролов Home — ГРАДИЕНТНЫЕ строуки, не плоские волоски** (26.08.2026):
  свет сверху-слева, провал до ~4 % в середине диагонали («+»/микрофон 24→4→24 %, пилюля
  20→5→15 %, трек вкладок 32→4→24 %, close пикера 12→4→8 %). Ранние чтения «24 %/32 %
  плоско» — экспорт расплющивает градиентный строук в один токен; канон и рецепт —
  `design-system.md` §5 «Liquid Glass — кнопки и контролы» (классы `.liquid-glass--control/
  --pill/--dim`, `.home-tabs-track` в `index.css`).
  ⚠️ **CJS-интероп Vite ломает UMD-плеер SVGator:** `typeof module` под интеропом реален,
  плеер экспортируется в мусорный неймспейс и логотип МОЛЧА не играет — обёртка обязана
  прибивать браузерную ветку (`src/ui/svgator-player.ts`).
  ⚠️ **Пиксельно-замороженный фон не расщеплять на анимируемые слои** (две попытки —
  дрейф ±1/255 на ~946k px и 219 угловых пикселей): вход гасит непрозрачный
  занавес-РЕБЁНОК над нетронутой краской, и занавес уходит из DOM.
  ⚠️ Reveal больших слоёв малой альфы — composited **clip-path**, не mask-size (15.5 →
  58.5 fps на софтверном рендере); и убитая reduced-motion CSS-анимация шлёт
  `animationcancel`, а НЕ `animationend` — переключению ОС на лету нужен свой слушатель.
  ⚠️ **`backdrop-filter` УМИРАЕТ под маской предка** (промпт-чипы, 26.08.2026): маска —
  это backdrop root, и элемент внутри неё сэмплит ПУСТОТУ, то есть блюр молча ничего не
  делает (замерено: рябь решётки убита на 74 % без маскированного предка и на 0 % под
  ним). `opacity` даже .999 — так же; `overflow`, `clip-path`, `contain: paint`,
  `transform` — безопасны. Изнутри не выкупается: `blur(0)` на маскированном предке не
  отдаёт детям свою копию подложки. Поэтому правый фейд ряда чипов ушёл С предка НА
  чипы (`--chip-fade-a/-b`, `useChipEdgeFade`) — своя маска элемента блюр не ломает.
  ⚠️ И **радиус backdrop-блюра больше ~16px делает ХУЖЕ**: Chrome сворачивает его по
  уменьшенной копии подложки, решётка возвращается (убито 92 % на blur(8), 74 % на 16,
  59 % на 24, 47 % на 32 — на 32 квадратными блоками). Матовое стекло чипов:
  `design-system.md` §5 «Матовое стекло промпт-чипов», замеры `scratchpad/qa10/`.
- **Рисунки шаблонов (`thumbs.tsx`) верстаются в px, а не в `cq`.** Каждый размер —
  `calc(N * var(--cw))` / `calc(N * var(--ch))`, где базис даёт ХОСТ: флюидный хост
  (сцена детального просмотра, клон полёта) ставит `--cw: 1cqw`, а пикер ЗАМОРАЖИВАЕТ
  бокс на нарисованных 233.328125×218 px, отдаёт px-константы и подгоняет рисунок к
  колонке через `transform: scale(calc(100cqw / var(--thumb-base-w)))`.
  ⚠️ Причина, и без неё правило выглядит произвольным: **стиль, содержащий `cq`-единицу,
  Chrome пересчитывает на КАЖДОЕ изменение вьюпорта — даже когда контейнер не двигался**
  (замер: 36 рисунков = 352–413 мс style recalc за 36-шаговый свип; те же рисунки в px = 0).
  Из-за этого перетаскивание края окна с открытым пикером вешало Chrome: 6891–7456 мс
  блокировки главного потока из 8.1–8.5 с (85 %). Сейчас 1021–1312 мс, худшая задача
  294–359 → 138–159 мс, и дом/сцена пиксель-в-пиксель (0 из 1.98 млн).
  ⚠️ Не возвращать `cq` в рисунки и не ставить `content-visibility` на карточку или на
  обёртку — только на бокс с определённым px-размером. Полная спека, все пять фактов
  Chromium и пиксельный отчёт — `design-system.md` §5 «Рисунок верстается ОДИН РАЗ».
  ⚠️ И `backdrop-filter` при `opacity: 0` ВСЁ РАВНО красится: скрытое стекло (пилюля
  `Preview` на 36 карточках) снимается `visibility: hidden`.
  ⚠️ Свап фильтра (23–34 fps) — НЕ проблема главного потока: он ограничен софтверным
  композитором гарнесса (главный поток занят на 27 %, fps линейно по пикселям,
  SwiftShader). На машине с GPU цифра другая — не «оптимизировать» по этому замеру.
- **Бриф перед билдом — `modules/chat/brief.ts`, `BriefPanel.tsx`, развилка в `send.ts`
  (06.09.2026, скопировано 1:1 с записи Lovable).** Пустой проект + слабый промпт
  (`isWeakPrompt`, правило Романа 07.09.2026: **короче 10 символов** — слабый; либо после
  вычитания глаголов, вежливости и слов про сам продукт остаётся меньше двух слов по существу —
  «Build me a website.» слабый, «Bella’s Bakery» строит) → НЕ строим. Ответ «I’d love to build you a website, but I need a little direction
  first…» c «Thought for 3s», статус-строка с шиммером, и над композером стыкуется панель из
  четырёх вопросов (тип сайта · разделы · палитра 2×2 · типографика 2×2, у карточек поле
  «Write your own…»); футер ‹ › + «Skip all» + синий «Next»/«Submit». Плейсхолдер композера
  становится «Tell Remixer what to do instead…», отправка из него снимает панель. Submit →
  карточка-сводка в треде (свободный ввод с префиксом «Other:», пропущенное — «Remixer’s pick»)
  → «Got it — a … with a … palette and … type. Let me build that for you.» → билд. Состояние
  брифа — ось `world.brief` (status/step/answers), сообщения несут `kind` (clarify/brief/ack).
  Вопросы бесплатны, платит только билд. Пресет «New project — thin prompt», флоу «Thin prompt →
  questions → first build».
  ⚠️ Сброс брифа в `world.set`: бриф гасится только когда СТАВИТСЯ ситуация (двигается ось
  `chat` без транскрипта или с пустым). Живая отправка, которая ДОПИСЫВАЕТ в `sent`, бриф
  сохраняет — по нему рендерится сводка ещё долго после ответов. Первая версия правила гасила
  бриф на каждом append, и сводка показывала «Remixer’s pick» во всех строках.
  ⚠️ **ПАНЕЛЬ ВОПРОСОВ — СВОЯ, по Figma 29464:33917 / 34333 / 34334 / 34335, а не копия
  Lovable** (дизайнер прислал ссылку 07.09.2026 в ответ на вопрос про ширину чата, со
  словами «в этом же макете дизайн компонента, где выбираешь варианты ответов, палитру,
  шрифты»). Что борд говорит вопреки копии:
  · **Панель и композер — ОДИН стеклянный объект** (29464:34334): оболочка 7% белого,
    рамка 15%, blur 16, радиус 24 сверху / 28 снизу, внутри вопрос + карточка ответов +
    живой композер с зазором 2. У Lovable панель — отдельная карточка в 8px над композером.
    Оболочка существует только пока открыты вопросы (`.brief-dock` в `ChatPanel`); без них
    композер стоит сам, как рисует его собственный борд 28016:43526.
    ⚠️ Внутри оболочки у поля заливка **8% белого ПО стеклу**, а не своя 80%-чёрная
    пластина: борд рисует композер СВЕТЛЕЕ тела оболочки, и тёмная база превращала его
    в дыру.
  · **Ответы — РАДИО-СПИСОК, не сетка 2×2**: радио 16 (выбранное — заливка `--action`
    с белым пипом 8; невыбранное — рамка 48% белого), тайтл 15 medium, под ним строка
    последствия 14px 64% белого («что будет, если я это выберу»). Карточка ответов —
    `rgba(9,9,11,.56)`, рамка 8% белого, радиус 16, разделители 4% белого; ряды
    pt-18/pb-19, pl-16/pr-24. Футер pt-12/pb-16/pl-6/pr-10: ‹ › 32px (недоступная — 25%
    прозрачности, не перекраска), «Skip all» текстом и синий Next/Submit — h-32, радиус 8,
    13px semibold. Вопрос — 16px semibold, pt-20/pb-18/px-16.
    Именно эта форма позволяет борду задать вопрос, который сетка задать не может;
    нарисованный копирайт это доказывает: «Show 3 Luxury First Class design options to
    choose from before building?» с опциями «Yes, show me 3 directions» / «No, build it
    right away».
  · **Шеврона сворачивания панели НЕТ** — борд его не рисует (у Lovable есть; убран).
  · **Чат сам по себе (превью схлопнуто) — 800, а не 600 Lovable** (`CHAT_CONTENT`):
    в макете фрейм `AI Chat` 800 с 880 подложки по обе стороны. На 600 ряды ответов
    переносились на строку раньше нарисованного. Ширина в СПЛИТЕ — другой борд
    (25819:143144), остаётся 432.
  · **У вопроса про ЦВЕТ своя нарисованная форма** (Figma 25732:139123, борд прислан
    07.09.2026): сетка 2×2 из плиток 40px, в каждой 4 равные ячейки без зазоров, между
    плитками и рядами 8, плитка радиус 8 с рамкой 4% белого; блок pt-16/pb-8/px-16.
    И — это ключ ко всему компоненту — поле «Write your own…» под сеткой идёт
    ВО ВСЮ ШИРИНУ, БЕЗ радио. В радио-списке поле это один вариант из нескольких и
    ему нужна точка; там, где выбор живёт на плитке, радио было бы вторым,
    противоречащим контролом. Поэтому и вопрос без вариантов рисует поле в одиночку.
    ⚠️ Выбранная плитка на борде НЕ нарисована: наше решение — кольцо 2px `--action`
    через `box-shadow` (не border — иначе сетка сдвинулась бы). Единственное, что
    осталось нашим в этом компоненте.
  ⚠️ **ХОВЕР РЯДА — КОЛЬЦО, А НЕ ЗАЛИВКА** (борд 29688:26919, дизайнер 08.09.2026: «ховер это
  скруглёный полупрозрачный бордер как в макете»): наведённый ряд берёт кольцо 1px радиусом 16,
  **белое 32%** — цифра дизайнера («подправь цвет ховера, это белый с непрозрачностью 32%»);
  сплющенный бордом `NA/500` читался как 48% и был громче нужного. Заливки нет вообще — прежние
  5% белого сняты. Кольцо — строук на оверлее внутри бокса ряда (см. следующий пункт), не
  `border`: бордер добавил бы пиксель и толкал бы все ряды на входе и выходе (тот же выбор, что у
  кольца выбранной плитки палитры). Радиус ПОСТОЯННЫЙ — без кольца и заливки скруглённый ряд не
  рисует ничего. Радио ярчает вместе с рядом: `NA/900` = 80% белого против покойных 48%.
  Замер: боксы всех рядов, карточки и поля идентичны с ховером и без — и во время рисования.
  ⚠️ **РЯДЫ СТОЯТ С ЗАЗОРОМ 6px, А ВОЛОСОК ЖИВЁТ В ЗАЗОРЕ** (баг-репорт дизайнера 08.09.2026:
  «когда пункт выбранный и ты делаешь ховер на соседний пункт… выглядит стрёмно, нужно добавить
  дополнительный спейсинг в несколько пикселей»). Вплотную стоящие ряды сводили 2px-кольцо
  выбранного и 1px-кольцо соседнего в две скруглённые линии в пикселе друг от друга — это читается
  как смазанное пятно, а не как два ряда. Ни одно из колец не виновато, им просто негде стоять.
  Волосок при этом НЕ потерян, он ПЕРЕЕХАЛ: был нижним бордером ряда, стал `::after` по центру
  зазора — на глаз в покое та же картинка, что рисует борд, только с воздухом. Гаснет он по обе
  стороны любого кольца, как раньше бордер. И поскольку разделитель больше не сидит ВНУТРИ бокса
  ряда, 19-й пиксель, который борд даёт первому ряду (18 сверху / 19 снизу — вес самого
  разделителя), ушёл вместе с ним: все ряды теперь 18/18.
  ⚠️ **ВЫБРАННЫЙ РЯД — КОЛЬЦО 2px ГРАДИЕНТНОЕ** (борд 29688:27643, дизайнер 08.09.2026:
  «бордер 2px и градиентный»). Экспорт сплющил строук в один токен `Neutral Alpha/800` = 72%
  белого — но это ГРАДИЕНТ, ровно как у контролов домашней (`design-system.md` §5). Кольцо —
  домашняя диагональ: свет сверху-слева, провал в середине, подъём в дальнем углу: `.98 → .55
  (48%) → .82`, среднее по прогону 0.72 — тот самый токен. Строук Figma сидит внутри геометрии,
  так что инсет — это ещё и верные пиксели. Волоски вокруг выбранного ряда гаснут так же, как у
  ховера, и кольцо ховера при выбранном ряде не рисуется — 2px сильнее.
  ⚠️ **ОБА КОЛЬЦА РИСУЮТ СЕБЯ ПО КОНТУРУ, а не проявляются целиком** (борд 29688:29507, дизайнер
  08.09.2026: «цвет бордера не равномерно одновременно по всему бордеру появляется, а градиентно
  по бордеру наполняет объект… плавно и красиво обволакивая бордером пункт. То же самое и на клик,
  более светлый белый цвет в 2 пикселя так же плавно заполняет пункт по кругу»). Средний кадр
  борда — весь бриф: бордер ярче всего там, где свет уже прошёл, и истончается к тому, куда ещё не
  дошёл. Реализация — `DrawRing` в `BriefPanel.tsx` + блок «THE BORDER THAT DRAWS ITSELF» в
  `index.css`: SVG-строук того же скруглённого прямоугольника, у каждого `<rect>`
  `pathLength="1"`, так что длины штрихов — ДОЛИ ПЕРИМЕТРА и одни кейфреймы годятся ряду 400 в
  сплите и 800 в схлопнутом чате. **ОДИН ЛУЧ, ОДИН ОБОРОТ на 360° ПО ЧАСОВОЙ** из верхнего-левого
  угла: по верху → вниз по правому краю → по низу назад → вверх по левому краю до замыкания на
  собственном старте (тело — `dasharray: l 1`, l 0 → 1). Впереди — ДЛИННАЯ РАСТУШЁВКА: 24
  сегмента по .015 периметра (`HEAD_N`/`HEAD_S` в `BriefPanel.tsx`), вместе 0.36 оборота, с
  прозрачностью по кривой `(1 − x)^1.4` от почти тела до нуля — на широком ряду при вспышке в углу
  верхняя кромка уже почти вся мягко светится. Все сегменты — ОДНО правило кейфреймов
  (`brief-draw-head`: `0 l s 1`, l 0 → 1), а место k-го задаёт статичный
  `stroke-dashoffset = −k·s`, сдвигающий его штрих вперёд по пути. Штриховой узор НЕ
  оборачивается через начало замкнутого пути (замерено), поэтому у конца пути голова просто
  сбегает с него — в собственный старт тела — и кольцо замыкается; в последней шестой она ещё и
  гаснет.
  ⚠️ **СТАРТ В УГЛУ РАССЕИВАЕТСЯ В ОБЕ СТОРОНЫ — «выплеск» (`BURST`)** (третья запись дизайнера,
  08.09.2026: «анимация по-прежнему выглядит жёстко и деревянно… видишь внизу слева цвет бордера
  не резко появляется, а плавно из прозрачности, градиентно… эффект рассеивания света по бордеру,
  мягкий, плавный градиентный край»). По кадрам его записи растушёвка спереди РАБОТАЛА (яркость
  падала на ~500 px), а жёсткость была в САМОМ УГЛУ старта: однонаправленный обход начинается
  линией — над углом бордер уже полный, под углом темнота. Борд 29688:29507 в этом углу рисует
  свет, расходящийся в обе стороны. Поэтому за стартом, ПРОТИВ часовой, стоят 12 статичных
  сегментов по .018 (0.22 оборота: левый край и начало нижней кромки на широком ряду) с
  `stroke-opacity` `.85·(1 − x)^1.3` от угла; положение — `stroke-dashoffset = (j + 2)·s`
  (второй экземпляр штриха `s 1`). Выплеск гаснет на 45–85 % оборота (`brief-draw-burst`),
  пока тело обходит этот участок навстречу, — так два слоя не складываются ярче кольца (для 32 %
  ховера сложение двух полных слоёв дало бы 54 %), и в покое от выплеска ничего не остаётся.
  Ховер 350 мс, клик 440 мс (четвёртая запись дизайнера, 08.09.2026: «в 2 раза ускорить»;
  было 700 / 880).
  ⚠️ **В НУЛЕВОМ КАДРЕ НЕ ПОДСВЕЧЕНО НИЧЕГО — СВЕТ ВЫРАСТАЕТ ИЗ УГЛА** (четвёртая запись
  дизайнера, 08.09.2026: «у тебя как будто ховер на ховере: когда я навожу, у объекта мгновенно
  появляется светлый бордер и как будто поверх него идёт анимация ещё одного бордера»). В
  предыдущей версии растушёвка стояла ВПЕРЕДИ тела, которое начиналось в углу, — в первый же
  кадр слабо светились 58 % кольца (весь верх, левый край, часть низа), а потом по ним пробегало
  яркое тело: две яркости = два бордера. Теперь весь прогон — тело и растушёвка вместе — сдвинут
  НАЗАД по пути на длину растушёвки: статичный `stroke-dashoffset` 0.36 у тела и `0.36 − k·s` у
  k-го сегмента головы, а штрих тела идёт 0 → 1.36. В нуле у угла стоит только кончик
  растушёвки; он выходит из угла, тело идёт на длину головы позади, и сам угол доходит до полной
  яркости, лишь когда прогон ушёл на 0.36 дальше. Выплеск тоже не стоит с первого кадра, а
  зажигается от угла наружу (инлайновая задержка 9 мс на сегмент). Групповое проявление
  (`brief-draw-in`) снято — нечего проявлять, если в нуле пусто.
  ⚠️ **Зазор в каждом штриховом узоре — 2, длиннее пути.** Пока первый экземпляр штриха ещё за
  началом пути (отрицательные позиции), второй экземпляр при зазоре 1 приземлялся бы на путь
  (у головы k=0 при L=0 — на позиции .655, в нижнем-правом углу). С зазором 2 второй экземпляр
  всегда за концом пути.
  ⚠️ **Короткая ступенчатая голова (4 сегмента, 6 % периметра) — ОТКЛОНЕНА по второй записи**
  (08.09.2026: «нет градиента, нет растушёванности… полоска без градиента, не так как на
  скриншоте, где края градиентные, плавные»). На записи она читалась как сплошная полоса с
  жёстким концом: 100 px хвоста на скорости 2,5 px/мс — это два-три кадра. Растушёвка должна
  быть длиной в сотни пикселей, как на борде, где верхняя кромка гаснет по всей длине. Ховер 640 мс, клик 800 мс, `cubic-bezier(.45,.35,.55,1)` — РОВНЫЙ обход с
  мягкой посадкой (на 50 % времени — 63 % пути), а не ease-out: с сильным ease-out верхняя кромка
  пролетала за 100 мс, а левая сторона ползла 230.
  ⚠️ **Первая версия — ДВА луча из угла навстречу друг другу, встреча внизу-справа — ОТКЛОНЕНА
  дизайнером по записи экрана** (08.09.2026: «в целом неплохо, но я хотел чтобы анимация была не
  слева направо, а по кругу вокруг объекта обтекла на 360»). На ряду в десять раз шире, чем выше,
  оба луча бегут вправо — по верху и по низу, — и это читается как «слева направо», а не как
  обтекание. Не возвращать.
  **Кольцо покоя — ПОСЛЕДНИЙ КАДР своего рисования** (база: сплошной строук, голова погашена):
  выбранный ряд, смонтированный уже выбранным, показывает те же пиксели без анимации, и
  кроссфейда нет; по `animationend` тела ключ падает в 0 и группа стоит сплошным строуком. Прежний
  блик по клику (`.brief-sheen`) снят: рисование и есть блик. Замер на реальной скорости: 60 fps,
  худший кадр 20 мс, бокс ряда один на всех сэмплах.
  ⚠️ **Ховер ведёт JS-ключ, а не `:hover`.** Chromium при снятии анимации сбрасывает значение на
  базовое СРАЗУ, без перехода (замерено: 0.1972 → 0 в следующем кадре): под `:hover` кольцо,
  нарисованное наполовину, либо исчезало бы за кадр, либо — с «полной» базой — вспыхивало целиком.
  Поэтому `onPointerEnter` бампает ключ группы (свежее рисование от угла) и ставит `data-hov`;
  `onPointerLeave` снимает `data-hov` — оверлей гаснет за 160 мс, ПОКА рисование продолжается под
  фейдом: свет тускнеет, не рвётся. И `:hover` неверен для ряда, смонтированного ПОД неподвижным
  курсором (следующий вопрос под указателем): он загорелся бы сразу, без рисования. Клик —
  `press`-ключ: `onAnimationEnd` тела возвращает его в 0, и та же группа стоит на полном кольце
  (ключ ховера падает в 0 так же, когда его оборот завершён); нажатие по уже выбранному ряду
  рисует его заново — это и есть подтверждение.
  ⚠️ Скрытие — `visibility`, не opacity 0 (урок пилюли `Preview`); рисование — покадровый
  repaint строука, разрешённый на тех же условиях, что `.thinking` и строка работы карточки
  генерации: 37 волосяных строуков на одном ряду (тело + растушёвка + выплеск), полсекунды,
  по жесту пользователя; замер — 60 fps, худший кадр 24 мс.
  Под reduce глобальное правило глушит кейфреймы, а базовые значения и есть кольцо — оно просто
  появляется; ключи под `useReducedMotion` не двигаются.
  ⚠️ **Сетки (палитра и шрифты) остались на СИНЕМ кольце `--action`**, а не на белом
  градиентном: борд рисует выбранное состояние только для радио-списка, а белое кольцо на
  светлой плитке палитры пропадёт совсем. Помечено дизайнеру как открытый вопрос — не выдумывать.
  ⚠️ **СЕТКА ДЕРЖИТ ЧЕТЫРЕ, РАДИО-СПИСОК — ТРИ** (правило дизайнера 07.09.2026: «в этих
  окнах может быть максимум 3 варианта выбора»). Поле «Write your own…» — эскейп-хэтч, а не
  вариант, и в три не считается. Обе сетки держат по четыре, и это не поблажка: палитру 2×2
  рисует его собственный борд (25732:139123), а типографику 2×2 он попросил сделать «как в
  ловбл». Из `goal` (был радио-список из четырёх) ушла «Explain the business» — не отдельный
  исход, а то, что три остальные и так включают. Разбор — `docs/knowledge/design-system.md`
  §«Панель вопросов: не больше трёх вариантов».
  ⚠️ **ВОПРОС О ШРИФТАХ ПОКАЗЫВАЕТ НАЧЕРТАНИЯ** (заказ дизайнера 07.09.2026, «как это в
  ловбл сделано»): сетка 2×2 карточек, в каждой «Title - Space Grotesk» набрано Space
  Grotesk, «Body - DM Sans» — DM Sans; сверху образец, волосок, снизу имя стиля и описание.
  Ради этого в `src/fonts/` зашиты шесть гарнитур (Space Grotesk, DM Sans, Instrument Serif,
  Work Sans, DM Serif Display, Fira Sans) — все **OFL** с npm-пакетов `@fontsource/*`,
  поэтому в публичном репозитории лежать могут, в отличие от Gilroy и Proxima Nova; лицензия
  и копирайты — `src/fonts/OFL.txt`.
  ⚠️ Каждая из шести подрезана до ОДНОЙ строки, которую рисует: 1,5–2,4 КБ вместо 13–24 КБ,
  все шесть вместе 11,4 КБ (в base64 ≈ 15 КБ). **Ничего другого этими семействами набирать
  нельзя** — остальных глифов в файле нет; поменялся текст карточки → пересабсетить (рецепт
  в блоке @font-face в `index.css`). Vite инлайнит их как data-URI сам, минуя сабсеттер
  артефакта, поэтому скрипт по-прежнему пишет «fonts embedded: 4».
  ⚠️ Проверять загрузку надо ДВУМЯ тестами: `document.fonts.check()` И шириной строки против
  фолбэка — зашитый, но битый woff2 первый проходит, второй нет (`check:brief` делает оба).
  ⚠️ **ПАНЕЛЬ ПОЯВЛЯЕТСЯ КАК ПУЗЫРЬ ИЗ ПОЛЯ ВВОДА — ПОРШЕНЬ, а не клип** (заказ дизайнера
  07.09.2026: «пузырь, который обволакивает наше поле ввода… в стиле Apple Liquid Glass», с
  наброском в пять кадров; **переделано 08.09.2026 по его записи экрана**: «анимации
  практически не видно, бордер глючит… переделай и сделай нормально»). Реализация —
  `modules/chat/dock.ts` + блок «THE BUBBLE» в `index.css`; разметка оболочки — три элемента
  в `ChatPanel` (`.dock-shell` › `.dock-piston` + `.dock-base`), контент листа — `.dock-sheet`,
  футер — `.dock-foot`.
  · Оболочка — статичный ПРЯМОУГОЛЬНЫЙ «цилиндр» с `overflow: hidden` вокруг дока (7 px наружу
    по бокам и снизу, 320 px вверх — под перелёт и под морф вниз). Внутри — **поршень** (тело
    оболочки: заливка, боковые рамки и СКРУГЛЁННЫЙ ВЕРХ со своей рамкой, выше дока на
    `--rise`+120) и **воротник** вокруг поля (низ с радиусами 28), нарисованный ПОВЕРХ поршня.
    Поршень едет только `transform: translateY`: стартует верхом на уровне верха поля —
    спрятан за воротником — и выезжает из него с рамкой и углами. Контент едет по той же
    кривой и проявляется (30 → 62 %), когда край уже прошёл поле; футер проявляется на месте
    (42 → 74 %). Всё — transform и opacity, композитно, кадры не зависят от главного потока.
    Воротник красит землю в свои угловые треугольники нулевой тенью со сдвигом вниз
    (`box-shadow: 0 60px 0 var(--gray-950)`) — иначе поршень просвечивал бы за его дугами.
  · **Кривая — один затухающий спринг (ζ .66, ω 12.5, из покоя), сэмплированный в `linear()`**
    (40 точек, 660 мс; за `@supports`, без него — ease-out без перелёта, а не отсутствие
    анимации): 88 % пути к 200 мс, **перелёт 6,3 % (27 px на 430) на ~330 мс**, посадка к 660.
    Поршень ждёт 90 мс, пока воротник проявится (160 мс): пузырь сначала конденсируется на
    поле, потом растёт. Та же кривая у контента и у морфа между вопросами, поэтому они не рвут
    друг друга. Замер на реальной скорости: 430 → −27 → 0, худший кадр 17,8 мс, поле неподвижно.
  · **Заряд на поле** (дизайнер 08.09.2026: «светлый всплеск от центра на всё поле… как заряд
    энергии, который плавно рассеивается… эффект лёгкий, не сильно бросающийся в глаза»): в
    момент появления панели по полю ввода из центра расходится мягкое пятно света (пик 20 %
    белого, круглый диск на ширину поля, scale .08 → 1.12 за 900 мс, гаснет к краям) и едва
    заметно откликается рамка поля (22 %, пик на 45 %). Одно пятно — без кольца-волны и без
    вспышки: волна и вспышка читались как «всплеск», он просил «заряд». Слой `.dock-splash`
    внутри `.composer-field` (`z-index: -1` в её изоляции — над заливкой, под текстом и
    кнопками), только transform/opacity, без блюра; играет на `.dock-rise`, до старта поршня:
    поле зарядилось — и из него вырос пузырь. Худший кадр подъёма с зарядом 18,0 мс.
  · **Отскок и у воротника** (дизайнер 08.09.2026: «bounce effect не виден справа/слева и
    снизу от поля ввода»): в пике перелёта (50 % хода, ~420 мс) воротник вокруг поля
    раздувается — низ на ~6,5 px вниз, бока на ~4,5 px в стороны, бока поршня вместе с ними
    (`.dock-swell` — отдельная обёртка поршня: два transform на одном элементе не складываются),
    откат на 1 px, посадка. `.dock-base` масштабируется от своего верхнего края (шов с
    поршнем стоит), обёртка оболочки расширена до 14 px под запас. Поле по-прежнему
    неподвижно: дышит только краска оболочки вокруг него.
  ⚠️ **Что показала запись (scratchpad/video1, разбор по кадрам).** У клип-версии первый
  нарисованный кадр уже имел клип открытым на 214 из 432 px — половина движения не была видна
  никогда; видимый край был СЫРЫМ СРЕЗОМ плиты, так что рамка 15 % отсутствовала весь подъём и
  вспыхивала в конце («бордер глючит»); перелёт 9 px на 430 не читался. Уроки:
  **(1) кромка должна быть настоящей кромкой** — клип открывает картинку через окно, и рамка
  на окне не едет; **(2) первый кадр движения рисуется ДО движения** — `dock-armed` паркует
  поршень и контент в стартовых значениях статично, браузер рисует этот кадр (слои, растр), и
  только через два rAF ставится класс анимации; **(3) проверять на РЕАЛЬНОЙ скорости**, а не
  только в 10× замедлении — потерянные 150 мс в замедлении это 2 % и не видны. Инструмент —
  сэмплирование `getComputedStyle(...).transform/opacity` по rAF (scratchpad/bubble/film.mjs).
  ⚠️ **ПОЛЕ ВВОДА НЕ ДВИГАЕТСЯ — ни на пиксель, ни в размере** (дизайнер 07.09.2026, отдельным
  сообщением: «очень важно»). Оболочка нарисована СНАРУЖИ бокса поля, а не паддингом. Бокс
  `[376, 783, 800, 101]` идентичен во всех сэмплах подъёма, морфа и ухода; инвариант в
  `check:brief`.
  ⚠️ **Уход — 220 мс CSS против 260 мс удержания в motion, и весь фейд контента — CSS
  (`dock-out`), не motion.** Motion-фейд идёт на композиторе и по окончании отдаёт элемент с
  инлайновой opacity 1 на ОДИН кадр до размонтирования: замерено — кадр t=291 с вопросами в
  полной яркости и без оболочки. Поэтому motion только держит узел (`sheetExit`: opacity
  → .999), классы ухода стоят до размонтирования (cleanup листа), и `dock-sink` не входит в
  список `animationend`, снимающих классы. CSS-часть короче удержания, чтобы поршень был дома
  и оболочка погашена ДО того, как узел снимут.
  ⚠️ **`restart()` без принудительного reflow, если класса на элементе ещё нет.** `.brief-dock`
  уходит в том же коммите, в котором начинается уход; любое чтение стилей между этим и
  добавлением `dock-fall` запускало transition оболочки со старыми параметрами (фейд стартовал
  на 100 мс раньше поршня). Reflow нужен только, чтобы перезапустить одноимённую анимацию
  (морф поверх морфа). Уход стартует из `useIsPresent()` в layout-эффекте — в том же коммите,
  что снимает класс; `onAnimationStart('exit')` у motion приходит на кадр позже.
  ⚠️ **Смена шага (Next / ‹ ›) — морф кромки + направленный свап** (заказ дизайнера
  07.09.2026: «очень классную и плавную анимацию смены шагов»). `useDockSheet(step)`:
  layout-эффект на шаге меряет новую высоту листа (уходящая группа уже вынута из потока
  `popLayout`), `--from` = новая − старая (+ текущий сдвиг поршня, если он в полёте — читается
  из `getComputedStyle` ДО смены класса), класс `dock-morph`: кромка едет к новой высоте тем
  же спрингом с перелётом (замер: −129 → +8 → 0 и 129 → −8 → 0), контент едет с ней. Вопрос и
  ответы — ОДНА группа под `AnimatePresence mode="popLayout"` с `custom={dir}` (`stepSwap` в
  `motion.ts`): вперёд — новая приходит справа (x 36 → 0, спринг .56), старая уходит влево
  (−28, 170 мс); назад — зеркально. **Футер и поле стоят**: кнопки под курсором не двигаются.
  Под reduce — только фейд (`stepSwapFade`). Вопросы одной высоты (goal → pages) морфа не
  дают — только свап, и это правильно.
  ⚠️ **Пиксельная сверка с 29464:34334 (08.09.2026, по `get_design_context`):** у панели
  НЕТ своего горизонтального паддинга — карточка ответов и композер ОДНОЙ ширины (снят
  `px-1.5` листа, карточка была на 12 px уже поля); поле «Write your own…» **42** (40 у борда —
  это state-layer ВНУТРИ рамки 1 px); футер `pb-18` = его 16 + зазор оболочки 2 до композера;
  последний ряд `py-18` без волоска, остальные 18/19 + волосок; плейсхолдер поля — `gray-400`,
  как у композера (токен text/secondary). Осознанно НЕ взято: **ширина оболочки** (борд 788 в
  чате 800, то есть поле 774; у нас поле 800 и оболочка 814 — правило «поле не двигается и не
  меняет размер» важнее) и **цвет `Next`**: борд красит его токеном `background/blue/default
  #0073ec` (= наш `--action-pressed`), а радио в том же компоненте — `#1587ff`; у нас `--action
  #1587ff`, как у всех синих кнопок продукта. Вопрос дизайнеру открыт.
  ⚠️ Не анимировать высоту дока: это перелайаут дока И скроллера треда каждый кадр — ровно
  то, что запрещает контракт (тот же вызов, что `FIELD_GROW` принял для композера домашней).
  ⚠️ **Оба листа (вопросы и план) — в ОДНОМ `AnimatePresence mode="wait"`.** Внахлёст они
  стояли в доке вдвоём на длину выхода, и верх оболочки прыгал на высоту карточки. Теперь
  вопросы складываются в воротник целиком, и план поднимается из него своим `--rise`.
  ⚠️ **Стили дока скоплены на класс `.dock`, а не на `.chat-col`** — колонка треда тоже
  `.chat-col`, и первая версия молча дала бы ей `position: relative; isolation` и
  transition паддинга. Пробник, читавший `querySelector('.chat-col')`, поймал именно тред
  и час показывал «анимации нет».
  ⚠️ **`backdrop-filter` на оболочке панели ЗАПЕЧЁН в плоский `#1a1a1c`** — из-за «глюка»,
  который прислал дизайнер: светлый прямоугольник с мягкими краями внутри панели. Высота
  оболочки меняется вместе с вопросом (536 → 407 → 536), а Chromium при ресайзе
  backdrop-слоя показывает блюр ПРЕДЫДУЩЕГО бокса. Блюрить там всё равно было нечего: за
  оболочкой плоский #09090b (док композера поверх фона шелла), тред — сосед выше, а не под
  ней. Δ ≤ 2/255 на всех ровных участках, после цикла ресайзов — пиксель-в-пиксель.
  Правило: **прежде чем ставить `backdrop-filter`, посмотри, что за элементом.**
  ⚠️ **ТЕКСТЫ И ВАРИАНТЫ — НАШИ** (дизайнер 07.09.2026: «этот текст просто для примера…
  ты должен придумать сам кейс для флоу и тексты и варианты выборов»). Четыре вопроса
  в `brief.ts`: `goal` (для чего сайт) · `pages` (сколько надо рассказать) · `palette`
  (сетка) · `type` (шрифты по ощущению). У КАЖДОГО варианта в слоте под тайтлом —
  ПОСЛЕДСТВИЕ: что появится на странице, если выбрать это.
  Почему не вопросы Lovable: два из их четырёх — ловушка для новичка. «What kind of
  website do you want to build?» и «What sections should it include?» это поля
  свободного ввода, которые возвращают человеку ту же задачу, с которой он только что
  не справился. На записи дизайнер ответил «design portfolio» и «landing» — два слова,
  брифа не больше, чем в самом промпте. Тот, кто написал «website», не может назвать
  ни категорию бизнеса, ни список разделов, ни гарнитуру.
  ⚠️ **ПОРЯДОК ВАРИАНТОВ — ЭТО ДЕФОЛТ ВСЕГО, ЧТО НИЖЕ.** Первый вариант вопроса это то, на
  что падает пропущенный или незаданный вопрос — и в плане, и в карточке генерации. У `pages`
  первым стоит **«A few pages»**, и это правка по замечанию дизайнера 07.09.2026 («почему я
  не вижу остальных страниц как в макеете?»): с «One page» первым самый частый демо-путь —
  сильный промпт прямо с домашней страницы, брифа нет вообще — рисовал карточку, под Home
  которой ничего нет, то есть терял ровно то, ради чего карточка существует. Меняешь порядок
  вариантов — меняешь дефолт продукта.
  ⚠️ `goal` и `pages` СПЕЦИАЛЬНО не спрашивают «что за бизнес»: прототип рисует ОДИН
  захардкоженный демо-сайт, и любой ответ про тип бизнеса будет опровергнут канвасом
  через несколько секунд (у Lovable в демо сводка говорит «design portfolio», а
  строится страница доставки еды). Цель и объём остаются правдой при любом канвасе.
  Копирайт тоже наш: вступление даёт причину в единственной валюте, которая волнует
  клиента, — «a guess costs you a build» (правда: вопросы бесплатны, билд 10 кредитов),
  вместо вежливого «I need a little direction first». Сводка: Goal / Pages / Colours /
  Lettering. «Got it — …» собирается из per-option фрагментов `ack`, а не из тайтлов
  («Show the work» в предложение не вставляется), каждая клауза выбрасывается по
  отдельности — поэтому «Skip all» это настоящий вариант, а не ловушка.
  ⚠️ Нарисованный на борде вопрос про «3 design options» подразумевает шаг с тремя
  визуальными направлениями (у Lovable он есть — три HTML-превью бок о бок). Он НЕ
  реализован, и такого вопроса в брифе нет: обещать три направления и выдать билд —
  хуже, чем не спрашивать. Отдельная задача.
  ⚠️ **ПЛАН ПЕРЕД БИЛДОМ — НАШЕ ОТЛИЧИЕ ОТ LOVABLE** (`modules/chat/plan.ts`,
  `PlanCard.tsx`, `PlanSurface.tsx`; заказ дизайнера 07.09.2026). У Lovable бриф
  кончается на «Got it — …» и билд стартует сам. У нас после Submit ось `brief.status`
  идёт в **`planning`**: ответы компилируются в ДОКУМЕНТ, он встаёт на место панели
  вопросов, и **ничего не генерируется, пока не нажат Approve**. Кнопки: `Review`
  (контурная, слева) и `Approve` (синяя, справа). Строки цены под ними НЕТ — она была
  и снята по просьбе дизайнера («этот текст нужно убрать»): читалась как предупреждение,
  привязанное к кнопке, а футер — это решение (Review или Approve), а не чек.
  · **`Review`** раскрывает превью и открывает `surface: 'plan'` — чат возвращается к
    сплитовым 432, а документ занимает канвас целиком (бар: ✕ · Plan · Approve). Это
    поверхность в смысле `state/ui.ts`, как доменный дашборд: превьюшить всё равно нечего,
    канвас свободен. `closePlanReview` возвращает карточку и убирает канвас.
  · **План СОБИРАЕТСЯ ИЗ ОТВЕТОВ**, а не лежит текстом: меняется `goal` — меняются блоки
    контента, меняется `pages` — меняется строка структуры, пропущен вопрос — план прямо
    пишет «Remixer's pick». Если бы он был статичным, четыре вопроса были бы театром, и
    внимательный человек заметил бы это со второго прогона. Карточка и документ рендерятся
    из ОДНОЙ структуры (`buildPlan`) — карточка не может обещать то, чего нет в документе.
  · Форма взята со скриншотов Lovable Plan mode, которые прислал дизайнер (у них это
    режим композера Build↔Plan, и на первую генерацию он не попадает — а именно там он
    ценнее всего: человек пришёл со словом «website», ответил на четыре вопроса и теперь
    видит, правильно ли его поняли, ДО того как потратит билд).
  ⚠️ **`Skip` из футера Lovable не перенесён.** У них Plan mode необязателен, поэтому
  «skip» значит «не надо плана, просто собери». У нас план — это и есть ответ на четыре
  вопроса; кнопка «собери без того, что я только что попросил» отменяла бы весь флоу.
  Помечено дизайнеру, а не поставлено пустышкой.
  ⚠️ **Тулбара редактирования у документа НЕТ** (у Lovable внизу B / I / Title / список —
  их план редактируемый). Тулбар, кнопки которого ничего не делают, хуже отсутствия
  тулбара: он приглашает ровно к тому взаимодействию, которого прототип не умеет.
  Редактируемый план — отдельная фича.
  ⚠️ **Любая живая запись, которая двигает ось `chat`, ОБЯЗАНА нести с собой `sent`.**
  `world.set` читает патч с `chat` без транскрипта как «ставится ситуация»: чистит `sent`
  И гасит бриф. Написанное как `set({ chat: 'long' })` это стёрло оба — тред откатился к
  демо-транскрипту сценария, а карточка плана вообще не смонтировалась, потому что бриф,
  из которого она рендерится, исчез. Оба новых перехода (`offerPlan`, `approvePlan`)
  передают `sent` и `brief` явно, хотя не меняют их.
  **Вход — домашняя страница (07.09.2026, критерий приёмки Романа).** Композер героя зовёт
  `startBuild` (`send.ts`), тот чистит оси проекта и передаёт промпт в ТОТ ЖЕ `sendMessage`,
  поэтому развилка работает с домашней страницы сама. Проверка — `npm run check:brief`
  (`scripts/check-brief-flow.mjs`, 29 проверок): каждый кейс открывает домашнюю, печатает в
  героя и жмёт Build — «website» уходит в вопросы, «Bella’s Bakery» строит, шаблон из дока
  строит, отправка из композера при открытой панели перебивает вопросы.
  **Переход домашняя → билдер — три фазы, ~3,6 с** (`ui/BootCover.tsx`, фазы и тайминги в
  `state/ui.ts`: `BOOT_DARKEN_MS` 380 · `BOOT_GLOW_MS` 2000 · `BOOT_ARRIVE_MS` 1200; заказ
  дизайнера 08.09.2026: «первый экран плавно затемняется, пока всё не станет чёрным, чат снизу
  вверх выезжает, сообщения сверху вниз, панель справа и кнопки въезжают, логотип в левом
  верхнем углу появляется»). Заменяет прежний коридор 850 мс со статичной маркой.
  · **Darken** — занавес цвета земли билдера (`--gray-950`) проявляется над домашней за 380 мс;
    страница НЕ переключается — домашняя гаснет сама собой, без подмены контента.
  · **Glow** — под непрозрачным занавесом переключается `page`, и по краю ВСЕГО ЭКРАНА на две
    секунды идёт фирменное свечение Remixer — тот же `SiriGlow`, что горит по кромке превью во
    время билда, здесь на самом вьюпорте, с прямыми углами, над чёрным. Вход — его собственный
    (`siri-in`, 600 мс), выдох — последняя четверть фазы (`boot-glow-life` в `index.css`, 2 s =
    `BOOT_GLOW_MS`, держать в паре), чтобы шелл прибывал на чистый чёрный кадр и блюр свечения
    не отнимал кадры у прибытия (урок «свечение стоит почти весь кадровый бюджет»). Губернатор
    качества работает и здесь: лёгкий срез, проба, полный — только если машина держит кадры.
  · **Arrive** — занавес поднимается за 260 мс, и шелл собирается по классам `arrive-*` на
    `[data-boot='arrive']` (блок «THE ARRIVAL» в `index.css`): чат поднимается на 56 px снизу
    (.06 с, 620 мс), рейл справа въезжает на 24 px (.18 с), его кнопки «выпрыгивают»
    (.34/.42 с), ходы треда каскадом сверху вниз по 70 мс (`--i`, с .30 с), марка загорается в
    шапке (.36 с, 240 мс), пилюля шапки (.40 с), слово «Remixer» разворачивается от марки
    (.48 с). Шапка не двигается. Только transform и opacity.
  ⚠️ **АНИМИРОВАННОЙ МАРКИ В ПЕРЕХОДЕ НЕТ — снята дизайнером** (08.09.2026: «нужно убрать этот
  логотип из анимации перехода на страницу, он лишний… вместо него вставить плавно и красиво при
  переходе эффект свечения на пару секунд по краю экрана»). Первая версия перехода (тот же день)
  собирала на чёрном марку SVGator и FLIP-ом уносила её в слот шапки; борд-марка в шапке теперь
  просто проявляется вместе с остальным шеллом. Не возвращать. Урок про единственный экземпляр
  плеера SVGator на страницу остаётся в силе для домашней (`svgator-player.ts`).
  ⚠️ **В хосте перехода у свечения НЕТ угловой вспышки** (`.boot-glow .siri-flash { display:
  none }`). Над контентом превью вспышка — зажигание свечения; над чёрным экраном её четыре
  угловых блума достают до центра и читаются как цветная заливка всего вьюпорта, а не как свет
  по его краю — снято по кадрам первой сборки. Вход свечения — его собственное проявление
  (`siri-in`).
  ⚠️ Замер на софтверном рендере (без GPU): фаза свечения ~9–10 fps в лёгком срезе — свечение
  одно на экране, и это цена именно блюра больших поверхностей; прибытие после него — 47–50 fps.
  Худший кадр — первый кадр Arrive (~130 мс): снятие слоёв свечения + старт всех анимаций
  прибытия на чёрном. На машине с GPU цифры другие; не «оптимизировать» по этому замеру.
  ⚠️ **Занавес поднимается ДО переключения страницы, а не в одном `set` с ним** (как было у
  коридора): домашняя должна погаснуть как она есть. Первый коммит билдера происходит под
  непрозрачным занавесом, кадр несобранного шелла невозможен.
  ⚠️ Под reduced motion фазы схлопываются в короткую крытую подмену (`openBuilder`: 80 → 240 мс,
  без свечения); занавес мгновенно непрозрачен (его базовая opacity 1, глобальное правило
  глушит фейд), шелл просто появляется.
  ⚠️ Каскад сообщений — обёртка `.arrive-msg` вокруг каждого хода в `ChatPanel` с `--i`,
  ограниченным шестью: длинный тред не должен вытолкнуть последние ходы за конец фазы (1200 мс).
  Вне перехода обёртка инертна.
  ⚠️ `check:brief` ждёт `.boot-cover` до `detached` (до 10 с), а не фиксированные 1200 мс —
  переход длиннее прежнего коридора, и проверяет, что в фазе Glow на чёрном есть `.siri-glow` и
  нет ни одного SVG (марки).
  ⚠️ В сводке строка «Lettering» печатает ИМЯ СТИЛЯ («Friendly»), а сами гарнитуры
  названы в строке последствия внутри панели («Outfit + Figtree.», приглушённым). Образцы
  начертаниями живут только В КАРТОЧКАХ вопроса о шрифтах — там шесть OFL-гарнитур реально
  зашиты (см. выше). В сводку и в план они не идут: сводка это перечень решений, а не
  витрина, и набирать одну строку списка чужим шрифтом значит выделить её без причины.
- **ГЕНЕРАЦИЯ САЙТА — минута, разложенная на названные секции** (`modules/chat/build.ts`,
  `BuildProgress.tsx`, ось `world.build`; Figma 29480:48478, заказ дизайнера 07.09.2026).
  Три продуктовых факта, на которых стоит вся карточка:
  · **в реальности генерация идёт 5–10 минут; в прототипе — РОВНО МИНУТА, захардкожено**
    (сумма `ms` секций + `ASSEMBLE_MS`);
  · **генерируется только ПЕРВАЯ страница / лендинг.** Остальные страницы названы и видимо
    ждут — поэтому это план всего сайта, а не прогресс-бар: бар позволил бы поверить, что
    приехал четырёхстраничный сайт, когда приехала одна страница;
  · **превью появляется, КОГДА эта страница готова — и канвас до этого НЕ ОТКРЫВАЕТСЯ.**
    Весь бриф, весь план и всю минуту билда чат занимает шелл целиком, а карточка получает
    свои 800. Это ЗАМЕНЯЕТ прежнее «оставить автораскрытие» (дизайнер 07.09.2026, увидев
    минуту вживую: «нет смысла показывать превью сайта, пока не сгенерируется страница
    первая… автоматически открывать превью нужно тогда, когда будет сгенерирована первая
    страница»). Прежнее решение принималось, когда билд шёл 5.6 с и смотреть было больше
    не на что. Открыть канвас руками стрелкой можно в любой момент — это дефолт, не замок.
  Карточка — это СООБЩЕНИЕ в треде (`kind: 'build'`), а не плавающая панель: после билда она
  остаётся в треде как запись о том, что собрано и какие страницы ещё нет, и уезжает
  скроллом как любой ход.
  ⚠️ **Секции КОМПИЛИРУЮТСЯ из ответов брифа**, как и план (`goal` даёт секции лендинга,
  `pages` — страницы под ним). Иначе план обещал бы одно, а прогресс называл другое — и это
  разоблачило бы план как украшение. Без брифа (сильный промпт, шаблон) каждый вопрос падает
  на первый вариант — то же правило, что у плана для пропущенного вопроса.
  ⚠️ **Имена секций описывают РАБОТУ, а не бизнес.** «Menu · Reservations» с борда — это мок;
  прототип рисует один захардкоженный демо-сайт, и обещание про закусочную канвас опровергнет
  через минуту. «Hero», «Gallery», «Footer» переживут любой канвас. Та же ловушка, которую
  обходят вопросы брифа.
  ⚠️ **СПАЙН СОБИРАЕТСЯ ПО СТРОКАМ, а не одной линией сбоку.** Борд рисует линию
  ФИКСИРОВАННОЙ высоты 204 в своей колонке на x=12 плюс 8px-уголок на строку на x=13 — две
  линии в пикселе друг от друга, то есть способ статичного мока написать «одна непрерывная
  линия». У нас строки меняют высоту (активная выше на строку работы), и фиксированный спайн
  не достаёт или перелетает. Поэтому каждая строка рисует уголок в СВОЙ центр иконки и, если
  не последняя, продолжение вниз через свой `padding-bottom` до начала уголка следующей.
  Тайлится точно, при любой высоте, без замеров: у первой строки уголок ВЕРХНЕ-левый (линия
  здесь начинается и уходит вниз), у остальных нижне-левый (линия приходит и заворачивает).
  ⚠️ И продолжение — ребёнок СТРОКИ, не колонки уголка: зазор между строками это её
  собственный `padding-bottom`, а `self-stretch` доходит только до content-box флекса —
  припаркованное в колонке, оно не дотягивало и спайн выходил пунктиром.
  ⚠️ **Зазоры: 16, но 24 по обе стороны от активной строки.** Так на борде (done→active 24,
  active→очередь 24, внутри очереди 16), и правило обобщается на любую позицию активной.
  ⚠️ **СВЕТЛАЯ ПОВЕРХНОСТЬ ПРИНАДЛЕЖИТ БЛОКУ СТРАНИЦЫ, а не карточке** (правка по замечанию
  дизайнера 07.09.2026 «дизайн этой штуки имеет много отличий от макета»). Спека блока
  `29612:24965`: заливка `Neutral Alpha/50` = `#ffffff0a`, обводка `#272728` со ВСЕХ сторон,
  радиус **20 по всем углам** (не «скругление снизу»), паддинг `px-px pb-px`. Блоки
  ожидающих страниц — обводка b/l/r, радиус 12 снизу, **без заливки**. Первая версия
  поставила заливку на карточку и подсветила заодно ожидающие страницы — то есть стёрла
  единственный сигнал «эту страницу собирают сейчас». Сама карточка — ШТРИХ: её содержимое
  (414×430) закрывает её целиком, заливка не видна нигде; радиус 21 у неё — вывод
  (концентрично к 20 блока), а не замер.
  ⚠️ **РАЗМЕР ФРЕЙМА ИКОНКИ — НЕ РАЗМЕР ГЛИФА.** Figma даёт готовой/текущей секции фрейм 24,
  ожидающей 20; «кружок по фрейму» даёт 20 px чернил, а в рендере борда все три кольца
  ~16–17. Итог: `r=8` штрих 1.5 и `r=7.25`. Метаданные тут не помогут (`get_metadata` на
  такой иконке отдаёт пустой фрейм), а SVG-экспорт недостижим — `figma.com` даёт 403 на
  прокси и для `curl`, и для `download_assets`. Мерить ЧЕРНИЛА по рендеру.
  ⚠️ **В ШАПКЕ БЛОКА СТРАНИЦЫ — ТОЛЬКО ЕЁ ИМЯ.** Строка «Putting the page together» на
  такте сборки была моей выдумкой (борд её не рисует) и снята дизайнером 07.09.2026
  («вот это лишнее»): к этому моменту все секции зелёные и страница уже едет — подпись
  про это дублирует сказанное. Вместе с ней ушли `min-height` у шапки (снова ровные 56)
  и выравнивание иконки по первой строке.
  ⚠️ **СВЕЧЕНИЕ ЗА ГЕНЕРАЦИЮ ВСПЫХИВАЕТ ОДИН РАЗ — на приезде страницы.** Канвас всю
  минуту закрыт, свечению негде идти; минуту несёт карточка, строка за строкой. Так что
  свечение делает ровно то, ради чего заведено и что стоит дёшево: отмечает появление
  превью, один раз, когда канвас открывается на готовой странице.
  ⚠️ Предыстория (пульс по секции), пока канвас на минуту открывался: Правило «glow —
  единственный индикатор загрузки превью» писалось, когда первый билд шёл 5.6 с. Теперь это
  минута, и есть карточка, которая называет секцию. Замер на этой же сборке, той же секундой:
  **60.6 fps без свечения против 9.5–10.3 fps во время вспышки.** Анимации карточки (бегущий
  градиент на строке работы, вращающаяся дуга, рост строк) — и есть предмет заказа, шестьдесят
  секунд по 10 fps убили бы их все. Поэтому свечение говорит то, что умеет: «что-то
  приземлилось» — одна вспышка ~0.9 с на завершение секции и длинная (2.2 с) на такте сборки,
  переходящая в появление сайта. Шесть событий на минуту, ~8 % времени.
  ⚠️ **Градиент строки работы — ЕГО, с борда**: `background-clip: text` по
  `linear-gradient(90deg, NA/300 0%, NA/800 21.6%, NA/300 52.4%, NA/800 99.5%)` — два ярких
  гребня 72 % белого на подложке 24 %. Читать в ТЁМНОЙ теме: экспорт резолвит эти токены в
  почти чёрный, и строка стала бы невидимой. Позиции стопов на борде — это ОДИН КАДР бегущей
  волны, и уложенные в тайл дают жёсткий скачок 72 %→24 % на стыке (мигание раз в период);
  наш период несёт те же две альфы и те же два гребня, но замыкается. Это второе и последнее
  место, где покадровый repaint разрешён (первое — `.thinking`, по той же причине): одна
  строка 14px, всегда одна на экране, живёт только пока пишется секция.
  ⚠️ **Часы генерации — свой таймер, не общий `pending`** (`send.ts`): `pending` это один
  слот, а билд это цепочка из тринадцати тактов. Расписание разворачивается заранее
  (`buildBeats`), поэтому его можно прочитать, проверить и ПРОДОЛЖИТЬ с индекса — перезагрузка
  посреди билда восстанавливает `world.build` из хранилища и не переигрывает минуту заново.
  И каждый такт перечитывает стор: `world.set` читает сдвиг оси `chat` как постановку новой
  ситуации и гасит `build` вместе с брифом, так что консоль сценариев (или новый сайт с
  домашней) может выбить землю из-под работающих часов.
  ⚠️ Пресет «Generating — mid-build» ставит эту сцену ЗАМОРОЖЕННОЙ (за пресетом часов нет),
  чтобы состояния можно было разглядеть, не выжидая минуту. Живая минута — в один клик с
  домашней страницы.
  ⚠️ **В ШАПКЕ ЧАТА ПИЛЮЛЯ ДЕРЖИТ ОДНО ИЛИ ДРУГОЕ, никогда оба** (дизайнер 07.09.2026:
  «кнопок история и скрыть чат тут быть не может»). Канвас открыт — чат это колонка 432
  рядом с ним, и «История версий» и «Свернуть чат» относятся именно к ней. Канвас убран —
  чат и ЕСТЬ шелл: «свернуть чат» сворачивать некуда, а история оказывается в тысяче
  пикселей от треда, к которому относится, прибитая к правому краю окна. Остаётся одна
  стрелка «вернуть канвас» — та самая единственная на весь шелл.
  ⚠️ **КНОПОК ПРАВОГО РЕЙЛА НЕТ, ПОКА НЕТ САЙТА** (решение дизайнера 07.09.2026: «когда идет
  генерация первая и сайта еще нет… этих кнопок нет, они потом красиво с анимацией
  появляются»). Style / Integrations / Analytics / Cloud действуют НА сайт, а через весь
  бриф и всю минуту билда сайта нет — то же рассуждение, что у погашенной `Publish`, на шаг
  дальше: контрол, которому нечего делать, лучше отсутствует, чем мёртв. Аватар сверху и
  freshchat снизу остаются — аккаунт и поддержка существуют до всякого сайта. Появляются
  ступенькой сверху вниз по 70 мс, `AnimatePresence initial={false}` — чтобы открытие УЖЕ
  собранного проекта не переигрывало прибытие (то же правило, что `settled` у чата).
  ⚠️ **СВОДКА БРИФА СОБРАНА ИЗ ТОГО ЖЕ МАТЕРИАЛА** (дизайнер 07.09.2026: «вот в макете
  дизайн для этой формы … и он на всю ширину чата как в макете», с ссылкой на 29480:48478).
  Две карточки — один объект в два момента одного хода: что договорились, и что из этого
  строится. Поэтому у сводки та же анатомия, что даёт борд карточке генерации: волосок-
  оболочка радиус 21, блок `#ffffff0a` радиус 20 с обводкой, шапка 56 с тайтлом 16
  semibold, внутри — тёмный вложенный бокс `--gray-950` радиус 18 со строками 14px.
  ⚠️ И она НЕ `w-fit`: раньше карточка сжималась по содержимому, поэтому самая длинная
  строка задавала её ширину — одна и та же карточка выходила разного размера на каждом
  прогоне, а в схлопнутом чате стояла маленьким боксом в колонке 800.
  ⚠️ **Карточка занимает ВСЮ ширину колонки чата, без max-width** (дизайнер 07.09.2026:
  «когда меню схлопнуто, этот компонент на всю ширину чата»). Нарисованные 416 — это ширина
  колонки сообщений на борде, а не размер карточки: в сплите колонка 416, а со схлопнутым
  канвасом — 800 (`.chat-col`), и все остальные ходы её занимают. Прибитая к 416 карточка
  читалась как узкий объект, плавающий в широком треде, а не как очередной ход в нём.
  ⚠️ Скрипт `npm run check:brief` вырос до **118 проверок** и идёт ~5 минут; почти всё это
  два настоящих билда. Главная из новых проверок — ОТРИЦАТЕЛЬНАЯ: сайт не появляется раньше
  страницы, превью которой он есть.
- **Publish СЕРАЯ и неактивная, пока сайта нет** (баг дизайнера 07.09.2026): активна только
  при `project === 'built'`. На пустом проекте публиковать нечего, во время `generating` —
  ещё нечего; живая синяя кнопка через весь бриф и весь билд приглашает к единственному
  нажатию, которое не может сработать, ровно там, где флоу учит последовательности.
  Погашенная пара — та же, что у `Build` на домашней, когда он не взведён (`--white-100`
  + буквы 24% белого), чтобы «пока нельзя» выглядело одинаково во всём продукте.
- **ПАНЕЛЬ PUBLISH ДЛЯ НЕОПУБЛИКОВАННОГО САЙТА — борд 29697:36970** (заказ дизайнера
  08.09.2026, `modules/publish/PublishPanel.tsx`). Борд ОБНОВЛЯЕТ панель 25819:144061 и
  меняет три вещи:
  · **Ширина 480, а не 548.** Все видимые фреймы борда разложены на 480 (карточка 468,
    поле 436), а 548-е варианты в файле выключены. Это не косметика: копирайт баннера
    ломается в те две строки, что нарисованы, только в колонке 324, которую даёт 480.
  · **Заголовок — СТАТУС, пока сайт не опубликован:** «Not published» вместо «Publish»;
    как только сайт живой, заголовок снова действие. ⚠️ Борд пишет «Not Publisher» — это
    не английский (сайт не publisher, он not published); шипим исправленным, как с
    «Add Promt» у панели шаблона, и помечаем дизайнеру.
  · **Баннер-подсказка 120px** «Ready to put your site live?» + абзац про то, что правки
    не видны посетителям до публикации. Чисто информационный, снимается своим ✕
    (`ui.publishHintOpen`). Внутри карточки он и поля — два ребёнка с зазором 8, баннер
    вставлен на 8 от краёв; ✕ 32/r10 в углу с инсетом 8, заливка `Black/500` + рамка
    `NA/100`, blur 16.
  ⚠️ **ОСЬ `world.published` — «сайт когда-либо публиковался», и это НЕ `unpublished > 0`.**
  Живой сайт с тремя правками в очереди — опубликован (заголовок «Publish», баннера нет);
  сайт, сгенерированный минуту назад, — нет, сколько бы правок ни было. Вывод из
  `unpublished` перекидывал бы заголовок в «Not published» на каждой правке живого сайта.
  `startBuild` ставит `false` (новый сайт), кнопка Publish в панели — `true`. В URL это `v`,
  в консоли сценариев — тумблер «Ever published» в группе проекта.
  ⚠️ Кнопка в топбаре тоже пошла за осью: «Update» и счётчик правок появляются только у
  опубликованного сайта — «обновить» значит «доставить правки тем, у кого уже открыта
  старая версия», а у неопубликованного счётчик отвечает на вопрос, которого нет.
  ⚠️ **Рамки панели, карточки, баннера и поля — `inset box-shadow`, не `border`.** Строук
  Figma сидит ВНУТРИ геометрии и не сжимает детей фрейма, а CSS-бордер сжимает: четыре
  вложенных бордера давали баннер 448 вместо 452 и колонку копирайта 318 вместо 324.
  С внутренними тенями замер сходится в ноль: панель 480, карточка 468, баннер 452×120,
  ✕ инсет 8/8, копирайт 324 в две строки.
  ⚠️ Панель на 16px выше борда (482 против 466) — это метрики подменных шрифтов в
  пунктирной карточке «Connect your own domain» (81 против нарисованных 65), а не ошибка
  раскладки. С Gilroy и Proxima Nova разница уходит.
  ⚠️ Паттерн баннера (искры по цветовому полю справа) — НАШЕ воспроизведение идиомы
  решётки героя (`.pub-hint-dots` в `index.css`): вектор борда 799×770 недостижим, прокси
  режет любой asset-URL figma.com. Совпадение по шагу, цветам и затуханию влево — на глаз
  по рендеру борда.
  ⚠️ **ДЕМО-САЙТ КАСТОМЕРА НЕ ОПУБЛИКОВАН ПО УМОЛЧАНИЮ** (дизайнер 08.09.2026: «у кастомера
  этот сайт первый в списке… пусть он будет по умолчанию неопубликованным»). `DEFAULT_WORLD`
  теперь `published: false`, поэтому дефолтное демо открывает панель ровно в том состоянии,
  что рисует борд. `published: true` осталось у пресетов, где перед сайтом реально стоит домен
  (connecting / live / multiple / unreachable); у staging-пресетов и у «searching» — false.
  ⚠️ **КЛЮЧ localStorage НЕСЁТ ВЕРСИЮ, И СМЕНА ДЕФОЛТА ЗНАЧИТ ЕГО БАМП** (`remixer-prototype/
  world/v2`). Мир целиком пишется в хранилище и читается ПОВЕРХ `DEFAULT_WORLD`, поэтому
  браузер, который уже открывал сборку, держит СВОЁ значение каждой оси — включая ту, чей
  дефолт с тех пор изменился. Именно так и вышло: `published` переключили в false, дизайнер
  перезагрузил артефакт и всё равно увидел опубликованную панель, потому что в его снимке
  лежало `published: true` часом раньше. Меняешь дефолт мира — бампай версию ключа, иначе
  правка не доедет до того, кто уже открывал прототип.
  ⚠️ **НА КНОПКЕ НЕТ «· Free»** (дизайнер 08.09.2026: «убери из кнопки — Free») — и борд
  рисует то же самое: кнопка 86px с одним словом «Publish». Суффикс был НАШ, он озвучивал
  вывод аудита №1 (мы единственные в категории берём кредиты за публикацию, значит публикация
  должна читаться бесплатной). Этот аргумент теперь негде разместить на кнопке: если его стоит
  делать, ему место в копирайте баннера-подсказки, а не пристёгнутым к глаголу. На кнопку не
  возвращать.
  ⚠️ Не взято из борда: `.remixer.app` в поле адреса (у нас verified `*.remixer.site`) —
  помечено дизайнеру.
  ✅ **Открытый вопрос про цвет `Next` закрыт этим бордом:** `get_variable_defs` в тёмной
  теме отдаёт `Background/Blue/Default` = **#1587ff**, то есть наш `--action`. Прежний
  #0073ec из экспорта брифа был светлым фолбэком токена, а не его значением.
- **ПЕРЕКЛЮЧАТЕЛЬ РЕЖИМОВ В КОМПОЗЕРЕ — «Autopilot» / «Build», борд 29697:54553**
  (пилюля 29697:55394, открытое меню 29697:55602; заказ дизайнера 08.09.2026: «точно такой
  же переключатель есть у lovable.dev в чате, у них там Build и Plan»). Компонент —
  `ModeSwitch` в `ChatPanel.tsx`, ось — `world.mode`.
  · **`Autopilot`** — Remixer ВЕДЁТ: почти после каждой задачи возвращается с окном-
    предложением, что делать дальше. Дефолт с первой генерации.
  · **`Build`** — стандартный режим для тех, кто знает, что делать: делает, что просят.
  · Наш `Plan` пока не делаем — он появится позже (документ плана уже есть, но как шаг
    первого билда, а не как режим композера).
  ⚠️ **Пилюля появляется, только когда САЙТ ЕСТЬ** (`project === 'built'`). Через весь бриф
  и весь билд Autopilot нечем вести, а Build нечего менять — то же правило, по которому
  отсутствуют кнопки правого рейла и гаснет Publish: контрол, которому нечего делать, лучше
  отсутствует, чем мёртв.
  ⚠️ **Сами окна-предложения Autopilot ещё НЕ сделаны** — дизайнер заказал «прямо сейчас»
  кнопку и дропдаун. Ось переключается по-настоящему, поведение за ней — следующая задача;
  не выдумывать её вид самим (то же правило, что у `Use Template` / `Preview`).
  Геометрия по борду, сверено замером в ноль: пилюля 95×32 (`Black/700` + стеклянная рамка
  ряда, pl 12 / pr 6, лейбл 13 medium, шеврон 20 со сдвигом 2 вниз), меню 200×113 —
  `Gray/750`, радиус 10, px 2 / py 4, ряды 52 с плитой ховера 40 и зазором 1, тайтл 14
  medium / строка последствия 12 в `NA/600`, галочка `--action` у выбранного. Меню
  открывается ВВЕРХ: правые кромки пилюли и меню совпадают, между ними 8, и меню
  перекрывает верх поля ввода — ровно как рисует борд. Моушен — домашний `popover`
  (`ui/motion.ts`), origin bottom-right.
- **Схлопываемое превью — `ui.previewOpen`, `App.tsx`, `ui/ChatResizer.tsx` (06.09.2026).**
  Новый проект (project empty, транскрипт пуст) открывается со СХЛОПНУТЫМ превью: колонка чата
  занимает весь шелл, а контент чата центрируется — класс `chat-col` (`max-width: var(--chat-content-w)`
  = 600px, у Lovable столько же) стоит и на треде, и на композере, поэтому «чат по центру» — не
  отдельный режим, а обычная колонка на всю ширину. Превью открывается само, когда стартует
  билд (у нас свечение — единственный индикатор, без превью его негде показать; у Lovable дизайнер
  открыл вручную — автораскрытие не подтверждено). Стрелки ⤢/⤡ в шапке чата и канваса.
  ⚠️ **СТРЕЛКА В ШЕЛЛЕ ОДНА, в шапке чата, и только когда превью убрано** — она
  раскрывает. Схлопывание — **драг разделителя** за минимум канваса, кнопки для него нет
  НИГДЕ. Дизайнер сказал это дважды 07.09.2026: сперва «эта стрелка не нужна тут, она видна
  только когда скрыто превью точно так же как у лавбл», потом, увидев мою кнопку в тулбаре
  канваса, «какова черта я вижу тут эту кнопку?». **Это разворот моей же правки того же
  дня** (я прочитал его «когда чат сбоку и видно превью, стрелки схлопнуть превью нет» как
  «стрелки не хватает, добавь») и потом ещё одного полуразворота (я оставил копию у
  канваса). Обратно не чинить ни на одной из сторон. Анимация
  открытия — переход `width` у aside 0,42 с ease-out (`.shell-aside`; единственное осознанное
  исключение из «только transform/opacity» — одноразовый переход, а контейнер-запросы сайта
  переверстывают его на лету, как iframe у Lovable). Драг разделителя за порог
  (канвас < `PREVIEW_MIN` 480) схлопывает превью; в схлопнутом виде у правого края ручка
  `.chat-reopen` — клик открывает на прежней ширине, тяга влево — на нужной. `CHAT_MAX` поднят
  до 1400: широкая колонка теперь просто поле вокруг центрированного контента.
  ⚠️ Во время драга `html[data-drag]` выключает transition у aside, иначе каждый pointermove
  сглаживается в лаг.
  ⚠️ **Схлопнутость — производный дефолт от СИТУАЦИИ, а не одноразовая запись** (07.09.2026).
  Эффект в `App.tsx` гасит превью, когда проект свежий ИЛИ бриф открыт (`brief.status ===
  'asking'`) — второе и держит перезагрузку посреди вопросов: стор `ui` не персистится, его
  дефолт «открыто». А сам момент входа с домашней страницы закрывает превью РАНЬШЕ, в развилке
  `sendMessage` (симметрично тому, как сильный промпт там же открывает) — она успевает до
  `openBuilder`, поэтому шелл открывается уже знающим, а не открывается широким и отбирает
  канвас через кадр. Оба правила — дефолты, не замки: они перезапускаются только на смене
  ситуации, так что открытое вручную превью посреди вопросов остаётся открытым.
- `src/ui/ScrollArea.tsx` — **все скроллящиеся поверхности только через этот компонент.**
  Нативные скроллбары выключены глобально: классический скроллбар забирает колонку в
  лейауте и красит её фоном скроллера — на двухтонном сайте это давало белую полосу
  вдоль всего правого края, включая тёмную половину. Наш индикатор плавает ПОВЕРХ
  контента: 4px, невидим в покое, появляется при скролле и гаснет через 700 мс,
  per-frame только `transform` (высота пишется лишь при смене размеров).
  Тон: `light` (тёмный шелл), `dark` (светлые панели), `auto` — трюк iOS: тёмная
  полоска со светлым волоском, читается и на белом, и на чёрном (для контента сайта,
  фон которого мы не знаем). Имена классов — полными литералами (Tailwind чистит `@layer`).
- Демо-сайт в превью (`SitePreview.tsx`) намеренно сделан двухтонным: белый хиро сверху,
  тёмная секция меню и футер снизу — стенд, на котором сразу видно поведение glow
  на обоих фонах. Это не случайный дизайн сайта, а инструмент проверки.
- Прототип опубликован как артефакт (постоянная ссылка, обновлять при каждом
  значимом изменении): https://claude.ai/code/artifact/3a24a501-7176-4bf4-8e99-cbb56b7ba1a9
  ⚠️ **Публиковать ТОЛЬКО из ветки, в которой есть вся работа.** 07.09.2026 в
  `claude/remixer-brief-flow-integration-na3puj` слиты `claude/remixer-home-page-jtiqae`
  (домашняя страница, шаблоны) и `claude/lovable-generation-flow-study-mhkq0q` (бриф перед
  билдом) — это и есть основа основного артефакта, из неё он и переопубликован
  (29/29 проверок прошли и на dev-сборке, и на одиночном файле, и на том файле, что
  реально отдаёт хост). Ветки `connect-domain-flow-research-z2vuky` (48 коммитов),
  `remixer-connect-domain-awdg3f` (19), `remixer-shopping-cart-p788be` (4) всё ещё растут
  из `ffba495` и НЕ влиты — публикация из любой из них откатит и домашнюю страницу, и бриф.
  Их интеграция — отдельная задача, без просьбы Романа не начинать. `main` — лишь первый
  коммит.
  Отдельный артефакт брифа https://claude.ai/code/artifact/4c95fe14-3530-445d-918c-5b4d8fe425b6
  был временной витриной до слияния — больше не обновляется, смотреть основной.

## Ключевые проверенные факты о продукте (не выдумывать, использовать эти)

- План: **«Remixer Build» $9.99/мес** (годовая оплата $119.88 «Save 33%»; помесячно $9.99
  первый месяц, дальше $14.99). 30 дней триала **без карты**. Триал стартует после первой
  генерации. Публикация на кастомный домен = только на платном плане. `*.remixer.site`
  превью всегда бесплатно и скрыто от Google.
- 1,000 кредитов/мес (+1,000 бонус в первый месяц). Докупка: +1,000 = $14.99, +2,500 = $34.99.
- Цены доменов (verified 06.08.2026, официальная таблица): .com $9.99/$19.99 renew;
  .net $4.99; .org $7.99; .io $34.99/$59.99; .shop $0.99/$34.99; .online $1.99/$29.95;
  .me $2.99/$32.95; .ai $89.99×2 года (минимум 2 года). WHOIS-приватность бесплатно навсегда.
  ⚠️ В хендоффе более старая цифра .io $39.99 — верить ресёрчу (официальная таблица).
- DreamHost **не продаёт премиум-домены** и не имеет брокериджа → никаких «Make an offer».
- DreamHost **не поддерживает Domain Connect ни в какой роли** (проверено). One-click
  подключение внешних доменов возможно только через покупку Entri (~$249/мес, так сделали
  Replit и Lovable) — пока не куплено, внешний домен = ручные nameservers/records.
  ⚠️ Не делать DreamHost Domain Connect-*провайдером* — это ускоритель оттока.
- Бесплатный домен на 1-й год у DreamHost действует **только** для годовых Web Hosting /
  DreamPress — билдер-план по документации не квалифицируется. Не использовать этот хук
  в основном флоу.
- SSL (Let's Encrypt) выпускается ~10–30 мин → не обещать «мгновенно защищено».
- Nameservers: ns1/ns2/ns3.dreamhost.com; смена 4–72 ч; регистрация домена ~15 мин.
- ICANN 60-дневный лок на трансфер; трансфер 5–7 рабочих дней, $9.99 включает +1 год.
- Verified бренд-правило (совпадает с Lovable): **синий #1587FF = действие**,
  индиго/фиолетовый = бренд/план/AI, зелёный = live, янтарный = connecting. Фон билдера #18181B.
  ⚠️ Но аудит §9.10: Inter + индиго-градиент + холодный почти-чёрный = 3 из 4 признаков
  «AI-слопа», и наш синий в 3 пунктах от синего Bolt — тема для будущего пересмотра палитры.

## Выводы аудита, которые надо держать в голове при каждом дизайн-решении

1. **Публикация должна стоить 0 кредитов.** Мы единственные в категории, кто берёт кредиты
   за публикацию (кроме Emergent с рейтингом 2.7/5). Самая атакуемая строка любой сравнительной таблицы.
2. **Владеть 60 секундами вокруг go-live**, а не «продажей домена» (это теперь у всех):
   zero-record connect для доменов уже в аккаунте DreamHost, понятная state-машина со
   словом-действием на каждый сбой.
3. **Словарь глаголов: Add = купить · Connect = подключить своё · Publish/Update = переопубликовать.**
4. **Де-жаргон:** никаких DNS/nameserver/A record/SSL certificate в основных путях.
   «Point domain to us», «secure padlock», канонический чеклист успеха:
   «Domain settings updated · Connected to your site · Security (SSL) on».
5. **Цену показывать до корзины**, renewal-цену не прятать (иначе dark pattern).
6. **Кредиты — постоянно в тулбаре** (GoDaddy вынес на отдельную страницу — их самая
   громкая жалоба).
7. Контраргумент критики: реальный отток — **до** go-live, на качестве генерации
   (мы сами ставим себе 1/5 за anti-generic). Домены/почта — «последняя миля» для тех,
   кто дошёл. Балансировать приоритеты.

## Рабочие соглашения

- Язык общения с пользователем — русский. Код, коммиты, UI-копирайт — английский
  (продукт для рынка США; в i18n прототипа EN default, UK вторым).
- Ветка разработки: та, что назначена сессии (сейчас `claude/remixer-brief-flow-integration-na3puj`).
  Пушить после каждого законченного куска работы.
  ⚠️ **Контейнеру сессии может быть назначена ДРУГАЯ ветка, и на ней не будет этой работы.**
  26.08.2026 сессия открылась на `claude/remixer-home-page-jsrtmo`, отрезанной от
  `claude/remixer-audits-61wwcy` (`ffba495`) — без единой строки Home page и с устаревшим
  `CLAUDE.md`. В свежем клоне настоящей ветки даже не видно в списке. Порядок на входе:
  `git fetch origin --prune`, затем чекаут назначенной ветки (или той, где лежит работа:
  `git checkout -B <ветка> origin/<ветка>`), и только после этого читать `CLAUDE.md` —
  иначе работа уйдёт на ветку без страницы.
- ⚠️ **Сеть облачной сессии решает, можно ли смотреть конкурентов живьём.** Окружение
  уровня «Trusted» (дефолт) пускает только к GitHub и реестрам пакетов: lovable.dev,
  docs.lovable.dev, bolt.new и вообще любой сайт получают 403 от прокси — и curl, и Chromium,
  и WebFetch. Работает только WebSearch (сниппеты) и GitHub. Живые разборы 13.08.2026 были
  возможны, потому что у той сессии сеть была открыта. Перед сессией, где нужно ходить по
  сайтам конкурентов: на claude.ai/code кнопка-облако над полем ввода → шестерёнка окружения →
  Network access → **Full** (или Custom с нужными доменами). Действует только на НОВЫЕ сессии.
  Вход в аккаунт конкурента — отдельный тестовый аккаунт (email+пароль), не основной Google
  дизайнера. Обходить блокировку через сторонние прокси/ридеры нельзя.
  Что сработало 06.09.2026 без сети: дизайнер снял экранную запись (.mov) и загрузил в чат —
  ffmpeg из pip-пакета `imageio-ffmpeg` (в системном ffmpeg Playwright нет демуксера mov)
  режет её на кадры, и разбор получается точнее любых статей. Просить запись, а не пересказ.
- Прототип должен всегда собираться (`npm run build`) — сломанный прототип нельзя показать CEO.
- ⚠️ **Проверять ОПУБЛИКОВАННЫЙ файл, а не только dev-сборку — и не через `textContent`.**
  Артефакт — один файл, в котором весь JS-бандл лежит ВНУТРИ `<body>`, поэтому в тексте
  документа присутствует любая строка, которую приложение вообще способно нарисовать:
  `body.textContent.includes('Editorial')` проходит на строковом литерале из бандла и
  «проверка» не проверяет ничего, честно печатая PASS. Ассерты — по `innerText` (только
  отрисованный текст, script/style исключены). Как прогнать по-настоящему опубликованному
  файлу: `Artifact action: read` сохраняет полный HTML на диск, отдать этот файл статикой и
  запустить проверку с `BASE=http://localhost:PORT/файл.html` — так 07.09.2026 подтверждены
  все 29 проверок на том, что реально отдаёт хост.
- ⚠️ **Пока дизайнер рядом и смотрит — перевыкладывать артефакт после КАЖДОГО принятого
  куска, а не в конце сессии.** 26.08.2026 он весь вечер тестировал вчерашнюю сборку (по
  скриншоту: шесть чипов вместо семи, 18 карточек вместо 36, белая Build) и присылал
  баг-репорты на код, которого уже нет. Ссылка постоянная, `npm run artifact` — секунды.
- ⚠️ **Отчёт агента — не истина, а заявка.** Ложный вывод в отчёте выглядит так же
  уверенно, как верный (пример 26.08: агент доложил, что подписи в сетке пикера не
  рисуются вообще — и код, и его же скриншоты говорили обратное). Всё, на чём стоит
  решение, менеджер проверяет сам: глазами по кадрам и чтением кода.
- Facts-дисциплина: у каждого факта о конкуренте или DreamHost — статус
  (verified / likely / не подтверждено) и дата. Не цитировать «не подтверждено» в материалах
  для руководства (см. synthesis §10.1).
