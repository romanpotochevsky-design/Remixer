# Промпт для новой сессии — Connect Domain флоу

Скопировать целиком в первое сообщение новой сессии. Обновлять в конце каждой сессии:
меняются только разделы «Где мы остановились» и «Открытые вопросы».

---

Продолжаем работу над **Connect Domain флоу в Remixer**.

## Вход в сессию — строго в этом порядке

1. `git fetch origin --prune`
2. `git checkout -B claude/remixer-connect-domain-flow-4fpgqc origin/claude/remixer-connect-domain-flow-4fpgqc`
   — вся работа лежит там.
   ⚠️ Контейнеру сессии может быть назначена ДРУГАЯ ветка, и на ней этой работы нет; зеркало
   `claude/remixer-connect-domain-flow-utiay8` отстаёт — **в него не пушить никогда**.
   ⚠️ **В ЭТОЙ ВЕТКЕ РАБОТАЕТ НЕ ОДНА СЕССИЯ.** 15.09–16.09.2026 две сессии писали в неё по очереди:
   параллельная запушила коммит поверх моего, пока мой контейнер перезапускался, а на
   следующий день — наоборот (пуш отклонён, поставлено поверх ребейзом своего непушенного
   коммита). Ничего не потерялось —
   но отсюда правило: `git fetch origin --prune` **перед каждым пушем**, а не только на
   входе, и прежде чем считать своё дерево актуальным — посмотреть, что лежит на remote
   (`git log --oneline HEAD..origin/<ветка>`). Своё поверх чужого ставить только `--ff-only`
   или мержем, никогда force.
3. Только после чекаута читать `CLAUDE.md` целиком и `docs/knowledge/decisions.md`
   (что уже принято и что уже ОТКЛОНЕНО — смотреть ДО того, как что-то предлагать).
4. Журнал последних сессий: `docs/sessions/2026-09-13b--connect-domain-for-product-owners.md`
   (раунды 1–29; раунды 28–29 — последняя работа, 17.09.2026).
5. Окружение, которое приходится поднимать в каждом новом контейнере (уроки 17.09.2026):
   · `cd prototype && npm install`, затем `pip install fonttools brotli` (сабсеттер шрифтов для артефакта);
   · превью-сервер поднимать **отвязанным от оболочки инструмента** — `setsid nohup npx vite preview
     --port 4173 --strictPort > /tmp/preview.log 2>&1 < /dev/null &` (или как фоновую задачу Bash):
     обычный `&` внутри команды умирает вместе с ней, и следующий пробник получает `ERR_CONNECTION_REFUSED`;
   · цикл ожидания чужого процесса НЕ писать через `pgrep -f '<имя скрипта>'` — паттерн совпадает с
     командной строкой самого цикла, и он ждёт вечно (17.09: цепочка «дождаться прогона → собрать
     артефакт» зависла). Ждать по итоговой строке лога (`until grep -q "checks passed" лог`);
   · `pkill -f` и запуск браузера — в РАЗНЫХ командах (16.09: `pkill` убил собственную оболочку с
     непримёненной правкой).

## Как со мной работать

Я UI/UX дизайнер DreamHost, **не программист**: не проси меня выполнять команды, читать код
или разбираться в git. Говори по-русски, код/коммиты/UI-копирайт — английский. Ты менеджер
этой работы: отчёт агента — заявка, а не истина, всё несущее проверяешь сам в живом браузере.
Если я начинаю просить что-то неоптимальное — скажи «стоп, вот почему», а не делай молча.

## Где мы остановились (17.09.2026)

Закрыто 17.09.2026 (раунды 28–29, по его ответам на вопросы 23 и 24):
- **Сжатие панели Publish — с отскоком** («да нужен»): кромка Reveal — одна пружина .62 / .32 в обе
  стороны; клип не бывает меньше нуля, перелёт несёт отрицательное нижнее поле. 449 → 302 → 309,6.
  Стекло блока по-прежнему уходит 140 мс плоско — закон 4 про уход объекта, а не кромки.
- **Окно Domains по `Connect` закрывается — подтверждено**, и панель Publish входит **как стекло**
  (`panelIn`: .94 → 1 из угла кнопки Publish, спринг .68 / .2, содержимое на 140 мс позже 1.03 → 1,
  рим ловит свет через дочерний `.glass-glint`); окно уходит 140 мс со сжатием к .975.
- Артефакт **версия 85** (965 454 байт), `check:brief` **397/397**, HEAD `fd894ae`.
- ⚠️ Урок прогона: **инлайновый стиль из `paint` перебивает классовые поля** — `margin-bottom: 0px`
  снял `-mb-px` у бокса описания и поднял его штрих на пиксель с штриха карточки (четвёртый двойной
  бордер, на этот раз мой; пойман набором на втором прогоне). В покое инлайн снимается.

Закрыто 16.09.2026:

Закрыто в последней сессии (раунд 13):
- **Слот статуса рядом с `Unlink` решён:** в нём стоит СЛОВО-СТАТУС ДОМЕНА — плашка
  `Recommended` (`ui/Chip.tsx`), стекло, кромка и текст которой окрашены в тон статуса
  (`.liquid-glass--chip[data-tone]`). Слова и тона — из одной таблицы с чипом топбара
  (`modules/domains/status.ts`, `domainRowStatus`): `Setting up` янтарный · `Ready` синий ·
  `Waiting on your email` янтарный · `Live` зелёный · `Not responding` красный · `Showing
  your old site` красный. Панель говорит три вещи тремя поверхностями: заголовок — про сайт,
  ряд — про домен, бар — про правки.
- **По дороге отклонены (не предлагать снова):** пустой слот · `Auto-renew is off — expires
  3 Oct` · `Domain settings changed 12 Sep` · форма `● Live` (точка + слово, как у бара —
  два одинаковых кружка в колонке читались списком). Правило слота переформулировано его
  словами: не «только новость», а «важно настолько, чтобы видеть всегда».
- Из предыдущей сессии (15.09) в силе: привязка домена не публикует сайт (`settle` → `ready`,
  `live` ставит только `Publish to {домен}`, у ремонта свой путь); ряд `Unlink` висит на
  подключении; адрес в поле следует за подключением (`domainIsHome` / `domainSettled`);
  карточка письма — только после подключения; шапка окна доменов сжимается 185 → 88.

- **Тексты процессов носят трёхцветный проблеск** (раунды 14–15): «Thinking», строка работы
  карточки генерации, заголовок карточки подключения в панели Publish — серый/белый в покое,
  **раз в 2,7 с** полоса «оттенок → белое ядро» слева направо **за 1,35 с**, оттенок по кругу
  синий → лиловый → персиковый (борд `30420:19876`; часы переставил дизайнер на живой сборке:
  пауза вдвое короче, полоса на треть медленнее). **Спиннер рядом с текстом носит тот же
  оттенок** (общий родитель `.shimmer-hue`, `ui/shimmer.ts`). ⚠️ Дуга спиннера больше не
  фиксированно-синяя (решение 14.09 отменено этим заказом). ⚠️ Бордовый градиент строки работы
  (29480:48478) заменён проблеском — дизайнер предупреждён, ответа пока нет.

- **Плашка статуса: слева внутри точка 8 в цвет чернил, инсеты 8 · 8 · 8; у `Ready` чернила и
  точка `--action-ink` #51A6FF, стекло на `--action`** (раунд 16).
- **Фон карточки подключения вспыхивает в такт проблеску** — полоса +4 % белого (8 → 12 %,
  борд `30425:27467`; 14 % простояли час — «слишком он яркий») едет `transform` в окне проблеска
  на общей фазе с заголовком и дугой. Спека — `docs/handoff/process-shimmer-spec.md`.

- **Панель Publish двигается, а не прыгает** (раунд 17, по его записи прогона): каждый блок,
  который может прийти и уйти, разворачивается/складывается через `ui/Reveal.tsx` — клип на
  спринге высоты с отскоком дока (.62 с, bounce .32), стекло на такт позже с бликом `.card-arrive`,
  уход 140 мс + кромка .3 без отскока. **Карточка подключения — одна на весь прогон**, стадии
  меняют слова внутри последовательной передачей (`swapText`), тем же — адрес в поле и слово
  плашки. Отступы блоков переехали внутрь клипа (`pad`). **Клик по консоли прототипа больше не
  закрывает панель** — двигай мир из консоли и смотри, как панель отвечает. ⚠️ Это анимация
  `height` — измеренное исключение из контракта с тремя условиями (`design-system.md` §9 п. 9),
  не прецедент. Спека — `docs/handoff/panel-reveal-spec.md`; фильмы — `prototype/scratchpad/reveal/`.

- **Плашка — не кнопка, у неё своё стекло** (раунд 18, по четырём референсам Apple Liquid
  Glass): два блика-штриха на кромке (вход сверху-слева, выход снизу-справа, радиус — половина
  ширины плашки), два слабых следа, кромка 7 %, прозрачное тело со светом сверху и тонкой полосой
  толщины; окрашенная — стекло в тоне, блик = тон наполовину к белому. Кнопки не тронуты.
  Отклонено до показа: вторая кромка внутри (двойной бордер), свечение снаружи, матовое тело.
  Стенд и живые снимки — `prototype/scratchpad/glass-stand/`. **Ждёт его взгляда**: если
  захочет короче блики или ярче тело — на стенде есть S1 (короткие) и S4 (свет снаружи).

- **Ряд с плашкой `Recommended` дышит по борду 30420:25403** (раунд 19, его баг-репорт «каша
  получается»): в колонке текста `gap 8` вместо 2 и строка тайтла 24 (высота плашки) с тайтлом,
  обрезанным по капители — **только у ряда с плашкой**; обычные ряды и сама плашка не тронуты.
  `Row` в `BriefPanel.tsx`; замер и снимок — `glass-stand/row-gap.mjs`, `live/rows-recommended.png`.
- **Насыщенность окрашенной плашки — как в v75, не трогать** (он отменил своё «чуть прозрачнее»);
  **слово плашки — два варианта** (тон по умолчанию / белое через тумблер консоли «Status chip —
  white word») — его выбор ждёт, см. открытый вопрос 16.

- **У слова-статуса в ряду `Unlink` есть тултип** (раунд 20, по его четырём кадрам Dock macOS /
  Figma): стеклянный пузырь с хвостом ОДНОЙ фигурой (клип + SVG-рим по одному пути, домашняя
  диагональ), `rgba(24,24,27,.82)` + blur 16, растёт из кончика хвоста (пик 1.014), уход 120 мс,
  показ через 350 мс ховера; тексты шести статусов — `HINT` в `modules/domains/status.ts`.
  Компонент дизайн-системы `ui/Tooltip.tsx`, спека — `docs/handoff/tooltip-spec.md`. ⚠️ Пузырь
  не покидает свою панель (клэмп к `[role="dialog"]`, хвост на плашке) — моё решение, показано.
  Снимки — `prototype/scratchpad/tooltip/live/`.

- **Propagating по борду 30425:28847** (раунд 21): описание карточки процесса стоит раскрытым
  **5 с**, письмо «One last step» при этом не показывается; затем описание складывается (Reveal
  внутри карточки) и **после** свёртки приходит письмо. Шеврон 32/r10 в ряду сворачивает и
  разворачивает руками; процент такта Gilroy 13 на 48 % (живой — с часов прогона, постановочный —
  27). Письмо — `LastStepCard` в материале карточки процесса, **`Unlink` внутри письма**, ряда
  домена при письме нет. Часы у постановочного мира — от открытия панели. Reveal получил
  `follow="instant"`. Снимки — `prototype/scratchpad/letter/live/`.

- **Нажатие Publish — такт 2,4 с** (раунд 22, его мок): кнопка синяя и выключена, слева дуга 24 белым
  (`step-spin`), слово несёт белый проблеск `sh-sweep` без оттенка (`.busy-ink`, 56 → 100 → 56 %);
  мир — заголовок, бар, домен — меняется в КОНЦЕ такта (`PUBLISHING_MS` 2400, `commitRef`).
  Пробник — `prototype/scratchpad/busy/probe.mjs`.
- **Дуга спиннера — насыщенный близнец оттенка текста** (раунд 23): `#5077fe` / `#a66fff` /
  `#ff8363` (`--sh-arc`, те же часы, пары по семейству), **текст остаётся в пастели борда** — он это
  сказал отдельно, увидев розовую пробу. Дуга карточки подключения и кольцо карточки генерации;
  спиннер кнопки Publish — белый.
- **В заголовке карточки подключения катится только ГЛАГОЛ, имя скользит, дуга не трогается**
  (раунд 23, его выбор — вариант B из стенда `prototype/scratchpad/swap/stand.html`, артефакт
  `44gMXW6CPYqheiuHiyh9Vi`): `RollingTitle` в `PublishPanel.tsx`, `verbRoll` / `HOST_GLIDE` в
  `motion.ts`, «THE HEADLINE THAT ROLLS ITS VERB» в `index.css` (сегменты красят свой проблеск
  арифметикой от строки — `--sh-p`, `--line-w`, `--seg-x`). ⚠️ Спаны глагола держатся на главном
  потоке `onUpdate`-заглушкой: WAAPI-фейд отдаёт элемент с доанимационной opacity на один кадр —
  ровно то мигание, от которого лечили. Плёнка и кадры — `prototype/scratchpad/swap/live/`.
  ⚠️ Прежнее «группа ряда передаётся `swapText`» — заменено; `swapText` остался у абзаца описания,
  адреса в поле и слова плашки (см. открытый вопрос 20).
- **Шов ряда домена (`Unlink`) — его слова записаны как закон** (раунд 24): непрозрачный #313133 =
  4 % белого над 4 %-й заливкой, взятый композитом, чтобы на общей кромке с римом телесной карточки
  не складывалось 4 + 4 → 8 %. Замер: штрих (49,49,51) ровно, рим карточки (47,47,50), заливка
  (39,39,42) — борд на 1:1 так же тих, в Figma он смотрел при зуме. **Ряд теперь 64, не 66:**
  штрих стал inset-тенью (борд 30282:19216: 420 × 64, штрих внутри). Пробник —
  `prototype/scratchpad/seam/probe.mjs`. **Через час — его новое число: верхняя кромка ряда
  #353538, кольцо #313133** (вторая inset-тень `0 1px 0 0` поверх кольца; верх (53,53,56), бока и
  низ (49,49,51)). Вопрос 22 закрыт.
- **`Unlink` в ряду — лейбл по капители** («текст в кнопке анлинк кривой»): тот же спан
  `text-box-trim`, что в письме; кап-бэнд по центру кнопки и иконки, `pl 16 / gap 4 / pr 6`.
  **И опущен на 1 px** (второе замечание: «на несколько пикселей выше чем нужно») — по чернилам
  слово теперь на 34 из 64 девайс-px против иконки 33,5; в письме так же. Если ему всё ещё высоко —
  следующий шаг 2 px, одно число (`translate-y-px` → `translate-y-[2px]`) на двух спанах.
- **Сжатие панели после Publish — С ОТСКОКОМ (17.09.2026, его «да нужен»)**: кромка Reveal — одна
  пружина .62 / .32 в обе стороны; клип не бывает меньше нуля, перелёт несёт отрицательное нижнее поле
  (`ui/Reveal.tsx`, кромка — motion-значение). Трасса 449 → 302,1 → 309,6, провал 7,5 px на 266-й мс,
  в пикселе к ~430 мс (`prototype/scratchpad/publish-shrink/bounce.mjs`). Письмо после свёртки — 360 мс.
- **Блик тултипа смягчён** («слишком агрессивный и белый… раза в 2–3 более прозрачным»): белый
  штрих .22 (был .55), 1,1 с, пик на 45 %, ease-in-out; на пике рим 31 % белого вместо 60 %.
  Пробник — `prototype/scratchpad/tooltip/glint/probe.mjs`.
- **Канвас передаёт по одному** (раунд 27, его запись нажатия `Connect`: окно Domains пропадало в
  один кадр, панель Publish пружинила одновременно): переключатель канваса в `App.tsx` теперь под
  `AnimatePresence mode="wait"` — окно уходит одним объектом (140 мс), сайт возвращается фейдом
  220 мс, панель ждёт (`hold`), пока сайт стоит. Обёртки и панель — на главном потоке. Закрывать окно
  по `Connect` — **подтверждено 17.09.2026** («да сначала анимация закрытия окна Domains, потом плавная и
  стильная анимация открытия Publish в стиле Apple liquid glass»). Окно теперь сжимается к .975 (как
  полноэкранный лист), а **панель входит как стекло** (`panelIn`: .94 → 1 из угла кнопки Publish, спринг
  .68 / .2, содержимое на 140 мс позже 1.03 → 1, рим ловит свет через дочерний `.glass-glint`). Трассы —
  `prototype/scratchpad/connect-exit/` (`trace2.mjs` — с масштабом и бликом).

Состояние инструментов: `npm run build` зелёный, `npm run check:brief` **397/397**,
артефакт переопубликован — **версия 85**, 965 454 байт. ⚠️ Пока идёт `check:brief`, не запускать
`npm run build` / `npm run artifact`: они пересобирают `dist`, который отдаёт превью, и прогон падает
на `ERR_HTTP_RESPONSE_CODE_FAILURE` посреди дистанции (16.09.2026, один прогон потерян). Стенды слота —
`prototype/scratchpad/slot-stand/` (формы) и `…/slot-stand/states/` (состояния).

## Дисциплина проверки — без неё ничего мне не показывать

- `cd prototype && npm run build` — обязателен, сломанный прототип нельзя показать CEO.
- Полная проверка: поднять `npx vite preview --port 4173`, затем
  `CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome npm run check:brief`
  (~7 минут, 397 проверок — два настоящих билда и две пятисекундные паузы на такте `propagating`; локальный playwright без этой переменной падает).
  ⚠️ Замеры панели Publish в наборе ждут посадки её трансформа (`settled()` в `openPublish`): панель
  входит пружиной с хвостом ~680 мс, и `getBoundingClientRect` до посадки читает НАРИСОВАННЫЙ бокс
  (64,01 вместо 64) — любой новый замер панели ставить после `openPublish`, не после голого клика.
- Перед `npm run artifact` — `pip install fonttools brotli`, иначе файл не влезет в лимит.
- Перед публикацией — `Artifact action: read` по ссылке
  https://claude.ai/code/artifact/3a24a501-7176-4bf4-8e99-cbb56b7ba1a9 (посмотреть, что там
  сейчас лежит: артефакт один раз уже откатывался на сборку недельной давности).
  Публиковать тем же `file_path` с `url` — ссылка постоянная. **Никогда `force: true`.**
- `CEILING` в `scripts/build-artifact.mjs` — это ПОЛ проверенных размеров (973 036), только
  повышать при успешной публикации большего файла; понижать нельзя.
- Пока я рядом и смотрю — перевыкладывать артефакт после КАЖДОГО принятого куска, а не в
  конце сессии.

## Открытые вопросы — ЖДУТ МЕНЯ, не решай сам

Нужен файл от меня:
1. **Глиф копирования** (борд `30289:59972`) — четыре захода мимо. Ассеты figma.com режет
   прокси организации, самый крупный доступный вид глифа — 32 настоящих пикселя. Следующий
   заход начинается с SVG от меня либо с выбора варианта из
   `prototype/scratchpad/copy-candidates.png`.
2. **Кольцо радио в чек-аут-шите** — борд отдаёт SVG-ассетом, проверить нечем; толщина и тон
   остаются прежним чтением (1.5px белого).

Нужно одно моё слово:
3. ~~**Что стоит в слоте статуса** в карточке с `Unlink`~~ — **ЗАКРЫТО 16.09.2026:** слово-статус
   домена в стекле цвета статуса (см. «Где мы остановились»). Записано здесь, чтобы не
   предлагать повторно ни пустой слот, ни продление, ни точку с словом.
4. **Два чипа на `ready + icann`**: топбар красит янтарный «Setting up your domain», строка в
   окне доменов — «Connecting», хотя подключение уже объявлено законченным. Пятого тона на
   бордах нет. **С 16.09 на шаг ближе:** словарь слов на состояние уже есть
   (`domainRowStatus` — на `ready + icann` говорит `Waiting on your email`); осталось его слово,
   что чип и окно доменов берут те же слова.
5. **Заголовок панели на такте подключения**: у уже опубликованного сайта те 6 секунд он
   читается `Published` (формально правда — сайт снаружи, на бесплатном адресе). Оставить или
   переключать на `Publish`?
6. **Тайтл шита**: борды 27275 озаглавливают его `Connect domain`, а у нас покупка называется
   `Buy domain` — и в эту дверь ведёт глагол `Buy` с дашборда.
7. **Альфа тени шита** — 20 % на двух бордах и 33 % на третьем у одного компонента; и высоты
   план-карточек 72/80 с разными токенами подписей.
8. **Размеры кнопок**: правило дизайн-системы «32 · 40 · 48» противоречит бордам в девяти
   местах (36 у `Build`, чипов, `Publish` в топбаре, `Connect` и `Buy`; 44 у пяти кнопок
   доменной поверхности; 18 у ✕ плитки).
9. **`Unlink` без подтверждения** — борд рисует одну кнопку без диалога, мы поставили
   подтверждение (скрим 70 %).
10. **Нет ни одного борда состояния «домен занят»** — самое важное состояние доменного поиска
    не нарисовано.
11. На `propagating`/`ready` с неподтверждённой почтой **карточки с `Unlink` нет** (висит на
    `domainSettled`): домен подключён, а отвязать его в этом окне нечем.
12. **Карточка генерации** осталась на прежней анатомии и разошлась со сводкой брифа, у которой
    теперь свой борд.
13. В карточке «идёт подключение» поле показывает **стейджинг**, хотя борд рисует домен: домен
    в этот такт не отвечает, и отдавать ссылку, которая не откроется, нельзя.
14. **Проблеск на плашке статуса?** Янтарные слова `Setting up` / `Waiting on your email` — тоже
    ожидание. Рекомендация — НЕ ставить: текст уже несёт цвет статуса, радужный проблеск поверх
    янтаря спорит с ним. После его правила «везде, где процессы/спиннеры» (раунд 15) — по-прежнему
    не поставлено (у плашки нет спиннера). Ждёт слова. То же про тайтл активной секции карточки
    генерации: её текст процесса — строка работы под тайтлом.
15. **Строка работы карточки генерации** потеряла свой бордовый градиент (29480:48478) в пользу
    проблеска — по прямому смыслу «загрузок всяких». Если она должна была остаться — откат в
    одну строку CSS.
16. **Слово окрашенной плашки статуса — в тоне или белое?** Он попросил два варианта
    («пусть будет 2 варианта цвет цветной или белый»); оба в прототипе, дефолт — тон, тумблер
    консоли «Status chip — white word». Стенд четырёх прочтений — `glass-stand/ink-variants.png`,
    живая пара — `glass-stand/live/ink-ab.png`. Как выберет — второй вариант убрать вместе с осью.
17. **Нативные `title` в шелле — переводить ли на компонент Tooltip?** Топбарный чип домена
    («Setting up your domain» / «Ready to publish» / …), кнопки превью, пилюля кредитов сейчас
    показывают системный тултип браузера рядом с новым стеклянным. Заказ был про слова-статусы
    панели; у топбара своя раскладка (кнопка 280 во флексе шапки) — если да, отдельный проход
    одним компонентом (`title` пропом для цели без своего слова уже есть).
18. ~~`Unlink` в двух цветах~~ — **решено 16.09.2026: серый везде** («ёлка рождественская, а не
    интерфейс»), янтарный ряда снят. Остаётся: `Resend email` — `#3c9bff`, литерал вне токенов
    (`--action` #1587ff, `--action-ink` #51a6ff) — оставить как нарисовано или свести к токену?
19. **Процент такта у постановочного мира — 27 борда**, он не двигается (часов нет). Живой прогон
    тикает 0 → 99 за девять секунд ноги. Оставить фигуру борда или тоже пустить бежать?
20. **Ролл — только в заголовке карточки подключения.** Адрес в поле `Website URL` (стейджинг →
    домен), слово плашки в ряду `Unlink` и абзац описания карточки по-прежнему передаются
    последовательным фейдом `swapText` (120 → 200 мс): там нет спиннера и нет слова, которое стоило
    бы удержать, а у адреса меняется вся строка. Распространять ролл (или иной единый жест) на них?
21. **Такт занятой кнопки Publish — 2,4 с, число моё** («несколько секунд» из заказа). Настоящая
    публикация длится иначе; для демо — оставить или переставить?
24. ~~**Закрывать ли окно Domains по `Connect`**~~ **ЗАКРЫТО 17.09.2026:** «да сначала анимация закрытия
    окна Domains, потом плавная и стильная анимация открытия Publish в стиле Apple liquid glass».
    Сделано: окно уходит 140 мс со сжатием к .975, сайт возвращается, панель входит как стекло
    (`panelIn`, дочерний блик `.glass-glint`).
23. ~~**Отскок на СЖАТИИ панели Publish?**~~ **ЗАКРЫТО 17.09.2026: «да нужен».** Решено разом для всех
    складываний панели: кромка Reveal — одна пружина .62 / .32 в обе стороны (стекло блока по-прежнему
    гаснет 140 мс плоско — закон 4 про уход объекта, а не кромки). Перелёт ниже нуля — отрицательное поле
    клипа. Замеры в `design-system.md` §5 «Сжатие панели после Publish — с отскоком».
22. ~~**Видимость шва ряда домена на 1×.**~~ **ЗАКРЫТО 16.09.2026 (ночь):** его число — верхняя
    кромка ряда **#353538**, остальное кольцо **#313133** («только у верхней части этого блока»).
    Вторая inset-тень поверх кольца; замер — верх (53,53,56), бока и низ (49,49,51).

## Чего не делать

- Не вешать `.card-arrive` на `position: fixed` элемент (панель Publish): класс объявлен после
  утилит Tailwind и ставит `position: relative` — панель упадёт в поток. Для таких хозяев есть
  дочерний `.glass-glint` (тот же свет, те же кадры).
- Не возвращать `REVEAL_CLOSE` к плоским .3 с «по правилу 4»: отскок на сжатии — его слово 17.09.2026
  («да нужен»), правило 4 переформулировано (уход ОБЪЕКТА, не кромки постоянной поверхности).
- Не трогать насыщенность и плотность окрашенной плашки статуса (тело 18 → 9 %, кромка 20 %,
  чистый тон): дизайнер сам отменил своё «чуть-чуть прозрачнее» на лесенке 16.09.2026. И не
  делать плашку кнопочным стеклом обратно — у бейджа своё, освещённое.
- Не возвращать в панель Publish мгновенное появление/исчезновение блоков и «четыре карточки
  подключения по очереди» — дизайнер снял прыжок на видео (16.09.2026); каждый новый блок панели
  идёт через `Reveal` с отступом внутри клипа, а не с `mt-*` на себе. И не расширять анимацию
  `height` за пределы панели без замера: три условия — `design-system.md` §9 п. 9.
- Не возвращать: движок флоу (`flows.ts`, `FlowPlayer`), скелетон загрузки превью, блик по
  кромке панели Publish, иридесцентное свечение резайзера, описания под доменами, «· Free» на
  кнопке Publish, «Keep editing» и ссылку назад в окно доменов в панели, вторую ссылку в окне,
  стрелку схлопывания превью в тулбаре канваса.
- Цвета Figma читать ТОЛЬКО `get_variable_defs` в ТЁМНОЙ теме: референс-код резолвит светлую
  (`Background/Blue/Default` печатается `#0073ec`, а в тёмной это `#1587ff` = наш `--action`).
- Штрих Figma сидит ВНУТРИ геометрии: рамки — `inset box-shadow`, а где внутри есть дуги —
  «вытянуть ребёнка» (`-mx-px`). Двойной бордер по этой причине ловили уже трижды.
- Размер фрейма иконки — не размер глифа: мерить чернила по рендеру (ловились трижды).
- Перформанс-контракт: покадрово только `transform` и `opacity`; покадровый repaint разрешён
  ровно в трёх местах (`.thinking`, строка работы карточки генерации, рисующееся кольцо).
