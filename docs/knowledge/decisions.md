# Журнал решений

> **Назначение:** что уже решено, почему, и что уже пробовали и отклонили. Половина
> ценности этого файла — строки со статусом «отклонено»: они не дают предложить второй раз
> то, на что дизайнер уже сказал нет.
> **Обновлено:** 26.08.2026 (вечер — ответы дизайнера по списку открытых вопросов) · **Порядок:** новые сверху.

**Статусы:** `принято` · `отклонено` · `заменено` (было принято, потом переиграно) ·
`открыто` (сделано так, но вопрос дизайнеру висит).

**Как добавлять:** одна строка — одно решение. Дата · формулировка · причина · статус ·
ссылка на глубину (файл `docs/…` или файл кода). Ничего не удалять: переигранное решение
получает статус `заменено` и ссылку на то, что его сменило.

---

## 2026-08 · Прототип, шелл и домены

| Дата | Решение | Почему | Статус | Глубина |
|---|---|---|---|---|
| 17.08.2026 | Шапка поиска доменов монтируется **один раз**, при поиске меняются только списки под ней (`listSwap`) | Замечание дизайнера: перемонтирование шапки теряло фокус и каретку, читалось как перезагрузка страницы, а не как «пришёл ответ» | принято | `features/domains/README.md` |
| 17.08.2026 | Заголовок доменного экрана **морфится** (40px → 32px, белый сабмит → тёмно-стеклянный), а не подменяется двумя компонентами | Постоянная шапка превратила расхождение двух макетов в переход: герой оседает в выдачу, как домашняя страница поисковика | открыто (если дизайнер захочет неподвижную шапку — заморозить `compact`) | `features/domains/README.md` |
| 17.08.2026 | Флеш отправки «налить и слить»: 0.7 с клок, поза ~30 мс, смерть оранжевым в нижнем-правом углу | **ПРИНЯТО дизайнером** («отлично, идеально»). Ужималось трижды с 200 мс/1.15 с по его просьбе «быстрее и чтобы не било по глазам» | принято — не трогать без просьбы | `CLAUDE.md`, `prototype/src/index.css` (секция «The send flash») |
| 17.08.2026 | Свечение резайзера — **однотонно-синее** | Иридесцентную версию в спектре Siri собрали «для консистентности» и показали — отклонена: «выглядит стремно, верни как было». Спектр принадлежит AI-моментам, резайзер — утилитарный инструмент | отклонено (иридесцентная версия) — не предлагать повторно | `CLAUDE.md` |
| 17.08.2026 | Бегущий блик по кромке панели Publish (`.glass-sheen`) убран | Вопрос дизайнера «зачем этот эффект блика?». Блик — признак СТЕКЛА; на непрозрачной панели он украшает материал, которого у неё нет | отклонено / откатано | `knowledge/design-system.md` §5 |
| 17.08.2026 | Id сообщений чата продолжаются от максимума в транскрипте, а не от константы | Транскрипт живёт в localStorage с id 1001+, а счётчик умирал со страницей — «новые» сообщения переиспользовали занятые id, и анимации молча не играли | принято (баг-фикс с последствиями для архитектуры) | `CLAUDE.md`, `features/builder-shell/README.md` |
| 17.08.2026 | Настоящие шрифты не коммитим; рендерим OFL-подмены (Figtree ← Proxima Nova, Outfit ← Gilroy) | Репозиторий публичный: коммит лицензионных файлов был бы распространением. Подмены дают одинаковый лейаут на любой машине вместо трёх разных системных | принято (до перевода репозитория в приватный) | `knowledge/design-system.md` §4 |
| 16.08.2026 | Стена плана перед всей доменной поверхностью **убрана**; гейт живёт только в `DomainModal` | Все три борда рисуют триального юзера в ПОЛНОМ дашборде со шитом поверх. Плюс правило аудита «цену до корзины» | принято | `features/domains/README.md` |
| 16.08.2026 | Чек-аут — **один** компонент на шесть состояний (две оси: `connect-existing` / `buy` × «есть план» / «нет плана») | Три борда рисуют один и тот же шит в разных состояниях, а не три разных шита | принято | `features/domains/README.md` |
| 16.08.2026 | В результатах поиска сначала **классическая выдача**, AI-имена ниже | Человек, который ввёл имя, просит именно это имя. Закопать точное совпадение под AI-выдачу — категориальная ошибка | принято | `features/domains/README.md` |
| 16.08.2026 | Цены только из `TLD_PRICES` (официальная таблица), не из макета | В макетах чек-аута продление $11.86 — плейсхолдер, такого числа нет ни в одной строке проверенной таблицы | принято | `knowledge/product-facts.md` §3, §8.5 |
| 16.08.2026 | Глагол на новом домене — **«Buy»**, как нарисовано в макете | Словарь аудита говорит «Add = купить». Реализовано по макету, расхождение поднято дизайнеру | открыто | `knowledge/glossary.md` |
| 16.08.2026 | Узкий чек-аут-шит 600px (а не 560), когда несёт цену | В системном фолбэке (без Gilroy/Proxima Nova) строка на 560 обрезается; 600 нарисован на борде 27058 — это в пределах нарисованного | принято | `features/domains/README.md` |
| 16.08.2026 | Карточки планов оставлены разной высоты (72 выбранная / 80 нет) | Сделано как нарисовано; помечено дизайнеру как вероятная случайность макета, а не «поправлено» молча | открыто | `features/domains/README.md` |
| 16.08.2026 | Свечение превью стартует **через 700 мс** после отправки, снимается за 220 мс | Замер: со свечением страница рисует 4 fps, без него 60. Без задержки glow съедал кадры у пружины пузыря и у печати ответа — обе анимации читались как «их нет» | принято | `handoff/siri-glow-spec.md` |
| 16.08.2026 | Губернатор качества glow меряет **во время** работы свечения и стартует с лёгкого среза | Старая версия мерила спокойную страницу ДО включения свечения, всегда решала «машина быстрая» — защита не срабатывала никогда | принято | `handoff/siri-glow-spec.md` |
| 16.08.2026 | Отправка **уводит сообщение наверх** вьюпорта (как у Lovable), чат не липнет к низу | Липнущий к низу чат выталкивает твоё же сообщение с экрана, пока пишется ответ | принято | `features/builder-shell/README.md` |
| 16.08.2026 | Пружина отправки длинная (`bounce .45 / duration .9`) | Короткая версия (.5 с, scale .6) РЕНДЕРИЛАСЬ — проверено покадрово, — но кончалась раньше, чем глаз замечал, и читалась как «анимации нет». Это единственный жест, который делает пользователь: читаемость важнее сдержанности | заменено (короткая → длинная) | `prototype/src/ui/motion.ts` |
| 16.08.2026 | Композер: **тёмное** поле + **стеклянные** кружки «+»/микрофон, контурная кнопка отправки в покое | По Figma 28016:43526/43528/43545, прочитанной в ТЁМНОЙ теме — в светлой экспорт отдаёт значения, при которых поле сливается с фоном | принято | `knowledge/design-system.md` §5 |
| 16.08.2026 | Панель Publish — сплошная `gray-850`, без стекла | Полупрозрачное стекло на ней было ошибкой. С iOS 26 берём только анимации, не материал | заменено (стекло → сплошная) | `knowledge/design-system.md` §5 |
| 16.08.2026 | Переключатель девайсов — **одна** кнопка; мобильный режим — фиксированная рамка 390×844, центрированная по обеим осям | Проверено по видеозаписи билдера Lovable: иконка = текущий режим, клик переключает | принято | `features/builder-shell/README.md` |
| 16.08.2026 | Фон кадра `#09090B` | Figma 28016:42812 красит весь кадр. Без явного фона `body` прозрачен, и хост (панель превью claude.ai) просвечивает | принято | `knowledge/product-facts.md` §8.6 |
| 16.08.2026 | Геометрия шелла — редизайн 2026: чат 432 СЛЕВА, топбары 52, рейл 56 СПРАВА | Снято с Figma «Website Builder», узел 25819:143144 (16.08.2026) | принято; старая геометрия 57/64/522 — `заменено`, не откатывать | `knowledge/design-system.md` §3 |
| ≤16.08.2026 | **Glow — единственный индикатор загрузки превью.** Скелетон, затемнение и перемонтирование сайта убраны | Как на реальном устройстве: эффект играет поверх любого контента, страница остаётся видимой | принято; скелетон — `отклонено`, не возвращать | `handoff/siri-glow-spec.md` |
| ≤16.08.2026 | Все скроллящиеся поверхности — через `ScrollArea`, нативные скроллбары выключены глобально | Классический скроллбар забирает колонку лейаута и красит её своим фоном: на двухтонном сайте — белая полоса по всему правому краю | принято | `knowledge/design-system.md` §8 |
| ≤16.08.2026 | Демо-сайт в превью намеренно двухтонный (белый хиро / тёмный низ) | Это стенд для проверки glow на обоих фонах, а не дизайн сайта | принято | `features/builder-shell/README.md` |
| ≤16.08.2026 | Стекло — по **macOS**, не по iOS: мягкая амплитуда косой градиентной рамки, без внутренней подсветки, тени и `saturate` | «Айосность» давала амплитуда 32 % белого + подсветка сверху. База — Figma 25819:143747 | заменено (iOS-версия → macOS) | `knowledge/design-system.md` §5 |
| ≤16.08.2026 | Флеш отправки — свет по кромке коробки, без маски | Маска применяется ПОСЛЕ фильтра и срезает растушёвку блюра в жёсткие края («толстый градиентный контур»). Жёсткая обводка 2px читается как неон, размытая клякса за полем заливает само поле | заменено (комета «летающая колбаска» → «налить и слить») | `CLAUDE.md` |
| ≤16.08.2026 | Единый источник правды — `world.ts`; новое измерение состояния добавляется сначала туда | Иначе через год будет двадцать расходящихся версий ответа на «есть ли у юзера план» | принято | `prototype/src/state/world.ts` |

## 2026-08-26 (вечер) · Home page — дизайнер прошёл список открытых вопросов

Двенадцать вопросов, накопленных к концу дневной сессии, были заданы дизайнеру одним
списком (журнал сессии 25–26.08, раздел «Открытые вопросы»). Ниже — его ответы, дословный
смысл и что из них следует для кода. Четыре вопроса он не понял с первого раза: они
переформулированы простыми словами и переспрошены.

| Дата | Решение | Почему | Статус | Глубина |
|---|---|---|---|---|
| 26.08.2026 | **Дубль чипа `Ecommerce` на борде — опечатка. Седьмой чип: `Tech & SaaS`** (третьим в ряду, в слот дубля) | Дизайнер: «не нужен дубль, придумай другой топик» — выбор топика делегирован. Под `Tech & SaaS` уходят карточки, чьи СКРИНШОТЫ и есть продуктовые лендинги: PayNexus (финтех), WorkPro (SaaS), MineMax (крипта). Требование к реализации: после правки каждый чип отдаёт ≥1 карточку в ОБОИХ списках — и в 6-карточной полке дока, и в сетке пикера; пустая выдача под чипом — ненарисованное состояние и в демо читается как поломка | принято | `prototype/src/data/templates.ts` |
| 26.08.2026 | **В дизайн-системе у кнопок ровно три размера: 32 (Small) · 40 (Medium) · 48 (Large).** 36 — нелегальный размер | Ответ дизайнера на вопрос «✕ пикера 36/r10 или 40/r12»: он не выбрал борд, а назвал правило. Из правила однозначно следует 40 — и это снимает разъезд ✕ на 6 px между списком и детальным просмотром (это ОДИН физический элемент на два вида) | принято — ✕ = **40 × 40, радиус 12** | `features/home-page/figma-spec-add-template.md` §12.6 |
| 26.08.2026 | **Кнопка `Build` синяя (`--action` `#1587FF`), когда активна**; неактивная остаётся призраком (8 % белого + лейбл 24 %) | Дизайнер: «да, когда она активна, она синяя». Это **разворот решения 25.08** («почти белая `#fafafa` по скрытому фрейму `28364:40238`») и заодно снимает вопрос о читаемости лейбла посреди перехода: у пары «синий ↔ призрак» есть читаемая середина, у инвертированной («#fafafa + чёрные буквы» ↔ призрак) её нет — контраст падал 19.06 → **1.17** → 2.20 : 1 | принято | `knowledge/design-system.md` §5, правило 4б |
| 26.08.2026 | **`Build` остаётся АКТИВНОЙ, когда прикреплён шаблон** (борд рисует disabled — след дублированного фрейма) | Дизайнер: «да, может быть активной, если текст промпта пустой или неинформативный — Ремиксер перед генерацией задаст дополнительные вопросы, чтобы собрать ТЗ» | принято | `features/home-page/figma-spec-add-template.md` §13.5 |
| 26.08.2026 | **ПРОДУКТОВОЕ: перед генерацией Ремиксер задаёт уточняющие вопросы, если промпт пустой или неинформативный** (intake-шаг между «Build» и генерацией) | Сказано дизайнером как данность продукта при ответе на вопрос про `Build`. Ни на одном борде этот шаг не нарисован, и в прототипе его нет — это отдельная ненарисованная фича, которую надо держать в виду при любом решении о композере | открыто — источник: дизайнер, 26.08.2026; борды отсутствуют | журнал сессии 26.08 |
| 26.08.2026 | **Прикрепить можно ОДИН шаблон; повторный выбор ЗАМЕНЯЕТ прикреплённый** — с плавной анимацией замены | Дизайнер: «один шаблон, если снова нажмёшь „Add template“ и выберешь — оно просто заменит шаблон, нужна анимация для этого красивая и плавная» | принято (хореография замены — в работе) | `knowledge/design-system.md` §5 «Хореография вложенного шаблона» |
| 26.08.2026 | **В доке синего «+» на карточке НЕТ.** Клик по карточке дока открывает полноэкранное превью, и уже там кнопка запускает сборку | Дизайнер: «нет, в доке ховера + нет, только открыть превью и там будет кнопка». Сегодня карточка дока запускает генерацию сразу — это меняется | принято | `features/home-page/README.md` §7 |
| 26.08.2026 | **Кнопка запуска в превью, открытом ИЗ ДОКА: `Remix this template`** (в пикере на том же экране остаётся `Choose a template`) | Нейминг делегирован («сам придумай правильное название кнопки на английском»). Выбран глагол продукта: заголовок пикера уже говорит «We'll remix it», и слово отделяет «начать сайт из шаблона» от «прикрепить шаблон к промпту» — два разных действия не должны носить одно имя | принято (наш нейминг, дизайнер может переиграть) | `knowledge/glossary.md` |
| 26.08.2026 | **Ховер нужен на САМУ карточку, и одинаковый в двух местах** — и в доке, и в пикере, потому что в обоих клик открывает полноэкранное превью | Дизайнер: «но ховер нужен, можешь придумать какой-то ховер на саму карточку? он должен быть в обоих случаях». Синий «+» остаётся только в пикере — это второе, другое действие (прикрепить), и он живёт в своём углу | принято (визуал наш, показать дизайнеру) | `features/home-page/figma-spec-add-template.md` §14.10 |
| 26.08.2026 | **Точка в заголовке пикера остаётся:** `Pick a template. We'll remix it.` | Дизайнер спросил «а без точки более правильно?». Довод против: герой на ТОЙ ЖЕ странице устроен той же конструкцией и заканчивается точкой — «Describe it. Remixer builds it.» Две одинаковые по строению фразы на одном экране с разной пунктуацией читаются как опечатка. Это **разворот решения 25.08 «без точки»**; борд `28616:59168` переделан дизайнером на месте (48 px, двухтоновый, с точкой) | принято | `features/home-page/figma-spec-add-template.md` §14.0 |
| 26.08.2026 | **Плашка-тень под подписью карточки остаётся в ОБОИХ состояниях шапки** | Вопрос «на прокрученном борде плашки нет» дизайнер не понял; объяснено, что это рассинхрон двух бордов, а не второе состояние. Возражений нет — оставляем как есть | принято (по умолчанию; переспросить, если всплывёт) | `features/home-page/figma-spec-add-template.md` §14.10 |
| 26.08.2026 | **Заказ: Material-риппл (наш, от точки клика) на чипы фильтров, на «назад» в детальном просмотре и на синюю `Choose a template`; переход при смене фильтра — плавнее и интереснее** | Прямой заказ дизайнера тремя сообщениями. Движок уже есть (`ui/ripple.ts`, `.glass-interactive`), новых механик не требуется — требуется распространение канона и своя хореография у списка под фильтрами | принято (в работе) | `knowledge/design-system.md` §5 |
| 26.08.2026 | **Текст внутри карточек шаблонов не выделяется** (`user-select: none` на миниатюру и подписи) | Баг-репорт дизайнера: «в списке темплейтов текст мешает нажать на кнопку, вместо линка ты просто текст выделяешь». Воспроизведено: растянутая кнопка карточки ЛЕЖИТ поверх всего и по чистому клику срабатывает, но текст выделяем — поэтому протяжка (и особенно начатая в промежутке между карточками) выделяет текст сквозь кнопку. Запрет выделения на карточке — конвенция любой галереи шаблонов | принято (в работе) | `prototype/src/modules/home/Dock.tsx` |
| 26.08.2026 | **Библиотека шаблонов расширяется: восемь новых нарисованных сайтов** (кофейня, fashion-лукбук, dev-tools, AI-аналитика, портфолио фотографа, юрфирма, йога, барбершоп) | Дизайнер: «добавь больше вариантов в список чтобы скрол был больше». Восемнадцать строк на десяти сайтах читались как повтор; по одному новому сайту на каждый фильтр, у каждого свой доминирующий цвет и своя композиция. Правила рисования не меняются: контейнерные единицы, без людей и фотореалистичной еды | принято (в работе) | `prototype/src/modules/home/thumbs.tsx` |

## 2026-08-26 · Home page — фон и вход по записи продакшена

| Дата | Решение | Почему | Статус | Глубина |
|---|---|---|---|---|
| 26.08.2026 | **Для фоновой краски героя Home и хореографии загрузки источник правды — ЗАПИСЬ ПРОДАКШЕНА; борд Figma этой роли лишён** (лейаут, типографика, композер, чипы, карточки — по-прежнему по борду) | Дизайнер отклонил вход v1 и фон против живой страницы: «ни твоя анимация, ни сам градиент, ничего вообще не похоже». Корень: борд считали правдой об осевшем состоянии, тогда как направлением дизайнера была запись. Пересобрано в тот же день по кадрам: МНК-блумы (rms 1.87/255), земля `#17161c`, решётка-«копия поля» 16px, волна 0.385 CSS px/мс, хлопок цвета ~150 мс — коммит `72c763b` | принято (дизайнер, 26.08.2026) | `features/home-page/README.md` §7.4-дополнение |

## 2026-08-25 · Home page

| Дата | Решение | Почему | Статус | Глубина |
|---|---|---|---|---|
| 25.08.2026 (позже) | **Заголовок пикера шаблонов — `Pick a template. We'll remix it`, БЕЗ точки в конце** | Дизайнер выбрал рекомендованный кандидат из шести и нарисовал его без завершающей точки (борд `28616:59168`) — строка канонична как нарисована; вопрос пунктуации (точка/апостроф) остаётся мелкой пометкой спеки §10.1 | **заменено 26.08.2026** — борд переделан дизайнером на месте (48 px, двухтоновый, С точкой); точка подтверждена доводом про герой той же страницы | `features/home-page/figma-spec-add-template.md` §3, §14.0 |
| 25.08.2026 (позже) | **Клик по карточке пикера открывает детальный просмотр; прикрепление — на пилюле `Choose a template`** | Дизайнер нарисовал шаг между карточкой и прикреплением (борд `28637:42088`). Переход карточка→сцена — один FLIP-морф с вложенным counter-scale (fixed point до 0.001px) | принято | `features/home-page/README.md` §7.3 |
| 25.08.2026 (позже) | **Сетка пикера fluid: фиксированные 32px поля, шесть `1fr`-колонок; ниже дизайн-ширины падают КОЛОНКИ (6→5→4→3), не карточки** | Референс дизайнера — сетка Dribbble (~2000: поля те же, карточки тянутся). Минимум колонки `max(200px, «шесть-в-ряд» − 1px)` — auto-fill не может добавить седьмую; 18 карточек делятся на шесть нацело, на восемь — с двумя сиротами | принято | `features/home-page/README.md` §7.2 |
| 25.08.2026 (позже) | **Шапка пикера приколота: заголовок и фильтр-чипы вне скроллера** | Высокие карточки заставляют шит скроллиться и на широких мониторах; увезённые с экрана фильтры бросают сетку. В борде не нарисовано — наше | принято | `features/home-page/README.md` §7.1 |
| 25.08.2026 (позже) | **Вход страницы: полный театр один раз за сессию приложения, возврат из билдера — 300 мс фейд** | Хореография снята покадрово с записи продакшена; политика повтора по одной записи не восстанавливается — наша. Демо скачет между страницами, 4.8 с занавеса на каждый скачок читались бы как поломка | принято (политика повтора — `открыто`, подтвердить у дизайнера) | `features/home-page/README.md` §7.4 |
| 25.08.2026 (позже) | **Вход не трогает краску героя: гасится непрозрачный занавес-РЕБЁНОК над ней; апертура решётки — composited `clip-path`, не `mask-size`** | Расщепление пиксельно-замороженного фона дрейфует (±1/255 на ~946k px; 219 угловых пикселей); mask-size-блум — 15.5 fps против 58.5 у clip-path на софтверном рендере. Осевшая страница байт-в-байт равна QA-бейзлайну | занавес — принято; апертура — заменено 26.08.2026 волной по записи (урок clip-path-против-mask-size стоит; новый эталон осевшего — кадр продакшена, 4/255) | `features/home-page/README.md` §7.5 · §7.4-дополнение |
| 25.08.2026 | **Home — верхнеуровневая СТРАНИЦА (`Root.tsx`), а не поверхность внутри шелла билдера** | У неё своя хромота (прозрачный топбар над героем, нет колонки чата и правого рейла). `App.tsx` с выверенной до миллисекунд хореографией отправки не оборачивается и не перепарентится. Каждая будущая страница (аккаунт, биллинг, хостинг-панель) — ещё одна ветка там же | принято | `features/home-page/README.md` §1 |
| 25.08.2026 | **Активная кнопка Build — почти белая `#fafafa` с лейблом `#09090b`, не синяя** | Так нарисован скрытый фрейм Figma `28364:40238`, и так же выглядит активная пилюля вкладок дока. **Сознательный отход от домашнего правила «синий `#1587FF` = действие» на этой странице.** Реализовано по макету | **заменено 26.08.2026** — дизайнер: активная Build синяя (`#1587FF`); неактивная остаётся призраком | `features/home-page/figma-spec.md` §5.4, §12.4 |
| 25.08.2026 | **Правый край ряда промпт-чипов — настоящая маска, а не плашка с борда** | Figma красит поверх последнего чипа непрозрачный `rgba(40,56,107,0) → #283a71`, подобранный на глаз к герою в одной точке: читается как подсвеченный синий чип и разъедется при любом изменении градиента, позиции ряда или ширины окна | принято, показать дизайнеру оба варианта | `features/home-page/README.md` §4.3 |
| 25.08.2026 | **Миниатюры карточек нарисованы разметкой, а не сфотографированы; людей не рисуем** | Артефакт живёт под CSP, который режет любой внешний запрос, а одностраничная сборка тащила бы шесть JPEG в base64. Четыре из шести миниатюр борда построены вокруг фотографии человека — вместо лица абстрактный градиентный блок в той же композиции | принято | `prototype/src/modules/home/thumbs.tsx` |
| 25.08.2026 | Карточка проекта несёт миниатюру **`payments`** | Так на борде: Figma подставила в неё скриншот первой карточки шаблонов. Воспроизведено честно, с пометкой в `world.ts`, а не «исправлено» молча | принято | `features/home-page/README.md` §4.4 |
| 25.08.2026 | Категории шаблонов — **наши**, не с борда | Ни одна карточка не объявляет категорию, а фильтр-чипы нарисованы. Категории проставлены, чтобы чипы что-то фильтровали; каждая помечена как наша | частично закрыто 26.08.2026: дубль `Ecommerce` отклонён, седьмой топик (`Tech & SaaS`) делегирован нам; распределение остальных карточек по чипам по-прежнему наше | `prototype/src/data/templates.ts` |
| 25.08.2026 | Три «случайности» борда выправлены (чипы все 40px; карточка шаблона 1 приведена к сиблингам 218/54; неактивная вкладка сохраняет нарисованный радиус 8) | Каждая — одна строка назад, если дизайнер хочет случайность | открыто | `features/home-page/qa-report.md` §7 |

## Открытые расхождения — ждут решения (25.08.2026)

Это не «баги документации», а вопросы, на которые нужен ответ человека. Пока ответа
нет, прототип живёт по правому столбцу. Полный список расхождений между источниками, включая
устаревшие цены в макетах, — `knowledge/product-facts.md` §8.

1. **`Add` или `Buy` на новом домене.** Словарь аудита говорит «Add = купить», макет
   доменного дашборда (27085:106382) рисует «Buy». **Правит сегодня:** макет — так и
   реализовано. → **Решение нужно от дизайнера:** один глагол на действие, глобально.
   Два глагола на одно действие хуже любого из них.
2. **Кредиты за публикацию.** Verified-факт продукта: кредиты тратятся и на публикацию.
   Вывод аудита №1: публикация должна стоить 0 кредитов. **Правит сегодня:** аудит —
   в прототипе публикация кредитов не списывает. → **Решение нужно от продукта:** снять
   плату или сознательно оставить. Это самая атакуемая строка сравнительных таблиц.
3. **Фон билдера `#18181B` против `#09090B`.** Первый — verified-факт о живом продукте,
   второй — редизайн 2026 (Figma 28016:42812). **Правит сегодня:** макет — прототип
   красит `#09090B`. → **Решение нужно от дизайнера:** подтвердить `#09090B` как целевой
   фон, либо развести два значения по названиям, чтобы одно не подставляли вместо другого.
4. **Domain Connect: нарисован путь, которого у продукта нет.** Хендофф (сценарий ③)
   описывает one-click для ~50 регистраторов; verified — DreamHost не поддерживает Domain
   Connect ни в какой роли. **Правит сегодня:** факт — one-click невыполним.
   → **Решение нужно от продукта/бизнеса:** покупать Entri (~$249/мес, цена unverified)
   или нет. Пока нет — дизайнеру перерисовать ③ на ручной путь. Блокирует весь сценарий
   «внешний домен».
5. **Гибридный старт «промпт + шаблон»: реализован; открытым осталось прикреплённое
   состояние.** Предложено дизайнером 25.08.2026, в тот же день нарисовано и собрано:
   пилюля «Add template» в композере, пикер (`28616:59168`), детальный просмотр
   (`28637:42088`). **Заголовок попапа — решён 25.08.2026:** дизайнер выбрал
   рекомендованный «Pick a template. We'll remix it», нарисован и реализован
   **без точки в конце**. **Правит сегодня:** макет + наш мок — прикреплённое состояние
   (чип с мини-миниатюрой и ✕ в композере) нигде не нарисовано и собрано как proposal;
   Build сознательно НЕ переименован в «Remix». → **Решение нужно от дизайнера:**
   нарисовать прикреплённое состояние (или подтвердить мок). Разбор целиком —
   `features/home-page/README.md` §7.
6. **Печатающийся плейсхолдер композера.** Живой продукт печатает текст плейсхолдера по
   буквам — постоянная per-frame текстовая анимация, прямое нарушение
   перформанс-контракта страницы (61 fps в покое, ноль покадрового repaint).
   **Правит сегодня:** контракт — не собрано. → **Решение нужно от дизайнера (и по
   бюджету кадров):** сначала разговор о цене, потом реализация — не молча.
   **26.08.2026 (вечер):** дизайнер сказал «не совсем понял, о чём ты» — цена объяснена
   словами и предъявлены три варианта: **(а)** напечатать один раз при загрузке и
   остановиться (почти бесплатно, one-shot, как вход); **(б)** менять примеры целиком
   плавным затуханием, без побуквенности (дешево); **(в)** как в продакшене, бесконечно
   (сознательно платим кадрами). Ждём выбора.
7. **Решётка точек на входе: продакшен против нашей версии.** В записи продакшена
   тусклая решётка приходит ВМЕСТЕ с глоу, и по ней идёт более яркая волна точек на
   ~2.5 с; у нас решётка невидима до апертуры и проявляется одной волной (чище и дешевле
   по кадрам). **Правит сегодня:** наша версия. → **Решение нужно от дизайнера:** какой
   вариант канонический.
   **Закрыто 26.08.2026:** дизайнер отклонил вход v1 против живой страницы — канон
   буквально продакшеновский. Решётка приходит вместе с цветом (хлопок ~150 мс), по ней
   идёт волна перегорающих точек 1.65–4.10 с (0.385 CSS px/мс); сама решётка на герое —
   яркая копия цветового поля сквозь ромбовидную маску 16px, не 6 % белого.
   `features/home-page/README.md` §7.4-дополнение, коммит `72c763b`.
8. **Финальный кадр SVGator-ассета ≠ знак борда** (искра-вырез крупнее, серые светлее:
   `#71717a` против `#52525b`). **Правит сегодня:** знак борда — вход кросс-фейдит его
   поверх собранного логотипа на 1.9 с (читается как финальный сеттл), осевший DOM
   держит только знак борда. → **Решение нужно от дизайнера:** привести ассет и борд
   к одному знаку.

## Продуктовые позиции (из аудита; не решения дизайнера, но обязательны к учёту)

| Дата | Позиция | Почему | Статус | Глубина |
|---|---|---|---|---|
| 13.08.2026 | **Публикация должна стоить 0 кредитов** | Мы единственные в категории, кто берёт кредиты за момент успеха (кроме Emergent ~2.7/5). Самая атакуемая строка любой сравнительной таблицы | принято как позиция аудита; в прототипе публикация кредитов не тратит | `audits/synthesis.md` §1.3 |
| 13.08.2026 | Владеть **60 секундами вокруг go-live**, а не «продажей домена» | Продажа домена в потоке стала table stakes: Lovable — регистратор, Vercel продаёт домены в v0 | принято как позиция аудита | `audits/synthesis.md` §8 |
| 13.08.2026 | Кредиты — **постоянно в тулбаре** | GoDaddy вынес их на отдельную страницу; это их самая громкая жалоба | принято | `prototype/src/App.tsx` |
| 13.08.2026 | Де-жаргон в основных путях: никаких DNS / nameserver / A record / SSL certificate | Аудитория — полный новичок («housewife test») | принято | `knowledge/glossary.md` |
| 06.08.2026 | Не делать DreamHost **провайдером** Domain Connect | Это ускоритель оттока (домен уезжает одним кликом) | принято | `knowledge/product-facts.md` §4 |
| 06.08.2026 | Никаких состояний «Make an offer» / aftermarket | DreamHost verified не продаёт премиум-домены и не имеет брокериджа | принято | `knowledge/product-facts.md` §4 |
| 06.08.2026 | Хук «бесплатный домен на первый год» **не использовать** в основном флоу | Кредит действует только для годовых Web Hosting / DreamPress; билдер-план по документации не квалифицируется | принято | `knowledge/product-facts.md` §4 |
| июнь 2026 | Публиш-панель — направление **«Launchpad» (⑥-A)**: один доминирующий герой «PUBLIC WEBSITE» + демотированная строка «PRIVATE PREVIEW LINK» | Из трёх нарисованных направлений выбрано дизайнером; альтернативы ⑥ (две равные карточки) и ⑥-B (Site Status) сохранены для сравнения | принято | `handoff/domain-connection-design-handoff.md` |

---

**Не найдено в истории:** решения до 16.08.2026 датированы «≤16.08.2026» — в клоне
репозитория история обрезана (grafted) на коммите от 16.08.2026, поэтому точные даты
более раннего периода восстановить нельзя. Даты, взятые из `CLAUDE.md` и из документов
(06.08, 13.08, июнь 2026), указаны как есть.
