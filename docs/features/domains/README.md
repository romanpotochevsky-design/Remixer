# Домены — единая точка входа

> Прочитал этот файл — знаешь, где стоит фича. Дальше по ссылкам: что решено
> (`DECISIONS.md`), какие состояния должны существовать (`STATES.md`), что ещё висит
> (`OPEN-QUESTIONS.md`). Правило: любое новое знание о доменах попадает в один из этих
> четырёх файлов, а не в голову сессии.

## Что это за фича и для кого

Доменный флоу Remixer — это всё, что происходит между «сайт сгенерирован» и «сайт живёт
на своём адресе». Внутри тёмного шелла билдера, вместо области превью, открывается
доменная поверхность: поиск имени, покупка, подключение уже имеющегося домена, статус
подключения. Ничего не открывается в панели DreamHost и ни в одной другой вкладке —
это принципиально (`DECISIONS.md` 11 «Ставка — zero-record go-live, и она про размещение,
а не про механизм»).

**Аудитория — полный не-технарь («housewife test»).** Человек, который никогда не слышал
слов «nameserver» и «A record», и не должен услышать. Он пришёл не «настраивать DNS», он
пришёл, чтобы его сайт был по адресу, который он может назвать вслух.

**Почему это важно коммерчески.** Кастомный домен — момент удержания: превью на
`*.remixer.ai` бесплатно всегда, а публикация на свой адрес требует плана Remixer Build
(цена — `../../product/FACTS.md` DH-001; сам гейт — DH-005).
⚠️ Две оговорки, обе из регистра. В зоне `remixer.ai` подтверждена только сама зона
(**DH-302**, verified 20.08.2026 — исправлено с `remixer.site`); форма левой части адреса
(`{project}.remixer.ai`?) не подтверждена. А «скрыто от Google» — **unverified**
(**DH-303**, понижено 20.08.2026: ни в KB, ни на странице продукта, ни в условиях триала
нет ни слова про индексацию). Раньше эта строка стояла здесь как факт — не восстанавливать
и в материалы для руководства не брать; в прототипе она печатается в UI, и это записано как
блокер в `../publish/README.md`.
Плюс у нас есть то, чего нет ни у одного конкурента-билдера:
DreamHost — регистратор для большой книги уже купленных, но неиспользуемых доменов.
Подключить такой домен можно вообще без единой записи, за секунды. Ни Lovable, ни v0, ни
Bolt так не могут — они умеют автоматизировать только домен, который продали тебе сегодня.
GoDaddy и Hostinger структурно могут, но закопали это в настройки (**CMP-014** — у GoDaddy
путь лежит на четыре клика в дереве настроек, а покупка домена внутри билдера вообще
выбрасывает в магазин) или выкидывают в hPanel (**CMP-015**). **Преимущество — не в механизме, а в размещении:
в моменте публикации.**

## Граница с фичей публикации

Канонический текст — `../README.md`, решение 2. Ниже он приведён дословно; правится он там,
и тогда правятся обе копии.

> **Публикация владеет панелью и самим актом публикации. Домены владеют получением адреса и
> его привязкой к сайту.** Шов — две двери, и обе принадлежат ему: dashed-карточка «Connect
> your own domain» в панели Publish и чип адреса в центре топбара. Чип показывает адрес
> (сторона публикации) и открывает доменную поверхность (сторона доменов).

Практическое следствие, которое эта волна и закрывала: **какой домен привязан** — состояние
мира (`world.customDomain`), а не константа и не навигация, и над ним стоят **два** селектора
адреса, которые на `connecting` намеренно расходятся (`DECISIONS.md` 29; панельная половина —
`../publish/DECISIONS.md` 02).

## Три сценария

| # | Сценарий | Ось `Inventory` в прототипе | Что видит человек |
|---|---|---|---|
| 1 | **Купить новый домен** | `none` | Поиск → выдача (точное совпадение героем, другие зоны, AI-идеи имён) → чек-аут-шит → подключается сам |
| 2 | **Подключить домен, который уже на DreamHost** | `dh-free`, `dh-in-use` | Домен в списке «Existing domains» → confirm «You own this» → «Connecting…» → live. Нуль записей, ноль других вкладок, меньше минуты |
| 2б | **Домен наш, но его настройки живут у другой компании** | `dh-external-ns` | Тот же вход, но выйти сам он не может: одна перемена нужна у названной компании, и срок мы обещать не вправе (**DH-308**; вариант B состояния `connecting` в `STATES.md`) |
| 3 | **Подключить домен у чужого регистратора** | `external-dc`, `external-manual` | Имя уже зарегистрировано → называем регистратора и **спрашиваем**, его ли это домен → «Yes, connect it» → что вставить у него → «Connecting» с возможностью уйти → live |

Сценарий 2 — наш козырь и должен стоять первым везде, где встречаются все три.
Сценарий 3 — самый хрупкий: в нём достижимо больше всего сбойных состояний. Но НЕ все они
живут только здесь — по матрице достижимости в `STATES.md` `in-use-here` возникает
исключительно в сценарии 2 (`dh-in-use`), а `ready` в сценарии 2 встречается «часто».
Формулировку «все тринадцать сбоев живут в сценарии 3» не восстанавливать: она противоречит
нашей же матрице.

Внутри сценария 2 есть три разных мира, и их нельзя путать: `dh-free` (свободный домен —
подключаем молча), `dh-in-use` (домен уже показывает сайт — нужен выбор, а не страшный
confirm) и `dh-external-ns` (домен наш, но управляется на стороне, например в Cloudflare —
записи, которые мы пишем у себя, просто не подействуют, и это надо объяснить). Третий —
причина, по которой он вынесен из строки сценария 2 выше: «меньше минуты, ноль других
вкладок» там неправда, и обещать это нельзя (**DH-308**).

## Карта: что где лежит

### Знания (эта папка)

| Файл | Что внутри |
|---|---|
| `README.md` | Этот файл. Точка входа и таблица статусов. |
| `DECISIONS.md` | Что решено и что отклонено, хронологически, с датой и доказательством. Читать до того, как предлагать идею — половина «свежих» идей уже отклонена. |
| `STATES.md` | Именованная стейт-машина подключения: состояния, EN-копирайт, глаголы, карта тринадцати сбоев, коллизии. Главный недостающий дизайн-дэлаверабл фичи. |
| `OPEN-QUESTIONS.md` | Что блокирует, кто отвечает, что делаем без ответа. |
| `handoff-design.md` | **Готовый вход, не редактировать.** Дизайн-хендофф в Figma: три сценария, интеракционная модель, Launchpad, правила копирайта. Наверху — блок поправок от 19.08.2026: три утверждения внутри устарели (Domain Connect, цены, неполная стейт-машина). |
| `research/search.md` | **Готовый вход, не редактировать.** Ресёрч ПОИСКА домена: факты DreamHost со ссылками на KB, UX поиска у 10 конкурентов, UX AI-подсказок, флоу «у меня свой домен», техническая детекция (RDAP, PSL, лимиты). Каждый факт с тегом `[verified]`/`[likely]`. |
| `research/connect.md` | **Готовый вход, не редактировать.** Ресёрч ПОДКЛЮЧЕНИЯ (17 продуктов): пять бит канонического флоу, три рельса автоматизации (Entri / Domain Connect / «свой дом»), §5 таблица статус-машин, §6 каталог из 13 сбоев, §9 коллизии, §12 шесть решений. Позвоночник `STATES.md`. |

Смежное: `../publish/README.md` (панель Publish — точка входа в доменный флоу),
`../../competitors/audits/synthesis-q3-2026.md` §8.1 (zero-record go-live) и §9 (токены,
где амбер зарезервирован под stale-dot и «needs attention»),
`../../competitors/audits/lovable-builder-teardown.md` (publish-флоу Lovable по пяти
состояниям, снятый с живого аккаунта).

**Числа и язык живут не здесь.** Проверенные факты — в едином регистре
`../../product/FACTS.md`: цену, срок или лимит цитируем по ID (`DH-101`), а не переписываем
своими словами — переписанная цифра это цифра, которая начнёт дрейфовать. Правила языка
продукта (словарь глаголов, де-жаргон, канонический чеклист, обещания про ожидание) —
`../../product/COPY-RULES.md`, и он ссылается на `STATES.md` как на источник имён
состояний. Сквозные решения проектного уровня — в `../../decisions/`; в `DECISIONS.md`
рядом с этим файлом только решения по доменной фиче.

### Figma

Файл **AI-Website-Builder**, страница **«Domain Connection Flow»**:
https://www.figma.com/design/GP4jNXtc37VTFVZDc9JF0a/AI-Website-Builder

Секции: ① Buy a new domain · ② Connect a DreamHost domain · ③ Connect an external domain ·
④ Manage domains · ⑤ Plan gate · ⑥ Publish panel · **⑥-A Launchpad (выбранное направление)** ·
⑥-B Site Status (альтернатива) · ✎ Alternates (старые варианты) · 📱 Mobile · iOS ·
㉗ «Умный поиск · все состояния».

Узлы, по которым собран прототип: дашборд доменов `27085:106382` / `26181:33524`,
шапка пустого состояния `27085:107047`, выдача `27729:14650` (шапка `27729:15315`),
строка результата `27257:14000`, чек-аут-шит `27058:100133` / `27254:11737` / `27275:33023`
(кнопка закрытия `27328:11613`), панель Publish `25819:144061`, шелл `25819:143144`.

### Прототип (реализация)

| Файл | Что делает |
|---|---|
| `prototype/src/state/world.ts` | Оси состояния. `Inventory` = что у человека есть, `DomainState` = что делает домен проекта. Новое измерение добавляется сначала здесь. |
| ~~`prototype/src/state/flows.ts`~~ | **Удалён 20.08.2026** вместе с `devtools/FlowPlayer.tsx` — по просьбе дизайнера: степпер, пересказывающий продукт, — не то, как клиент встречает интерфейс. Флоу проходятся руками по настоящему UI, пресеты `scenarios.ts` дают только стартовое состояние. |
| `prototype/src/state/scenarios.ts` | Мировые пресеты консоли: `dh-zero-record`, `connecting`, `live`, `live-stale`, `domain-broken`, `external-dc`, `external-manual`. Обе внешние ситуации решают ровно одно: **какой регистратор определяется** (Cloudflare / Namecheap) и, следовательно, какой путь подключения идёт дальше. Владение они не назначают — его нельзя знать. |
| `prototype/src/data/domains.ts` | Цены (`TLD_PRICES`, verified), AI-подсказки, генераторы выдачи, домены в аккаунте (`OWNED_DOMAINS`), `STAGING_HOST`. |
| `prototype/src/modules/domains/DomainsSurface.tsx` | Пять экранов поверхности: `home`, `results`, `own`, `external`, `status` + постоянная шапка поиска, в которой с 20.08.2026 живут **два** постоянных элемента: поле и строка «Already have a domain? Connect it» (`DECISIONS.md` 28). |
| `prototype/src/modules/domains/DomainModal.tsx` | Чек-аут-шит: один компонент на шесть нарисованных состояний по двум осям. Здесь же живёт плановый гейт и здесь же в мир пишется, **какой** домен привязывается. |
| `prototype/src/modules/domains/ConnectChecklist.tsx` | Канонический чеклист успеха: один список, два дома (статус-экран и панель Publish). Четыре стадии за тремя пунктами — четвёртая существует ради `securing`. |
| `prototype/src/modules/publish/PublishPanel.tsx` | Панель Publish — точка входа в доменный флоу. |
| `prototype/scripts/smoke.mjs` | Браузерный проход (ворота 2, `../../global/AGREEMENTS.md` §5). Доменные экраны в нём: дашборд, выдача, вопрос о владении, чек-аут-шит на обеих осях, все значения оси `domain` холодным свипом, обе внешние ситуации, устаревшая ссылка. Запуск — `npm run smoke`. |

## Статус: что нарисовано, что реализовано, чего нет

Обозначения: **Figma** — фрейм существует · **Прототип** — работает в коде ·
**Решено, не нарисовано** — решение принято в документах, фрейма нет ·
**Решено, не реализовано** — фрейм и решение есть, а код говорит другое ·
**Расхождение** — код и решение расходятся в деталях, которые видит человек ·
**Открыто** — решения нет. «—» значит «нет», и это не оценка, а факт.

### Поиск и покупка

| Экран / состояние | Figma | Прототип | Статус |
|---|---|---|---|
| Дашборд доменов: «Existing domains» + «AI suggestions» с героем Best match | да | да | готово. ⚠️ В двух внешних ситуациях (`external-dc`, `external-manual`) колонка «Existing domains» **пустая, и это правда**: про домен в чужом регистраторе мы знать не можем (`DECISIONS.md` 31) |
| **Постоянная строка «Already have a domain? Connect it»** под полем поиска | — | да, в обоих состояниях шапки, без анимации | **Реализовано по просьбе дизайнера, фрейма нет.** Шапка выросла на 32px, отступление названо. `DECISIONS.md` 28; вопросы 16 (фрейм + сегмент на мобильном), 17 (плейсхолдер повторяет), 18 (два вопроса о владении рядом) |
| Выдача поиска: точное совпадение героем → другие зоны → «Name ideas for your site» | да | да | готово |
| Чек-аут-шит: `connect-existing` / `buy` × есть план / нет плана (6 состояний) | да | да | готово |
| **Имя уже зарегистрировано** (в макетах — «домен занят») | — | да, как **вопрос**: «Already registered · Registered at {registrar}. Is it yours?» + «Yes, connect it», без цены и без слова *taken* | **Частично закрыто в коде, фрейма по-прежнему нет** (вопрос 01 остаётся). Разница между «занято» и «моё» на уровне системы не существует: `research/connect.md` §7 — **никто в отрасли не проверяет владение в момент поиска**, все формулируют условно (Shopify «If you already own…», Wix, Squarespace, Netlify «[domain] already has an owner. Is it you?»), а доказательством служит сама смена записей позже. Поэтому строка утверждает только регистратора (RDAP, уровень реестра) и спрашивает про остальное. Два выхода из одной строки: «да» → внешний экран, «нет» → альтернативы ниже. `search.md` own-domain-transfer-ux прямо рекомендует копировать этот паттерн Netlify — **и ни один конкурент не задаёт этот вопрос на экране выдачи** |
| Деградация поиска (сервис недоступен, бот-гейт) | — | — | **Открыто.** У живого поиска DreamHost это тупик без повтора — повторять нельзя. Вопрос 02 |
| Опечатка в зоне («.con» → «Did you mean .com?»), ввод без зоны → генерация имён | — | частично (ввод без точки уходит в поиск) | Решено на уровне ресёрча (`research/search.md`, technical-detection). ⚠️ Опечатка в зоне сейчас читается как **чужой домен**: `submit()` отправляет всё, в чём есть точка, на экран внешнего подключения, и человек с опечаткой видит «Registered at GoDaddy» |
| Premium / «Make an offer» | — | — | **Никогда.** У DreamHost нет брокериджа. `DECISIONS.md` 03 «Ни премиум-доменов, ни „Make an offer“ — никогда» |
| Трансфер как поздний опциональный апселл | не подтверждено | — (единственное упоминание — успокаивающая строка «It stays there — no transfer needed» на внешнем экране) | Решено (не предусловие): `DECISIONS.md` 06 «Connect — по умолчанию, transfer — поздний необязательный апселл» |

### Подключение

| Экран / состояние | Figma | Прототип | Статус |
|---|---|---|---|
| Confirm «You own this» для свободного домена на DreamHost (`dh-free`) | да (②) | да | готово |
| Гард «этот домен уже показывает сайт» (`dh-in-use`) | частично | да, но **один** страшный confirm «Replace site at {domain}» | Решено иначе: два названных варианта с последствиями (`STATES.md`, коллизии; `DECISIONS.md` 20 «Коллизию решает пользователь, а не поддержка») — не нарисовано |
| Гард «домен наш, но управляется снаружи» (`dh-external-ns`) | не подтверждено | да (амбер-карточка про Cloudflare) | Решено, фрейм под вопросом. ⚠️ Карточка правильная, а состояние ПОСЛЕ неё — нет: домен уходит в общий «Connecting», который обещает «пара минут». Нужен вариант B состояния `connecting` (`STATES.md`) |
| Внешний домен: определён регистратор + что вставить у него | да (③) | да; регистратор больше не захардкожен — берётся из ситуации (`registrarFor`: `external-manual` → Namecheap **STD-003**, `external-dc` → Cloudflare **STD-002/STD-004**, иначе прежний GoDaddy) | готово, но покрыт один регистратор из списка «топ-8», названного в `DECISIONS.md` 17 «Ручной путь — primary; one-click — помеченный вариант». ⚠️ Карточка записей стоит открытой в основном пути, а COPY-RULES §3 требует прятать её за строкой «Prefer to do it yourself? Show the settings» — на ручном пути это взаимоисключающие требования, и решается это вопросом 03 (сравнение вместо таблицы записей) |
| Внешний домен: one-click согласие (Domain Connect / Entri) | да, но в Alternates | — | **Условный дизайн.** Требует PO на Entri. `DECISIONS.md` 17 «Ручной путь — primary; one-click — помеченный вариант» + 04 «Не проектируем вокруг Domain Connect» |
| Diff «что мы видим сейчас / что должно быть» вместо таблицы записей | — | — | **Открыто.** Самое рычажное решение флоу. Вопрос 03 |
| Страховка почты (переносим настройки почты, а не ломаем) | — | — | Решено (`research/connect.md` §12.4) как строка в конфирме — не нарисовано |
| Коллизия «домен в другом аккаунте DreamHost» | — | — | **Открыто.** Нужна политика. Вопрос 06 |

### Состояния после нажатия «Connect»

Полная спецификация — в `STATES.md`; там же полный список расхождений с прототипом. Здесь
эти же состояния по столбцам «нарисовано / работает», и три верхних строки — про то, что
счастливый путь **кликается, но говорит не то**, а не про отсутствующие фреймы.

| Состояние | Figma | Прототип | Статус |
|---|---|---|---|
| `connecting`, вариант A (домен в аккаунте DreamHost) | да | да (`status`, стадия 0) | готово |
| `connecting`, варианты B и C (настройки у другой компании / чужой регистратор) | — | **нет: та же строка «Usually a few minutes» печатается на всех путях** | **Решено, не реализовано.** Обещание, которого продукт не выполняет на двух путях из трёх — `FACTS.md` §2.16 |
| `securing` (сертификат выпускается) | частично | да, с 20.08.2026: своя стадия чеклиста, своё надглавие «Almost there — turning on the padlock» и своя строка ожидания (десять-тридцать минут, **DH-301**) — и то же самое в панели Publish | готово по копирайту. ⚠️ Остаётся расхождение по кнопкам: правило состояния в `STATES.md` говорит «глагола, действующего на домен, нет», а на экране стоят `Refresh status` и `Keep editing`. Автопродвижение времени вернулось, значит «ни одной кнопки» снова реализуемо — вопрос дизайнеру открыт в `STATES.md` |
| `live` + канонический чеклист успеха | да | да; пункты тикают **по одному** — четыре стадии за тремя пунктами (`ConnectChecklist.tsx`, `connectStage`) | готово. Порядок и есть информация: замочек приходит последним (**DH-301**). ⚠️ Остаются расхождения кода и карточки по главному действию и по строке «Share it to get your first visitors.» — перечень в `STATES.md` |
| `ready` — домен настроен, сайт не опубликован | — | да, в двух местах: статус-экран доменной поверхности и панель Publish | **Реализовано как ПРОЧТЕНИЕ, фрейма нет.** Панель печатает домен, даёт копирование и выход в `Manage domains`, но **не** ставит зелёную точку и **не** обещает замочек. Плюс синяя кнопка в этом состоянии теперь действительно публикует — изменение поведения. `DECISIONS.md` 29; вопросы 19 и 20 |
| `waiting-on-you` — таймаут ~1 ч | — | — | **Решено, не нарисовано** |
| `unfinished` — настройку начали и бросили | — | — | **Решено, не нарисовано** |
| `needs-attention` — работало и перестало | — | да в доменной поверхности («Stopped showing your site», янтарная точка, `Fix this`); **в панели Publish — нет: она предлагает `Connect your own domain` подключённому и сломанному домену** | **Долг закрыт наполовину, фрейма нет ни на одну половину.** `DECISIONS.md` 26 (дополнено 20.08.2026); вопрос 19 |
| `wrong-site` — по адресу открывается другой сайт | — | — | **Решено, не нарисовано** |
| `multiple` — значение оси `world.domain` | — | рендерится как `live` (стадия 3) минус зелёная точка | **Не названо.** У этого значения оси в стейт-машине нет имени состояния: либо назвать, либо убрать с оси (`STATES.md`, «Расхождения с прототипом»; вопрос 22) |

### Вокруг

| Экран | Figma | Прототип | Статус |
|---|---|---|---|
| Панель Publish, направление Launchpad (⑥-A), 4 состояния | да | да, но пересобрана в язык `25819:144061`: Case 1 и Case 2 сведены в одну dashed-карточку с одной меняющейся строкой | расхождение зафиксировано, см. `../publish/README.md` |
| Адрес в панели Publish и в топбаре | `28071:53189` рисует одно состояние из нужных | да, из мира: `world.customDomain` + два селектора; на `connecting` панель и топбар печатают **разное намеренно** | готово. `DECISIONS.md` 29, `../publish/DECISIONS.md` 02. Не сливать селекторы «для консистентности» |
| Карточка-кандидат «{domain} уже в вашем аккаунте DreamHost — 0 записей» в панели Publish | — | — | Решено аудитом (§8.1), не нарисовано |
| Plan gate отдельным экраном (⑤) | да | снят осознанно | Гейт живёт внутри чек-аут-шита. `DECISIONS.md` 22 «Стена плана перед доменной поверхностью убрана; гейт — внутри чек-аут-шита» |
| «Manage domains» после live: сменить основной адрес, отключить, редирект | да (④) | — | Решено только как фрейм, кода нет |
| Мобильные экраны (📱, 10 штук) | да | — | Прототип — только десктопный шелл (мобильный режим превью — это рамка сайта, а не мобильный UI билдера) |

**Честный итог на 20.08.2026.** Из трёх строк, которыми этот раздел был написан («счастливый
путь кликается, но говорит не то»), **две закрыты**: у `securing` появилось своё состояние с
собственным надглавием и своей стадией, и чеклист успеха больше не тикает двумя пунктами
разом — замочек закрывается последним, как и обещает **DH-301**. Осталась одна: `connecting`
по-прежнему печатает «Usually a few minutes» на всех трёх путях подключения, включая тот, где
документация DreamHost обещает сутки (`FACTS.md` §2.16). Проверка одной командой —
`grep -rn "Usually a few minutes" prototype/src`: одно вхождение, без ветвления по пути.

**Но характер долга изменился, и это важнее счёта.** Раньше расхождения были «нарисовано, а в
коде не так». Теперь два самых свежих — наоборот: **в коде есть то, чего нет в макетах**.
Строка «Already have a domain? Connect it» и ветка `ready` в панели Publish реализованы как
**прочтения** — первая из ресёрча, вторая из стейт-машины. Прочтение не равно дизайну
(§7 соглашений), поэтому в очереди на рисование они стоят первыми, а в продакшен-артефакт до
слова дизайнера не уходят: вопросы 16, 19, 20.

Из тринадцати известных сбоев подключения **девять не нарисованы вовсе, ещё два —
частично** (перечень и нумерация — `research/connect.md` §6; что именно частично — `STATES.md`), и
это примерно 40% реального трафика. Плюс не нарисовано состояние «домен занят» — то есть
базовый случай поиска. Это не недоделка ресёрча: материал есть, фреймов нет.

**Один шаг браузерного прохода красный — специально.** Ситуация 20 в
`prototype/scripts/smoke.mjs` («own domain → zero-record connect») падает, потому что две
двери из трёх пишут в мир только фазу подключения, а не имя домена: подключение из списка
«Existing domains» всё ещё оставит в панели предыдущий адрес. Проверка —
`grep -rn "domain: 'connecting'" prototype/src/modules/domains/DomainsSurface.tsx`.
Утверждение в проходе не ослаблено, чтобы он позеленел (`DECISIONS.md` 29).

## Инварианты (нарушение = переделка)

1. **Словарь глаголов.** Add / Buy = купить · **Connect** = подключить своё ·
   **Publish / Update** = переопубликовать. Расхождение «Add vs Buy» — вопрос 08.
   Два запрета внутри этого же правила: `Transfer` в основном пути не появляется никогда
   (другой продукт, решение 06), и к `Connect` никогда не дописывается «free» —
   бесплатность не новость, а отсутствие списания (`COPY-RULES` §2, решение 28).
2. **De-jargon.** Ни в одной строке основного пути не появляются DNS, nameserver,
   A record, CNAME, 301, «SSL certificate». Вместо них: "Point your domain to us",
   "secure padlock", "the two lines to paste at {registrar}".
3. **Канонический чеклист успеха, всегда в одном порядке:**
   "Domain settings updated · Connected to your site · Security (SSL) on".
   Порядок — это и есть информация: замочек приходит последним (**DH-301**), поэтому третий
   пункт закрывается **позже** второго, а не вместе с ним. Стадии — в `STATES.md`, раздел
   «Канонический чеклист».
4. **Цена до корзины, продление никогда не спрятано.** Первый год крупно, "Renews at $X"
   рядом. Цифры — только из `TLD_PRICES`.
5. **Никакой строки списка, которая подключает по клику.** Любая строка ведёт на confirm.
6. **Ноль записей — вслух.** Для домена в аккаунте DreamHost мы единственные, кто может
   написать «мы и так знаем, что он ваш». Это надо говорить, а не подразумевать.
7. **Публикация стоит 0 кредитов** — это позиция, а не описание продукта. Слово «Free»
   из UI прототипа убрано 20.08.2026 (решение 27), потому что сегодня публикация кредиты
   тратит (**DH-008**), а сколько — неизвестно (**DH-009**). Позиция в силе, обещание с
   экрана снято; сам вопрос — 12.
8. **Ни одного срока без ID.** Обещание времени стоит на ряде регистра (**DH-217** /
   **DH-203** / **DH-301**), и у трёх путей подключения обещания разные — одна строка на все
   три и есть тот сбой, из-за которого переписан `STATES.md` (`FACTS.md` §2.16).
9. **У адреса сайта один дом — мир.** Какой домен привязан, живёт в `world.customDomain`;
   ни константа, ни навигационный стор (`activeDomain`) источником адреса быть не могут.
   Селекторов **два** (`siteAddress` — где сайт отвечает, `projectDomain` — что привязано),
   и на `connecting` они расходятся намеренно. Слить их «для консистентности» = удалить
   одно из двух правильных поведений (решение 29).
